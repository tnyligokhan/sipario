# DECISIONS.md — Sipario

Her satır bir karar ve tek cümlelik gerekçesi. Yeni kararlar sona eklenir, eskiler silinmez;
değişen bir karar "~~üstü çizili~~ → yeni karar" biçiminde güncellenir.

## Stack

- **Mobil: Flutter + native Kotlin köprüsü** — ekipte Flutter deneyimi var, tek kod tabanı Android+iOS; arayan tanıma zaten native yazılacağı için Flutter engel değil.
- ~~API: Laravel 12 / PHP 8.3~~ → **API: Laravel 13 / PHP 8.3** — iskelet oluşturulduğunda güncel major 13'tü (kurulu: v13.19.0); karar yazıldığındaki "12" o günün güncel sürümüydü, niyet "güncel Laravel + PHP 8.3"tür; gerekçe değişmedi (ekip deneyimi + bakım maliyeti).
- **Veritabanı: PostgreSQL 16** — Row-Level Security ile kiracı izolasyonu veritabanı seviyesinde zorlanabiliyor (kırmızı çizgi #1); MySQL'de bu yok.
- **Yerel DB: SQLite (Drift)** — offline asıl üründür, Drift tip güvenli sorgu ve migration disiplini verir.
- **Site + yönetim paneli: Laravel + Livewire, aynı repo ayrı guard** — v1'de ayrı frontend'in maliyeti karşılığını vermiyor.
- **Ödeme: iyzico** — TR yerleşik (KVKK/veri yerleşimi), kart saklama + abonelik API'si olgun; PayTR'nin yinelenen tahsilatı zayıf, Craftgate bu ölçekte pahalı.
- **Sunucu: Türkiye'de barındırılan VPS + Docker** — kırmızı çizgi #4; Hetzner/DO/Firebase bu yüzden eleniyor.
- **Hazır senkron motoru (PowerSync/Realm) kullanılmayacak** — defterin çakışma kuralları bize özgü, kara kutuya emanet edilmez.

## Veri modeli

- **Para ve hareket kayıtları append-only (`ledger_entries`)** — kırmızı çizgi #2; silme/güncelleme yok, düzeltme yalnız ters kayıtla.
- **Bakiyenin doğruluk kaynağı defterdir; `customers.balance_kurus` yalnız bir okuma-modeli önbelleğidir** — arayan tanımanın 1 sn bütçesinde native taraf toplama yapamaz, tek satır okur; önbellek her defter yazımında tazelenir ve bozulursa defterden yeniden kurulur.
- **Para her yerde kuruş cinsinden tam sayı** — kayan noktalı sayı esnafın defteriyle kuruş farkı üretir, ürüne güven ölür.
- **Kimlikler UUIDv7 ve istemcide üretilir** — offline oluşturulan kaydın id'si sunucuya çıkınca değişmez, referanslar kırılmaz, senkron sırası önemsizleşir.
- **`customer_phones` ve `customer_addresses` ayrı tablolar** — müşterinin ev+cep numarası, ev+işyeri adresi olur; tek sütun 1NF ihlaliydi ve arayan tanımayı ikinci numarada kör bırakıyordu.
- **`tenant_id` her tabloda tekrarlanır (bilinçli 3NF sapması)** — RLS politikası her tabloda join'siz çalışsın diye; tutarlılık `(tenant_id, parent_id)` bileşik yabancı anahtarıyla veritabanı seviyesinde zorlanır.
- **`order_lines.unit_price` ve `product_name` satırda saklanır** — denormalizasyon değil: satırdaki fiyat ürünün bugünkü fiyatının fonksiyonu değil, siparişin çekildiği andaki gerçeğidir.
- **`orders.status` bir önbellek sütunudur, kaynak `order_events`** — sorgu kolaylığı için tutulur, doğruluğun kaynağı olay tablosudur.
- **Kupon bakiyesi eksiye düşebilir** — teslim edilmiş mal gerçektir, sistem reddedemez; UI kırmızı gösterir, düzeltme kaydıyla kapatılır.

## Senkron

- **Yazma yolu outbox üzerinden** — uygulama asla doğrudan API'ye yazmaz; yerel SQLite + outbox aynı işlemde yazılır, arka plan işçisi gönderir.
- **Her olayda `client_event_id`, sunucuda tenant içi tekil** — retry her zaman güvenlidir, "gönderdim mi acaba" durumu oluşmaz.
- **Okuma yolu tenant başına monoton `sequence` ile delta pull** — ilk kurulumda tam snapshot.
- **Defter kayıtlarında çakışma yoktur, birleşme vardır** — iki cihaz aynı anda yazdıysa iki kayıt da geçerlidir.
- **Varlık alanlarında (ad, adres) son yazan kazanır, eşitlikte `device_id` ile deterministik ayrım** — adres para değildir, kaybı tolere edilebilir.
- **Sunucu her yanıtta kendi saatini döner, istemci offset tutar** — esnafın telefon saati yanlış olabilir, `occurred_at` düzeltilmiş saatle yazılır.

## Abonelik ve kilit

- ~~72 saat sunucuyla konuşulamazsa salt-okunur~~ → **`valid_until` tarihi + 7 gün lütuf süresi** — "N saat sessizlik" kuralı bizim sunucu kesintimizi ödeyen bayinin cezasına çeviriyordu; istemci abonelik bitiş tarihini zaten biliyor, uçak modu açığı tarihle kapanır.
- **`valid_until` gelecekteyken cihaz sunucuyla haftalarca konuşamasa da tam çalışır** — fatura ödenmiştir, kilitlemek için sebep yok.
- **`valid_until` geçmişse ve sunucuya sorulamıyorsa 7 gün lütuf, sonra salt-okunur** — ödeyen bayiyi yanlışlıkla kilitlemenin maliyeti, ödemeyene bir hafta fazladan kullandırmanın maliyetinden kat kat büyük.
- **Cihazda ileri-sadece saat (`elapsedRealtime` + son görülen sunucu zamanı)** — kullanıcı sistem saatini geri alarak lütuf süresini uzatamaz.
- **Kilitliyken outbox akmaya devam eder; sunucu `occurred_at <= locked_at` olanları kabul eder** — kilit yazmayı durdurur, veri kaybettirmez (kırmızı çizgi #5).

## Faz 0 kapanışı

- **Faz 0 kararı: GO (şartlı) — 2026-07-10** — iki gerçek cihazda (Samsung S24 FE/And.16, Xiaomi 14/HyperOS 2) soğuk süreç, kilitli ekran, derin Doze, rehberli numara, giden arama ve sıfır kurulum uçtan uca kanıtlandı; yasaklı izin sıfır.
- **20 aramalık sistematik tur kullanıcı kararıyla iptal; doğrulama pilotun ilk haftasına devredildi** — kapsamlı ad-hoc test mevcut, ölçüm ekranı üründe kalıyor, pilottaki gerçek aramalar sayacı dolduracak.
- **Xiaomi derin Doze'da zil anı kartı ~2,2 sn (tek bütçe aşımı) — kabul edildi** — gecikmenin tamamı sistemin Doze'dan uyanması (sorgu 5 ms); yanıt anındaki kart 419 ms; bayi telefonu cebinden çıkarana kadar süre zaten geçiyor. MIUI'nin SmartPower'ı ölü süreci kendisi diriltiyor (loglandı) — Doze korkusu kapandı.
- **Stok Android gerçek cihazda test edilmedi** — emülatör (API 34) yazılım yolunu doğruladı; pilot Xiaomi/Samsung ağırlıklı, risk düşük, açık kayıt edildi.

## Faz 1

- ~~Yerel geliştirme veritabanı: taşınabilir PostgreSQL zip'i, Docker yok~~ → **Yerelde de Docker (kullanıcı Docker Desktop'ı kendisi kurdu)** — yerel ile üretim aynı Postgres 16 imajını koşar, "bende çalışıyordu" sınıfı fark ortadan kalkar.

## Çalışma disiplini

- **Otomatik commit disiplini Claude Code Stop hook'u ile (`scripts/quality-gate-commit.ps1`)** — anlamlı iş bitince kalite kapısı (flutter analyze+test, pint, phpstan, php test, sır taraması) yeşilse dev'e otomatik commit+push; main/master'a asla; kapı kırmızıysa commit yok; araç kurulu değilse kontrol atlanır ama commit gövdesine "atlanan" olarak yazılır — çalışmamış kontrol başarılı sayılmaz. Hook harness tarafından çalıştırılır, modelin unutmasından etkilenmez.

## Kiracı izolasyonu

- **PostgreSQL RLS + her istekte `SET LOCAL app.tenant_id`** — değişken set edilmemişse sorgu sıfır satır döner; güvenli varsayılan.
- **Yönetim paneli iş verisi tablolarına yalnız SELECT yetkisi olan ayrı DB rolü kullanır** — panelin bayinin siparişini değiştirememesi kod disiplinine değil, veritabanı iznine bağlı.
- **CI'da cross-tenant test matrisi zorunlu** — yeni endpoint bu testi almadıysa build kırılır; kırmızı çizgi #1'in "sürekli kanıtla" şartı bu.

## Arayan tanıma (Faz 0)

- **Yalnız `CallScreeningService` + `RoleManager.ROLE_CALL_SCREENING`** — kırmızı çizgi #6; SMS/Call Log izin grubundan hiçbir izin alınmaz, numara `Call.Details.getHandle()` ile izinsiz gelir.
- **Overlay tamamen native Kotlin, Flutter engine hiç başlatılmaz** — telefon çaldığında süreç çoğu zaman ölüdür; soğuk Flutter engine 1–2 sn alır ve ≤1 sn hedefi daha başlamadan kaybedilir.
- **Native taraf Drift'in SQLite dosyasını doğrudan salt-okunur açar** — ağ yok, kanal yok; sorgu telefon indeksinden tek okumadır.
- **`SYSTEM_ALERT_WINDOW` ile overlay (birincil), heads-up notification (yedek)** — bu izin Play'in kısıtlı SMS/Call Log grubunda değildir, kırmızı çizgi #6'yı ihlal etmez.
- **`applicationId = com.sipario.app`** — mağazada bir daha değişmez, marka ile hizalı.
- ~~**Manifest izin denetimi CI'da merged manifest üzerinde çalışır**~~ → **Manifest izin denetimi İKİ KATMANLI (2026-07-16 düzeltme, audit-redlines bulgusu):** üçüncü parti paketlerin enjekte ettiği izinler ancak BİRLEŞTİRİLMİŞ manifest'te görünür ve onu `check_permissions.sh` build sonrası denetler — bu katman TAM MOBİL CI ile gelecek (yazıldığında CI'a bağlı DEĞİLDİ; iddia repo durumuyla örtüşmüyordu). Kaynak-manifest katmanı build'siz CI'da koşar (`check_permissions_source.sh` + `manifest-lint.yml`). Ayrıntı: "Faz 6 — hesap-silme + denetim" bölümü.
- **`minSdk = 29` (Android 10)** — `CallScreeningService` bu sürümle geldi; arayan tanıma ürünün varlık sebebi olduğundan altındaki sürümü desteklemenin anlamı yok.
- **`respondToCall` tanımadan ÖNCE çağrılır** — sistem yanıtı beklerken çağrıyı tutar; sorgu için beklersek zil gecikir ve kullanıcı çağrıyı kaçırır.
- **Numara eşleşmesi son 10 hane üzerinden (`phone_last10`, indeksli)** — +905321112233 / 05321112233 / 5321112233 aynı numaradır ve son 10 hane ülke içinde tekildir.
- **Ölçüm loglarına telefon numarası veya müşteri adı yazılmaz** — kırmızı çizgi #4; yalnız gecikme, eşleşme var/yok ve kullanılan yol tutulur.
- **Simüle çağrılar go/no-go sayımına girmez** — süreç zaten ayakta olduğu için soğuk başlangıç maliyetini hiç ödemez, sahadaki asıl zorluğu ölçmez.
- **Tek kaçırılan çağrı veya tek geç kart NO-GO sayılır** — bayi telefonu açtığında ekranda müşteri yoksa ürünün vaadi çökmüştür; ortalama değil, en kötü durum yönetilir.
- **İzin denetim scripti pozitif kontrolle doğrulanır** — bilerek enjekte edilen bir ihlali yakalayamayan denetim, yeşil yanan bir yalandır. _(2026-07-16: kaynak-manifest katmanı bu ilkeyle doğrulandı — enjekte edilen `READ_PHONE_STATE`→exit 1; merged katmanın pozitif kontrolü mobil CI ile.)_
- **Arayan kartı ekranın ORTASINA çizilir, üstüne değil** — emülatörde görüldü: üstte sistemin heads-up çağrı bildirimi kartın tam olarak müşteri adı ve borç satırlarını örtüyordu; altta da tam ekran çağrı arayüzünün Cevapla/Reddet düğmeleri var.
- **Emülatör sonucu go/no-go SAYILMAZ** — yazılım yolunu (rol, servis, soğuk başlangıç, sorgu, çizim) kanıtlar; korku #1'in asıl konusu olan OEM pil yönetimi ancak gerçek Xiaomi/Samsung cihazda sınanır.
- **`READ_CONTACTS` izni alınır — rehberi okumak için değil, çağrıyı görebilmek için** — Telecom'un `CallScreeningServiceFilter.startFilterLookup()` kodu `if (contactExists && !hasReadContactsPermission()) { servisi atla }` der; izin olmadan bayinin rehberine kaydettiği müşteri aradığında `onScreenCall` HİÇ çağrılmaz. Samsung SM-S721B / Android 16'da doğrulandı: izinsizken kart çıkmadı, izinle birlikte "contact exists" olan çağrıda da `SCREENING_BOUND` alındı ve kart 12 ms'de çizildi.
- **`READ_CONTACTS` bağımlılığı belgelenmemiş bir davranıştır — sürüm yükseltmelerinde sınanacak** — resmî doküman yalnız "rehberdekiler taranmaz" der, izin istisnasından söz etmez; kod Android 11–16 arası değişmemiş (Android 10'da filtre hiç yok). Faz 6'da her hedef sürüm için regresyon testi zorunlu. İzin manifest'ten düşerse hata sessizdir; `scripts/check_permissions.sh` bunu build'de kırar.
- **Varsayılan telefon uygulaması (`ROLE_DIALER`) olunmayacak** — rehberdeki çağrıları görmenin diğer yolu buydu; tam ekran çağrı arayüzü (cevapla/reddet/tuş takımı/bekletme), Samsung'un arama özelliklerinin kaybı ve kalıcı telefon-katmanı bakım yükü demekti. `READ_CONTACTS` aynı sonucu tek izinle veriyor.
- **`NotificationListenerService` ve `AccessibilityService` yolları reddedildi** — ilki rehberdeki aramada ham numara yerine kişi arama URI'si veriyor ve OEM'e göre değişiyor; ikincisi Play'in erişilebilirlik politikasınca yasak. İkisi de Play'in "kısıtlı veriyi dolaylı yoldan türetme" yasağına yaklaşıyor.
- **Kilit ekranında kart, tam ekran niyetli bildirim + `showWhenLocked` Activity ile gösterilir** — `TYPE_APPLICATION_OVERLAY` penceresi keyguard'ın ALTINDA kalır ve hiçbir pencere bayrağı bunu değiştirmez (`FLAG_SHOW_WHEN_LOCKED` API 27'de kaldırıldı, zaten yalnız Activity pencerelerinde çalışıyordu). Samsung'da doğrulandı: kilitliyken kart görünmüyordu.
- **Ekran kilitliyse tam ekran Activity, kilitsizse overlay** — kilitsizken Activity açmak bayinin o an yaptığı işi bölerdi; kilitliyken overlay hiç görünmüyor. İki yol da aynı `CallerCard` görünümünü kullanır.
- **Arka plandan Activity başlatmanın tek meşru yolu tam ekran niyettir** — Android 10+ arka plan Activity kısıtı `CallScreeningService`'i muaf tutmaz; sistemin gönderdiği `PendingIntent` belgelenmiş muafiyettir.
- **`USE_FULL_SCREEN_INTENT` izni Play riskidir — Faz 6'da "çekirdek işlev" beyanı yapılacak** — Android 14+ bu izni yalnız arama/alarm uygulamalarına otomatik verir ve Play, uymayanlarınkini geri alır. Yan yüklemede sorun çıkmıyor, mağazada çıkabilir. İzin yoksa uygulama ölmez: kilit ekranında bildirime düşer, kurulum sihirbazı kullanıcıdan izni ister.
- **`Measurement.shown` kilitliyken yalnız `fullscreen` yolunu gösterim sayar** — kilitli ekranda overlay çiziliyor, `onDraw` tetikleniyor ve ölçüm "başarılı" kaydediliyordu; metrik kendi başarısızlığını başarı olarak raporluyordu. Sahadan gelen "kart çıkmıyor" bildirimi olmasa fark edilmezdi.
- **GO için en az 5 çağrı ekran kilitliyken sınanmalı** — sahada telefon çoğu zaman cepte ve kilitli; kilitsiz ölçüm ürünün asıl kullanım anını hiç test etmiyor.
- **Kilit ekranında Samsung ve Xiaomi ayrışıyor; iki yol BİRDEN denenir** — Samsung kilitliyken çağrıyı bildirimle gösterir (keyguard örtülmez → tam ekran Activity görünür); MIUI tam ekran çağrı ekranı açar ve Activity'mizin üstüne biner. MIUI çağrı ekranı keyguard'ı örttüğü anda SAW overlay çağrı ekranının üstüne çıkabilir — MIUI'nin "Kilit ekranında göster" izni (`MIUIOP 10020`) tam bunu açar. Kilitli yolda: tam ekran niyet (ölçümü yazar) + sessiz overlay (ölçüm yazmaz, yoksa tek çağrı iki kayıt üretir).
- **Tam ekran niyet GECİKTİRİLEMEZ** — sistem Activity'yi yalnız ekran kapalı/kilitliyken doğrudan açar; 600 ms gecikme denendi, çağrı ekranı ekranı yaktığı için niyet pencereyi kaçırıp sıradan bildirime düştü (Xiaomi'de ölçüldü). Kart hemen açılır, üste çıkma işi sonradan yapılır.
- **Task'lar arası z-order savaşına girilmez** — `REORDER_TO_FRONT` kendi task'ımızda "zaten üstte" dedi ama MIUI InCall task'ının altında kaldık; tekrarlı öne alma çağrı ekranıyla titreşim savaşı demektir. Tek deneme kalır, gerisi overlay+bildirime emanettir.
- **Kilitli ekranda taşıyıcı bildirim silinmez ve `VISIBILITY_PUBLIC` + BigText taşır** — kart çağrı ekranının altında kalırsa müşteri/borç/notu gösteren tek yer o bildirimdir; kartla aynı içeriği taşır.
- **Giden aramalarda da kart gösterilir; go/no-go YALNIZ gelen aramalarla hesaplanır** — bayi müşteriyi geri aradığında da borcu görmek ister; `onScreenCall` giden çağrıları da veriyor (`READ_CONTACTS` sayesinde rehberdekiler dahil). Ölçüm kaydına `dir` alanı eklendi, giden aramalar 20'lik sayımı şişirmez.
- **MIUI'de tam ekran niyet izninin ÖN KOŞULU var: "arka planda açılır pencere"** — MIUI "Tam ekran bildirimleri" anahtarını, kendi "arka planda yeni pencere açma" izni verilmeden açtırmıyor; kurulum sihirbazı Xiaomi'de bu sırayla yönlendirmeli. `OemBatteryGuide` artık MIUI'nin "Diğer izinler" ekranına gider.
- **MIUI kilit ekranında zil sırasında kart gösterilmeye ÇALIŞILMAZ; kart yanıt ve kapanış anlarında yeniden gösterilir** — rakip analizi bunu kesinleştirdi: Play'deki Halı Takip (tüm izinlere ve eski READ_PHONE_STATE yaklaşımına rağmen) bile zil sırasında MIUI çağrı ekranının altında kalıyor; deseni "yanıtta ve kapanışta yeniden tetikle". Bayi siparişi konuşma sırasında alır — kartın asıl lazım olduğu an yanıt anıdır. Zil sırasında bilgiyi zengin bildirim taşır.
- **Yanıt/kapanış tespiti `AudioManager.mode` ile — READ_PHONE_STATE'siz** — mode okumak izin gerektirmez (RINGTONE→çalıyor, IN_CALL→açıldı, NORMAL→bitti); kırmızı çizgi #6 korunur. Süreç konuşma ortasında ölürse izleyici de ölür — en iyi çaba; kart görünürken süreç görünür öncelikte olduğundan risk düşük.
- **Kart dokunulmadan kapanmaz; 120 sn yalnız emniyet süresi** — saha geri bildirimi: 12 sn'lik otomatik kapanma, adres konuşma sırasında lazımken kartı erken kaçırıyordu. Cevapsız çağrıda da kart yeniden gösterilir — bayi kimi kaçırdığını görmeli.
- **Test cihazında rakip arayan-tanıma uygulaması varken alınan sonuçlar geçersizdir** — Xiaomi'deki ilk kilit ekranı denemeleri, aynı anda kendi çağrı ekranını açan ikinci bir uygulamayla (Halı Takip) kirlenmişti; kurulum sihirbazına "benzer uygulama tespiti ve uyarısı" Faz 2'de eklenecek.
- **MIUI izinleri adb/appops ile VERİLEMEZ — kaynak MIUI'nin kendi izin veritabanıdır** — `appops set 10020/10021 allow` yazıldı ve appops "allow" gösterdi, ama MIUI ayar ekranı kapalı gösterip uygulamayı kendi veritabanından denetlemeye devam etti; kart bu yüzden çıkmadı. "Kilit ekranında görüntüle" ve "Arka planda çalışırken yeni pencereler açın" yalnız kullanıcı eliyle, MIUI ayar ekranından açılabilir. Kurulum sihirbazının bu ekranı açıp adım adım tarif etmesi zorunlu; otomatikleştirilemez.
- **Overlay ile yeniden gösterim, keyguard arkasında bekleyen kart Activity'sini kapatır** — kapatmayınca kilit açıldığında üst üste iki kart çıkıyordu (sahada görüldü).
- **Kilitli ekranda keyguard'ı yalnız EN ÜSTTEKİ task örtebilir (AOSP `KeyguardController.mTopOccludesActivity`, tek kazanan)** — alttaki showWhenLocked Activity'ler görünmez; kazanmanın tek yolu çağrı ekranından SONRA en öne geçmek. Kilitli yeniden gösterim bu yüzden overlay değil, Activity yeniden başlatmadır. Overlay kilitli/örtülü keyguard üstünde hiçbir mekanizmayla çizilemez (AOSP + MIUI doğrulandı); MIUI'nin "Kilit ekranında görüntüle" izni Activity'leri yönetir, overlay'leri değil.

## Sürüm yönetimi

- **PR akışı: "PR aç" de, gerisi Claude'da** — gh CLI kuruldu (her iki geliştirici de kurmalı: `winget install GitHub.cli` + `gh auth login`). main'e geçiş istendiğinde Claude dev'deki commit aralığından başlık+tam açıklama yazarak PR'ı açar; merge kararı ve düğmesi insanda. GitHub çok commit'li PR'da açıklamayı otomatik doldurmaz; elle açılan PR'lar için .github/pull_request_template.md iskelet sunar.

- **Stop hook komutu cmd sarmalayicidan bash formuna cevrildi; vardiya senkron kontrolu SessionStart hook'u olarak eklendi** — cmd/IF EXIST tirnak katmanlari hook'u sessizce isleymez kiliyordu (elle test edilerek bulundu); oturum basinda fetch + temiz agacta otomatik ff-pull, gerideyse acik uyari. Kabuk artigi sifir baytlik dosyalar artik kapida filtrelenir.

## Faz 1 — uygulama (coder, 2026-07-11)

- **İki DB rolü: migration `sipario_owner` (imaj superuser), runtime+test `sipario_app` (NOSUPERUSER/NOBYPASSRLS)** — Postgres'te tablo sahibi ve superuser RLS'i atlar; izolasyonu gerçekten kanıtlamak için app/test bağlantısı sahibi-olmayan rol OLMALI. Her tabloda ek `FORCE ROW LEVEL SECURITY` — owner ile yanlışlıkla bağlanılsa da izolasyon çökmesin (kuşak+askı).
- **UUIDv7 trait'i `HasUuids` (architect'in adlandırdığı `HasVersion7Uuids` Laravel 13'te YOK)** — `HasUuids` bu sürümde zaten `Str::uuid7()` üretiyor ve id boş değilse istemcininkini koruyor (offline-first gereği); aynı sonuç, mevcut trait.
- **RLS bağlama noktası middleware (`ResolveTenantContext`), DB connection event DEĞİL** — tenant token satırından çözülüyor ve isteğin transaction ömrünü kontrol etmemiz gerekiyor; istek tek transaction'a sarılır, `set_config('app.tenant_id', ?, true)` yazılır, commit'te otomatik sıfırlanır (kalıcı bağlantıda sızıntı yok).
- **`ResolveTenantContext` middleware önceliği auth:sanctum'dan ÖNCE zorlandı (`prependToPriorityList`)** — Laravel'in öncelik listesi `Authenticate`'i öne alıyordu; tenant bağlamı kurulmadan kullanıcı RLS altında yüklenemeyip 401 dönüyordu (yerel uçtan uca testte yakalandı).
- **Login lookup `SECURITY DEFINER` fonksiyon + sahibi `sipario_auth` (BYPASSRLS); ayrıca `GRANT SELECT ON users,tenants TO sipario_auth`** — tenant bilinmeden email ile kullanıcı bulmanın tek meşru yolu; BYPASSRLS RLS'i atlar ama tablo yetkisi ayrıdır, grant olmadan "permission denied" (yerel testte yakalandı). Nötr 401 ile email enumeration engellenir.
- **`personal_access_tokens`: `uuidMorphs('tokenable')` + `tenant_id` kolonu** — User kimliği UUID olduğundan Sanctum'un varsayılan bigint `morphs`'u token üretiminde patlıyordu (yerel testte yakalandı); `tenant_id` token satırında tutulur ki middleware kullanıcıyı yüklemeden tenant'ı çözebilsin (bu tablo RLS'e tabi değil).
- **Rol modeli: tek `role` sütunu + string PHP enum (`UserRole`), ayrı tablo/paket yok** — yetki `role` middleware ile; `TenantStatus` enum'u login'de trial/active kapısını tutar (diğerleri nötr 403).
- **Email GLOBAL tekil (tenant başına değil)** — login yalnız email+parola alır (mobilde tenant kodu yok), lookup deterministik tek satır dönsün; RLS zaten cross-tenant görünürlüğü engeller.
- **Yönetim paneli salt-okunur DB rolü (`sipario_panel`) Faz 5'e ertelendi** — Faz 1'de yalnız kolon/yapı hazır; rol ve grant'leri para/panel fazında kurulur.
- **Testler ayrı `sipario_test` DB'sinde, gerçek Postgres'e koşar (RLS SQLite'ta yok); phpunit.xml DB_USERNAME=sipario_app** — migration `--database=pgsql_owner` ile, HTTP istekleri app rolüyle. RefreshDatabase yerine owner ile migrate:fresh + TRUNCATE deseni (SET LOCAL doğal sıfırlansın, prod davranışı taklit edilsin).
- **Kök `.gitignore`'daki `.env.*` deseni `.env.example`'ı da yutuyordu; `!**/.env.example` negasyonu eklendi** — Laravel konvansiyonu `.env.example`'ın commit'lenmesini gerektirir (sır içermez, şablondur).
- **Yerel Postgres 16 imajı ICU `tr-TR` collation ile (`--icu-locale=tr-TR`), libc tr_TR.UTF-8 üretilmeden** — Türkçe sıralama/case; docker init betikleri (`10-roles.sh`, `20-test-db.sh`) rolleri ve test DB'sini yalnız ilk initdb'de kurar, CI'da aynı SQL elle koşulur.
- **Docker Postgres host portu 55432 (5432 değil)** — geliştirici makinelerinde Laragon'un yerli PostgreSQL'i 5432'yi tutuyor; standart portta uygulama sessizce yanlış sunucuya bağlanıyordu (sahada yakalandı), standart-dışı port bu çakışma sınıfını bütün makinelerde kökten kapatır. Konteyner içi port 5432 kalır; CI service container'ı etkilenmez.
- **Yan dal/worktree açılmaz; iş doğrudan dev'de yapılır, main'e yalnız dev→main PR ile gidilir (kullanıcı kararı, 2026-07-11)** — Faz 1 izole worktree+ara dalda yürüdü ve dosyalar yerelde "kayboldu" (kafa karışıklığı); akış tek hat: yerel çalışma = dev, dev→main PR, merge kararı insanda. `.claude/settings.json`'a `worktree.bgIsolation=none` eklendi ki arka plan oturumları da doğrudan dev'de çalışabilsin.

## Faz 1 — güvenlik denetimi (2026-07-13)

- **Login + korumalı API hız sınırı (`throttle`), limitler `AppServiceProvider`'da** — F1: zamanlama yan-kanalı kapalıydı ama deneme SAYISI sınırsızdı (kaba kuvvet/credential-stuffing). Login: aynı e-posta+IP'ye 5/dk + tek IP'den 20/dk (yayılı numaralandırma); korumalı grup: kullanıcı/IP başına 60/dk (çalınan token istismarı + DoS). Aşımda 429.
- **429 (throttle) yanıtı `server_time` taşımaz — bilinçli kapsam sınırı** — throttle exception olarak render edilir, `AppendServerTime`'ın post-işlemesini atlar (auth 401 exception'ları da öyle); `server_time` yalnız denetleyiciden dönen normal JsonResponse'larda garanti. İstemci offset'i başarılı yanıtlardan gelir, 429'da gerekmez. Genişletmek Faz 2+ işi.
- **Güvenlik başlıkları middleware'i (`SecurityHeaders`) tüm api yanıtlarında** — F3: `X-Content-Type-Options=nosniff`, `X-Frame-Options=DENY`, `Referrer-Policy=no-referrer`, `X-Permitted-Cross-Domain-Policies=none`. HSTS burada DEĞİL — TLS ters vekilde sonlanır, HSTS orada verilir (yanlış http ortamda istemci kilitlenmesin).
- **`config/cors.php` yayımlandı: yüzey `api/*`, origin env-driven, credentials kapalı** — F4: Laravel varsayılanı joker `*`; mobil bearer CORS uygulamaz ama Faz 5 tarayıcı paneli için joker risk. İzinli origin'ler `CORS_ALLOWED_ORIGINS` env'inden (tanımsızsa boş → hiçbir tarayıcı origin'i, mobil etkilenmez); bearer token çerez istemediği için `supports_credentials=false`.
- **Sanctum `token_prefix` varsayılanı `sipario_`** — F2: sızan token'ları depo/GitHub secret-scanning yakalayabilsin. Token doğrulaması öneki yok sayar, üretimde henüz token yok (geriye dönük risk sıfır). Token süresizliği (`expiration=null`) offline-first gereği bilinçli KORUNDU — saha cihazı sunucuya günlerce ulaşamadan tam çalışmalı; kilit `valid_until` ile (Faz 5), token süresiyle değil.
- **larastan/phpstan seviye 6 kuruldu (`phpstan.neon`), kalite kapısındaki "atlanan statik analiz" boşluğu kapandı (F6)** — 4 gerçek tip bulgusu KÖK NEDENDEN düzeltildi (baskılama yok): modele `casts()`-türevli `@property` blokları (larastan enum/Carbon tiplerini kolon şemasından çıkaramıyordu), enum erişimi `$x->value` (savunmacı `instanceof BackedEnum` ölü daldı), `CreateTenant`'ta nullable `trial_ends_at` için `?->`.
- **Doğrulama: pint + phpstan(sıfır hata, seviye 6) + phpunit 37/37(105 assert) + composer audit(CVE yok) yeşil** — 3 yeni güvenlik testi (`SecurityHardeningTest`: 429 rate-limit, güvenlik başlıkları, izinsiz origin CORS reddi) eklendi; mevcut 34 izolasyon/auth testi bozulmadı.

## Faz 2 — mimari (2026-07-13)

- **İstemci sunucuya ASLA doğrudan REST CRUD yapmaz; tek yazma yüzeyi `POST /sync/push`, tek okuma yüzeyi `GET /sync/pull`** — yazma yolu outbox üzerinden (DECISIONS "Senkron"); "müşteri/sipariş CRUD" istemcide yereldir (Drift) ve outbox olayı üretir, sunucu yalnız push/pull görür.
- **Delta pull kaynağı merkezî append-only `sync_changes` günlüğü (tenant başına monoton `seq`), canlı tablolarda per-row seq YOK** — tek sıralı akış paging/cursor'ı önemsizleştirir, silme bir tombstone satırdır, para tabloları transport günlüğünden mantıken ayrık kalır.
- **Tenant başına `seq`, push transaction'ında `tenant_sync_state` satırı `SELECT ... FOR UPDATE` ile tahsis edilir** — satır kilidi commit'e kadar tutulduğundan seq atama sırası = commit sırası; CDC "sıra boşluğu / kayıp güncelleme" sınıfı kökten kapanır (korku #2).
- **Idempotency `processed_events(tenant_id, client_event_id)` UNIQUE ile; tekrar gönderim duplicate olarak AYNI sonucu döner** — "gönderdim mi acaba" durumu yok (DECISIONS "Senkron"); ağ zaman aşımı sonrası retry çift-uygulama yapmaz.
- **İlk kurulum snapshot'ı canlı tablolardan REPEATABLE READ transaction'da okunur, cursor = o an görülen `last_seq`; sonraki pull'lar `sync_changes WHERE seq > cursor`** — snapshot temel çizgiyi kurar, delta üstüne biner; snapshot sırasındaki yazımlar seq>cursor ile bir sonraki delta'da gelir (kayıp yok).
- **Varlık alanları LWW: her satırda `updated_occurred_at` + `updated_device_id`; gelen olay ancak `occurred_at` daha yeniyse (eşitlikte `device_id` büyükse) uygulanır, değilse ack'lenip yok sayılır** — DECISIONS "son yazan kazanır + device_id eşitlik kırıcı"nın sunucu tarafı; reddedilen olay da ack alır ki istemci retry'ı durdursun.
- **`order_events` Faz 2'de KULLANILIR (sipariş baştan olay-kaynaklı, `orders.status/total_kurus` önbellek); `ledger_entries` şeması kurulur ama YAZIMI Faz 3'te** — team-lead kararı "outbox baştan doğru olsun"; sipariş durumu olaylardan türer, defter iş akışı sonraki faz.
- **Tüm yeni tablolar Faz 1 desenini birebir izler: `tenant_id` + ENABLE/FORCE RLS + `NULLIF(current_setting('app.tenant_id',true),'')::uuid` politikası, ana tabloda `unique(tenant_id,id)`, çocukta `(tenant_id,parent_id)` bileşik FK** — izolasyon join'siz ve veritabanı seviyesinde; cross-tenant referans imkânsız.
- **Push/pull uçları cross-tenant test matrisine eklenir (RouteCoverageGuard aksi halde build'i kırar); silme fiziksel değil tombstone (`deleted_at` + `sync_change op=delete`)** — kırmızı çizgi #1 "sürekli kanıtla"; veri silinmez (kırmızı çizgi #2/#5), tombstone senkronla yayılır.
- **Drift `sipario.db` dosya adını, `customers`/`customer_phones` tablo adlarını, `customers.balance_kurus` ve `phone_last10` indeksini Faz 0 sözleşmesi olarak KORUR; native salt-okunur okur** — arayan tanımanın 1 sn bütçesi bu indekse ve önbelleğe bağlı; Drift journal modu native salt-okunur açıcıyla uyumlu doğrulanmalı (WAL riski açık).

## Faz 2 — uygulama (coder, 2026-07-13)

- **Push'ta her olay ayrı savepoint'te (`DB::transaction` iç içe) uygulanır; istemci-kaynaklı hata (InvalidArgument veya SQLSTATE 22P02/23502/23503/23505/23514) o olayı `rejected` yapar, parti bozulmaz** — Postgres'te hatalı statement transaction'ı zehirler; savepoint tek olayı geri alıp partiyi kurtarır. Referanslar (customer_id vb.) yazımdan ÖNCE RLS-kapsamlı SELECT ile doğrulanır ki FK ihlali hiç oluşmasın (zehirlenme önlenir).
- **`ChangeApplier` (domain mutasyonu + LWW/append) ile `SyncService` (seq/idempotency/pull) ayrı sınıflar** — 500 satır sınırı + tek sorumluluk; ChangeApplier seq bilmez, SyncService domain bilmez.
- **`balance_kurus` istemciden YAZILAMAZ; ledger olayında sunucu `SUM(amount_kurus)`'u defterden yeniden kurar** — DECISIONS "önbellek bozulursa defterden kurulur"; istemci upsert payload'ından balance düşürülür.
- **Her çocuk tabloda composite `(tenant_id,parent_id)` FK'ye EK olarak doğrudan `tenant_id→tenants` cascade FK** — nullable parent'lı `orders`/`ledger_entries` (müşterisiz sipariş/kayıt) tenant silinince composite FK MATCH SIMPLE ile atlanıp yetim kalırdı; doğrudan FK cascade'i garanti eder.
- **`tenant_sync_state` satırı `Provisioning`'de kurulur; push ayrıca `ON CONFLICT DO NOTHING` ile self-heal yapar** — factory ile açılan tenant'lar (testler) satırı almaz, self-heal onları da kapsar (ve yolu test eder).
- **İstemci Drift TEK KİRACIDIR: tablolar sunucu aynası MİNUS `tenant_id`** — cihazda tek bayi oturur; izolasyon sunucuda RLS, istemcide oturum. Faz 0 `sipario.db` + `customers`/`customer_phones`/`phone_last10` sözleşmesi korunur, `address` kolonu `customer_addresses`'e normalize edildi (native adres gerekiyorsa ayrı sorgu — 1 sn hot-path'te değil).
- **Drift `schemaVersion=2`: v1→v2 ADDİTİF migration (architect kabul kriteri) — `customers`/`customer_phones` DROP EDİLMEZ, yalnız `updated_occurred_at`/`updated_device_id`/`deleted_at` ALTER ADD COLUMN + yeni tablolar CREATE** — native sözleşme (tablo/kolon adları, `phone_last10` indeksi, `balance_kurus`) ve mevcut veri korunur; Faz 0'ın `customers.address` kolonu orphan/nullable kalır (adres artık `customer_addresses`'e yazılır). NOT NULL `updated_occurred_at` mevcut satırlara `1970` varsayılanıyla eklenir → sunucu güncellemesi LWW'de kazanır. `PRAGMA journal_mode=TRUNCATE` (WAL değil) native salt-okunur açıcı için (gerçek cihazda doğrulanacak — kabul kriteri).
- **Drift codegen (Dart 3.10) friction ÇÖZÜLDÜ: `sqlite3>=3.3` ve `objective_c` native build-hook'ları `dart run build_runner`ın AOT derlemesini kırıyor** — `path_provider` kaldırıldı (DB yolu için `sqflite getDatabasesPath` yeterli, objective_c gitti); üretilmiş `app_database.g.dart` COMMIT'lenir; yeniden üretim gerekirse pubspec'teki kapalı `sqlite3 <3.3` override'ı geçici açılır (yönerge pubspec'te). Runtime/`flutter test` sqlite3 3.4 ister (drift 2.34 API'si) — override kapalı normaldir.
- **`order_events` id'sini SUNUCU üretir, istemci `client_event_id` ile dedup eder; `ledger_entries` id'sini istemci üretir, id ile dedup** — append tablolarında "yoksa ekle"; istemci kendi lokal olayını sunucudan geri gelince ikizlemez.
- **İstemci çakışma: pull bir varlık değişikliği getirdiğinde o varlık için daha yeni `pending` outbox düzenlemesi varsa YEREL korunur** — gönderilmemiş yerel düzenleme sunucuda kazanacağından üzerine yazılmaz; append tabloları bu kuraldan muaf.
- **larastan/phpstan bu checkout'ta vendor'da YOKTU, `composer install` ile kuruldu (lock'ta vardı); kalite kapısı seviye 6'da 0 hata** — Faz 1 F6 kararı gereği statik analiz koşulmalı.
- **Cross-tenant referans doğrulaması TÜM yabancı id'lerde simetrik: `order_lines.product_id` ve `ledger_entries.related_order_id` de yazımdan önce RLS-kapsamlı `exists()` ile doğrulanır (tester bulgusu)** — customer_id doğrulanıyordu ama bu ikisi doğrulanmadan yazılıyordu; veri henüz sızmıyordu ama Faz 3 raporları bu id'leri join'lerse gerçek sızıntı olurdu. Başka bayinin ürün/siparişine referans artık InvalidArgument + savepoint ile reddedilir (kırmızı çizgi #1).
- **Append-only defter/olay değişmezliği VERİTABANI SEVİYESİNDE zorlanır: `sipario_app`'ten `ledger_entries`, `order_events`, `sync_changes`, `processed_events` üzerinde UPDATE/DELETE REVOKE edilir (migration 000211, reviewer bulgusu)** — kırmızı çizgi #2 "silinmez/ezilmez" koda değil DB iznine bağlanır (FORCE RLS felsefesiyle simetrik savunma-derinliği); bir kod hatası bile geçmişi ezemez. Uygulama bu tablolara yalnız INSERT eder; `sipario_owner` (superuser) revoke'tan muaf (bakım açık). `tenant_sync_state` DAHİL DEĞİL (seq push'ta UPDATE edilir). `AppendOnlyLedgerTest` app rolüyle UPDATE/DELETE denemesini 42501 ile kanıtlar.
- **`ChangeApplier` 500 satır sınırını aştığı için üçe bölündü (reviewer): `ChangeApplier` (dispatch + basit varlık LWW + defter), `OrderChangeApplier` (sipariş olay mantığı), `SyncPayload` (ortak `change()`/`req()` saf yardımcıları)** — davranış aynı, 66/66 test yeşil kaldı.
- **Claude Code hook komutları BASH biçiminde yazılır (`f="$CLAUDE_PROJECT_DIR/..."; if [ -f "$f" ]; then node "$f" <komut>; fi`), ASLA `cmd /c` ile değil** — hook'lar bu makinede Git Bash üzerinden çalışıyor; MSYS yol dönüşümü `/c` argümanını `C:/`'ye çevirip etkileşimli cmd açıyor, hook sessizce hiç çalışmıyor (belirti: hook çıktısında "Microsoft Windows [Version..." banner'ı + ham JSON). Ayrıca hook `timeout` alanı SANİYE cinsindendir, milisaniye değil.

## Faz 3 — mimari (2026-07-14)

- **Para modeli çift-satır: her SATIŞ bir `debit` (+borç), her TAHSİLAT bir `payment` (−borç); peşin/kart/havale satış ikisini birden üretir (net borç 0, para kasada), veresiye yalnız `debit`, sonradan tahsilat yalnız `payment`** — `customers.balance_kurus = SUM(amount_kurus)` net borcu doğru verir (debit+, payment−), kasa = `payment` toplamı, ciro = `debit` toplamı; tek toplama üç raporu birden besler, bakiye önbelleği filtresiz kalır.
- **`amount_kurus` işareti istemcide konvansiyona göre imzalı gelir, sunucu tiple tutarlılığı doğrular (`debit ≥ 0`, `payment ≤ 0`, `credit ≤ 0`, `correction` serbest); uymayan olay `rejected`** — istemci işaret hatasını sunucu yakalar, ama korrektif ters kayıtların serbestliği korunur (DB CHECK yerine app doğrulaması, esneklik için).
- **Kupon PARA değil ADET; ayrı append-only `coupon_movements` tablosu (qty_delta imzalı) + türetilen `coupon_balances` önbelleği (customer+product bazında adet)** — "para her yerde kuruş int" felsefesi bozulmaz (adet≠kuruş), kupon paranın parası zaten normal `debit`+`payment` ile deftere düşer; kupon bakiyesi `SUM(qty_delta)`'dan kurulur, `customers.balance_kurus` deseninin birebir ikizi.
- **Kupon satın alma parası = normal peşin satış (`debit`+`payment`, ödeme tipiyle); kupon KULLANIMI teslimde para hareketi ÜRETMEZ (peşin ödendi), yalnız `coupon_movements` −qty + `orders.payment_type='kupon'`** — çifte sayım olmaz; teslim edilen mal "bedava" değil, bedeli satışta tahsil edildi.
- **`ledger_entries.entry_type` CHECK'i para-set'ine daraltılır: `('debit','credit','payment','correction')`; `coupon_grant`/`coupon_use` kupon tablosuna taşındığı için düşülür (Faz 2 hiç ledger iş satırı yazmadı, kayıp yok)** — kullanılmayan enum değeri istemcinin çifte-satır kuralını atlayacağı bir doğrulama deliğiydi; kupon adet olarak ayrıştı, para enum'u temizlendi.
- **`ledger_entries`'e `payment_type` (nakit|kart|havale, nullable) + `reverses_entry_id` (nullable, self-FK) eklenir** — kasa özeti "ödeme tipine göre" için `payment_type` ZORUNLU (join'siz gruplama); `reverses_entry_id` ters kaydı düzelttiği satıra bağlar ("eksik para kanıt olarak görünür kalmalı" — BRIEF).
- **`orders.payment_type` CHECK'ine `kupon` eklenir** — kuponla ödenen teslimatın ödeme tipi nakit/kart/havale/veresiye'nin hiçbiri değil (peşin ödenmiş teslimat), 0 para hareketi + 1 kupon kullanımı üretir.
- **Defter iş akışları İSTEMCİDE (Drift repo), sunucu ChangeApplier yalnız doğrular+append eder; ledger/coupon id'sini istemci üretir, id ile dedup** — Faz 2 deseni ("iş akışları istemcide yereldir, sunucu ChangeApplier ile uygular" + "ledger id istemci üretir"); teslimat = tek yerel transaction'da order_event + ledger(ler) + kupon hareketi, her biri ayrı outbox olayı (ayrı client_event_id).
- **Yeni outbox/entity_type: `coupon` (hareket); `ledger` genişler (payment_type, reverses_entry_id). `CouponChangeApplier` ayrı sınıf (OrderChangeApplier simetriği, 500 satır sınırı)** — ChangeApplier 270 satır, kupon mantığı ayrı sınıfa; ChangeApplier yalnız dispatch eder.
- **Gün sonu Faz 3'te SALT-OKUNUR read-model (istemcide yerel Drift sorgusu): kasa özeti (ödeme tipine göre `payment` toplamı) + borç durumu (`SUM(balance_kurus>0)` + borçlu listesi) + kupon durumu (eksi bakiyeli müşteriler); YENİ kalıcı durum ÜRETMEZ** — istemcinin tek yüzeyi push/pull, gün sonu tüm veriyi zaten yerelde tutar; kurye kasa DEVRİ (kalıcı mutabakat kaydı) ile atama Faz 4 sınırıdır.
- **Gün sınırı sabit +03:00 (Türkiye, 2016'dan beri DST yok); günlük gruplama `occurred_at + 3s` tarih parçasıyla** — esnafın "günü" yerel takvim günü, occurred_at düzeltilmiş sunucu saati; sabit offset DST karmaşasını kökten kapatır.
- **Negatif kupon bakiyesi sunucuda KABUL edilir (reddedilmez); iki cihaz son kuponu offline harcarsa iki `use` de geçerli, bakiye −1, UI kırmızı, düzeltme `correction` hareketiyle** — teslim edilmiş mal gerçektir, sistem reddedemez (DECISIONS "kupon bakiyesi eksiye düşebilir"); append-only, düzeltme yalnız ters kayıtla.
- **`coupon_movements` append-only DB seviyesinde kilitli (sipario_app'ten UPDATE/DELETE REVOKE, ledger_entries deseni); `coupon_balances` önbellek olduğundan revoke DIŞINDA (customers.balance_kurus gibi UPDATE'lenir)** — kırmızı çizgi #2 koda değil DB iznine bağlı; cache tazelenebilir kalmalı.
- **Yeni tablolar Faz 1/2 RLS desenini birebir izler: `tenant_id` + ENABLE/FORCE RLS + `NULLIF(current_setting('app.tenant_id',true),'')::uuid` politikası, ana tabloda `unique(tenant_id,id)`, bileşik `(tenant_id,customer_id)`/`(tenant_id,product_id)` FK + doğrudan `tenant_id→tenants` cascade** — izolasyon join'siz, cross-tenant referans imkânsız; push/pull uçları zaten cross-tenant matriste (RouteCoverageGuard).
- **Kasa (gün sonu) = `payment_type IS NOT NULL` taşıyan TÜM ledger kayıtlarının `−amount_kurus` toplamı, ödeme tipine göre gruplu (payment + payment_type'lı correction); `payment_type` `payment` VE `correction`'da kabul, `debit`/`credit`'te yasak (tester B bulgusu, 2026-07-14)** — yanlış nakit tahsilat ters kayıtla düzeltilince bakiye zaten düzeliyordu ama kasa nakit rakamı fazla kalıyordu; correction ters çevirdiği payment'ın `payment_type`'ını kopyalar → bakiye VE kasa telafi kaydıyla birlikte düzelir. Enum saymak yerine "payment_type taşıyan kayıt kasaya dokundu" değişmezi: ileride till'e dokunan yeni tip çıksa kod değişmeden doğru kalır. BRIEF "kasa kuruşuna kuruşuna" + "para telafi kaydıyla düzeltilir, eksik para kanıt kalmalı"; kupon tarafında analog boşluk yok (bakiye SUM'dan türer). İptal-sonrası para OTOMATİK ters dönmez (tester A): iptal yalnız `status`, para düzeltmesi elle correction — append-only ile tutarlı, iade/alacak iş kararı.

## Faz 3 — uygulama (coder, 2026-07-14)

- **`ledger_entries`/`coupon_movements` bileşik self-FK (reverses) closure DIŞINDA `DB::statement` ile eklenir; `ledger_entries`'e `unique(tenant_id, id)` EKLENDİ** — self-referencing FK'nin hedefi olan `unique(tenant_id,id)` tablo kurulmadan var olamaz; aynı Schema closure'ında FK unique'ten önce koşup SQLSTATE 42830 verir (yerelde yakalandı). Faz 2 `ledger_entries` yalnız PK(id)+unique(tenant_id,client_event_id) taşıyordu, bileşik referans için (tenant_id,id) unique'i şart.
- **`coupon_balances`'a surrogate uuid `id` PK eklendi (iş anahtarı yine `(tenant_id,customer_id,product_id)` UNIQUE NULLS NOT DISTINCT)** — `sync_changes.entity_id` NOT NULL uuid; tekil id'siz bir önbellek satırı değişiklik akışına giremezdi. id yalnız transport içindir, upsert bileşik anahtarla yapılır (id çakışmada korunur, kararlı); istemci tarafı `id`'yi yok sayıp `(customerId, productId)` ile anahtarlar (sentinel `''` = genel kupon, Drift PK NULL sorununu kapatır).
- **Kupon `product_id` NULL (genel kupon) NULL-güvenli işlenir: sunucu `whereNull`/`where` ayrımı (`= NULL` hep false), Postgres `UNIQUE NULLS NOT DISTINCT`, istemci SENTINEL `''`** — genel kuponun tek bakiye satırı kalması için üç katmanda tutarlı NULL stratejisi.
- **İşaret doğrulaması sunucu ChangeApplier'da (DB CHECK değil): defter `debit≥0`/`payment,credit≤0`/`correction serbest`, kupon `grant≥0`/`use≤0`/`correction serbest`; `payment_type` yalnız `payment`'ta ve nakit|kart|havale** — istemci işaret hatasını sunucu yakalar (`rejected`), ama korrektif ters kayıtların serbestliği korunur (esneklik için app doğrulaması, çift-satır kuralını atlayan enum deliği kapanır).
- **`CouponChangeApplier` ayrı sınıf (OrderChangeApplier simetriği); `ChangeApplier::applyLedger` genişledi; defter/kupon düşük seviye yazımları `ChangeApplier` içinde kaldı (bakiye recompute FİLTRESİZ `SUM` — tüm entry_type imzalı borç-deltası)** — ChangeApplier 337 satır (500 sınırı altında), kupon mantığı ayrı; `SyncService` snapshot'ına `coupon_movement`/`coupon_balance` eklendi, delta zaten `sync_changes` üzerinden akıyor.
- **`SyncPushRequest` beyaz listesine `coupon` entity_type + `grant`/`use`/`correction` op eklendi** — eksikti, kupon push'u 422 dönüyordu (yerel testte yakalandı: `results.0.status` null). API sınırında girdi doğrulaması kuralı.
- **İstemci defter/kupon düşük seviye yazımları `lib/repo/ledger_ops.dart`'ta TRANSACTION AÇMAYAN saf fonksiyonlar (`writeLedgerEntry`/`writeCouponMovement`/`recompute*`); repo'lar bunları `db.transaction` ile sarar** — teslimat gibi çoklu-yazımlı akış (order_event + ledger debit+payment / coupon use) tek atomik birim olur (Faz 2 outbox atomikliği); DRY, her repo kendi transaction'ını yönetir.
- **`OrderRepository.deliver` imzası `{required String paymentType, int? couponQty}` oldu; veresiye→debit, nakit/kart/havale→debit+payment, kupon→coupon use(−qty, satır adedinden türer)+para YOK** — teslimat parayı/kuponu deftere aynı transaction'da düşürür; kupon teslimatı müşterisizse `ArgumentError` (kupon müşteriye ait).
- **Codegen sqlite3 override sınırı `<3.0.0`'a düzeltildi (eski `<3.3` YANLIŞ: 3.2.0 sonradan `hook/build.dart` kazandı); hook'suz son sürüm 2.9.4** — Faz 2 notu güncel pub.dev'de kırılıyordu (build_runner "dart compile does not support build hooks"); `.g.dart` 2.9.4 ile üretildi, runtime sqlite3 3.4.0'a geri alındı, pubspec notu düzeltildi.
- **Gün sonu `DayEndRepository` salt-okuma (yeni kalıcı satır YOK): kasa özeti (`payment` toplamı ödeme tipine göre, +03:00 sabit gün sınırı), borç durumu (`balance_kurus>0` müşteriler), kupon durumu (eksi bakiyeliler + gün içi grant/use)** — gün sınırı `occurred_at`(UTC ISO)+3s'nin takvim günü; DST yok (TR 2016'dan beri), sabit offset karmaşayı kapatır.
- **Kasa telafisi (tester B) — UYGULAMA DETAYI; karar+gerekçe "Faz 3 — mimari" bölümünde:** `ChangeApplier::validateLedgerEntry` payment_type'ı `payment` VE `correction`'da kabul eder (debit/credit'te yasak); `DayEndRepository::kasaOzeti` `payment_type IS NOT NULL` sorgusuna geçti (entry_type saymaz, invariant); `LedgerRepository::duzeltme` ters çevirdiği kaydın payment_type'ını kopyalar (payment→kasa+bakiye, payment_type'sız→yalnız bakiye). `cancel` para akışına dokunmaz (tester A) — mevcut deliver/cancel davranışı zaten böyle, kod değişmedi.

## Faz 3 — inceleme (reviewer, 2026-07-14)

- **İnceleme sonucu: YEŞİL — kırmızı çizgi ihlali YOK; Faz 3 defter çekirdeği kapanışa uygun** — beş kırmızı çizgi kod üzerinden tek tek doğrulandı; bağımsız doğrulama bu makinede yeşil koştu (API pint + phpstan sv6 0 hata + phpunit 83/83·310 assert; mobil flutter analyze 0 sorun + test 52/52 — tester'ın B re-test turu +2 test dahil, son sayılar). Kaynak kod 16:50'de dondu; doğrulama son hal üzerinde.
- **Kiracı izolasyonu (kırmızı çizgi #1) doğrulandı:** `coupon_movements`/`coupon_balances` ENABLE+FORCE RLS + güvenli-varsayılan politika (Faz 1/2 deseni birebir); bileşik `(tenant_id,reverses_movement_id)`/`(tenant_id,reverses_entry_id)` self-FK cross-tenant ters kaydı imkânsız kılar; TÜM yeni yabancı id'ler (customer/product/order/reverses) yazımdan önce RLS-kapsamlı `exists()` ile SİMETRİK doğrulanır (Faz 2'de eksik olan related_order_id/reverses simetrisi burada baştan doğru). `TenantIsolationTest` +3 kupon/ters referans reddini kanıtlar.
- **Append-only (kırmızı çizgi #2) doğrulandı:** `coupon_movements` DB seviyesinde kilitli (`sipario_app`'ten UPDATE/DELETE REVOKE, migration 305 — ledger_entries 211 deseniyle simetrik); düzeltme yalnız ters hareketle; `coupon_balances` bilinçli önbellek olarak revoke DIŞINDA. `AppendOnlyLedgerTest` coupon_movements 42501'i + coupon_balances yazılabilirliğini pozitif+negatif kilitler.
- **Offline-first (kırmızı çizgi #3) doğrulandı:** teslimat/kupon satışı çoklu-yazım (order_event + ledger debit+payment / coupon use) TEK `db.transaction`'da atomik (outbox dahil); son yazım patlarsa üçü de geri döner; hiçbir akış çevrimdışıyken bloklanmaz; kupon eksi bakiye REDDEDİLMEZ (use daima uygulanır).
- **KVKK (kırmızı çizgi #4) doğrulandı:** kod tabanında sıfır `Log::`/`print`/`debugPrint`; reddetme mesajları yalnız ALAN ADI taşır (ör. "customer_id bu bayide bulunamadı"), müşteri adı/telefon/adres DEĞERİ hiçbir hata yoluna yazılmaz.
- **Para (int kuruş) doğrulandı:** her yerde imzalı int kuruş (float yok); kupon PARA değil ADET (imzalı int qty_delta), paranın parası normal debit+payment ile deftere düşer.
- **Kalite doğrulandı:** tüm değişen/yeni kaynak dosyalar 500 satır altında (en büyük ChangeApplier 316); CouponChangeApplier ayrı sınıf (OrderChangeApplier simetriği); ölü kod yok; dosya organizasyonu korunur.
- **Tester gözlem B (ödeme düzeltmesi kasayı düzeltemiyor) inceleme SIRASINDA coder+architect tarafından kök nedenden kapatıldı, reviewer'ca doğrulandı:** correction artık payment_type taşır, `kasaOzeti` `payment_type IS NOT NULL` toplar, `LedgerRepository::duzeltme` ters çevrilen kaydın payment_type'ını kopyalar — kod (validateLedgerEntry / kasaOzeti / duzeltme) ile belge (satır 174-203) tutarlı; +2 sunucu +1 mobil test yeşil. Tester gözlem A (iptal parayı otomatik ters çevirmez) mevcut davranışın append-only ile bilinçli tutarlılığı olarak belgelendi; kod değişmedi, kabul.
- **Bilinen açık (BLOKLAMAZ — Faz 4'e devir):** iki cihaz aynı siparişi offline teslim ederse iki bağımsız ledger seti (çift debit/payment) üretir — append/birleşme deseninin doğal sonucu (kupon çifte-harcamayla simetrik, BRIEF kabul); net etki veresiyede çift borç, düzeltme ters kayıtla kapanır. Kalıcı kasa mutabakatı + sipariş-düzeyi teslim idempotensi (çift-dokunma koruması) Faz 4 (kurye kasa devri) kapsamında ele alınmalı.
- **Bilinen sınır (BLOKLAMAZ — savunma-derinliği, ileride architect değerlendirir):** `correction`'ın taşıdığı `payment_type`'ın ters çevirdiği kaydınkiyle EŞLEŞMESİ sunucuda zorlanmıyor — istemci (`LedgerRepository::duzeltme`) doğru tipi kopyalıyor, sunucu yalnız değerin geçerli (nakit|kart|havale) olduğuna bakıyor. Tek kiracı kendi cihazından yazdığı için cross-tenant/append-only/para-tipi kırmızı çizgisi ihlali YOK; ama bozuk/kötü niyetli istemci yanlış göze (ör. kart yerine nakit) düzeltme yazarsa kasa mutabakatı o gözde bozulur. Sunucuda `reverses_entry_id`'nin payment_type'ıyla eşleşme kontrolü ileride savunma-derinliği olarak eklenebilir (dondurulmuş ağaç bunun için açılmadı; tester+reviewer mutabık). (tester notu + reviewer, 2026-07-14)

## Faz 4 — mimari (architect/lead, 2026-07-15)

- **Sipariş ATAMA olay-kaynaklı: `orders.assigned_user_id` (nullable) bir ÖNBELLEK sütunudur, kaynak `assigned`/`unassigned` order_events'idir (status/total ile birebir desen)** — atama/geri-alma birer order olayıdır, `orders.assigned_user_id` en son olaydan `recomputeOrder` ile türetilir. Tek kişilik bayide atama UI'da HİÇ görünmez (BRIEF "tek kişilik işletmede kuryeye ata adımı görünmez"); sunucu her zaman destekler. `assigned_user_id` yazımdan ÖNCE RLS-kapsamlı `User::exists()` ile doğrulanır (customer/product referans deseni birebir); başka bayinin kullanıcısına atama InvalidArgument + savepoint ile reddedilir (kırmızı çizgi #1). Doğrudan `tenant_id→tenants` FK + referans doğrulaması; users'a bileşik unique gerektiren sert FK EKLENMEZ (referans-doğrulama deseni yeterli, migration yüzeyi küçük kalır).
- **TESLİM İDEMPOTENSİ (Faz 3 açığı KAPANIR) — deterministik uuid5 `client_event_id` ile, yeni sunucu kavramı YOK:** teslimden türeyen TÜM olaylar (delivered order event + ledger debit + ledger payment + coupon use) `client_event_id`'lerini sipariş id'sinden deterministik **uuid5** ile üretir (sabit namespace + `"deliver:{orderId}"`, `"deliver-debit:{orderId}"`, `"deliver-payment:{orderId}"`, `"deliver-coupon:{orderId}"`). İki cihaz aynı siparişi offline teslim edince AYNI uuid5'ler üretilir → sunucu `processed_events(tenant_id, client_event_id)` UNIQUE bunları `duplicate` olarak tekilleştirir (ChangeApplier'a hiç ulaşmaz) → sonuçta TAM tek delivered event + tek defter seti + tek kupon hareketi. Faz 2 idempotency makinesi aynen kullanılır; sunucu DEĞİŞMEZ; hiçbir şey REDDEDİLMEZ (append/birleşme korunur, çift-dokunma inşa gereği imkânsız). `SyncPushRequest` `client_event_id`'yi `uuid` doğruladığından uuid5 zorunlu (rastgele string kabul edilmez). Türetilen ledger/coupon `id`'leri de aynı şemayla deterministik (kemer+askı: id-dedup yolu tutulsa da tek satır). Teslim DIŞI defter kayıtları (elle tahsilat, correction) rastgele id/client_event_id'de kalır — teslim tek mantıksal olgu olduğu için yalnız ona özgü.
- **Sabit uuid5 namespace istemci+sunucuda ortak sabit** — istemci Dart `uuid` paketi `Uuid().v5(namespace, name)`, aynı namespace+name aynı uuid'i verir; sunucunun uuid5 hesaplamasına GEREK YOK (yalnız gelen client_event_id ile dedup eder). Namespace bir kez seçilir, mağazada değişmez.
- **KASA DEVRİ kalıcı append-only mutabakat kaydı: yeni `cash_handovers` tablosu** — kurye gün sonu kasayı patrona devreder; kayıt SAYILAN nakit (`counted_cash_kurus`) + sistemin o an beklediği (`expected_cash_kurus`, anlık snapshot) + farkı (`diff_kurus`, denetim için saklanır) taşır. Append-only (DB seviyesinde `sipario_app`'ten UPDATE/DELETE REVOKE — ledger_entries 211 / coupon 305 deseni); fark KANIT olarak görünür kalır (BRIEF "eksik para kanıt olarak görünür kalmalı"), düzeltme yeni devir kaydı veya ledger `correction` ile — asla ezme. RLS ENABLE+FORCE + güvenli-varsayılan politika (Faz 1/2/3 deseni). Alanlar: `id`(istemci uuid), `tenant_id`, `from_user_id`(kurye), `to_user_id`(patron, nullable), `counted_cash_kurus`, `expected_cash_kurus`, `diff_kurus`, `period_start`(önceki devir/gün başı occurred_at), `occurred_at`, `device_id`, `note`(nullable).
- **Nakit atfı için `ledger_entries` += `collected_by_user_id` (nullable uuid)** — tahsilatı KİM aldı; teslim/tahsilatta oturumdaki kullanıcı yazılır. Kuryenin beklenen nakiti = `collected_by_user_id = kurye` olan nakit `payment` toplamı (period_start'tan beri). Tek kişilik bayide `collected_by = patron`, devir opsiyonel. Nullable + geriye null; `kasaOzeti` etkilenmez (hâlâ payment_type bazlı). Cross-tenant: `collected_by_user_id` de RLS-kapsamlı `User::exists()` ile doğrulanır.
- **Yeni senkron yüzeyi: entity_type `cash_handover`; order ops `assigned`/`unassigned`; `CashHandoverChangeApplier` ayrı sınıf (OrderChangeApplier simetriği, 500 satır sınırı)** — `SyncPushRequest` whitelist'ine `cash_handover` entity_type + `assigned`/`unassigned` + kasa devri op'u (`handover`) eklenir; `SyncService::snapshot` `cash_handover` ekler (append tablosu, tam çekilir); delta zaten `sync_changes` üzerinden akar. `cash_handovers` append olduğundan pull tombstone/silme üretmez.
- **iOS başlangıcı — paylaşılan Dart offline çekirdeği platform-nötrdür; iOS arayan tanıma YOK (OS izin vermez, BRIEF); iOS build/çalıştırma doğrulaması ERTELENDİ (bu ortamda Mac yok)** — Faz 4'te iOS yalnız yapılandırma/sınır belgeleme düzeyinde; gerçek iOS derleme+çalıştırma bir Mac'te veya pilotta sınanacak (dürüst açık, uydurma "iOS'ta çalıştı" YOK). Native arayan-tanıma iOS'ta zaten kapsam dışı; offline defter/sipariş/kasa çekirdeği Dart olduğundan iOS'ta aynen çalışır (doğrulama beklemede).
- **Drift v3→v4 ADDİTİF migration (Faz 0/2/3 native sözleşmesi korunur): `orders` += `assigned_user_id`; `ledger_entries` += `collected_by_user_id`; yeni `cash_handovers` tablosu** — DROP yok, yalnız ALTER ADD COLUMN + CREATE; `.g.dart` yeniden üretilir (codegen sqlite3 override `<3.0.0` prosedürü — DECISIONS Faz 3). `phone_last10`/`balance_kurus`/`sipario.db` sözleşmesi dokunulmaz.

## Faz 4 — uygulama (coder + lead düzeltmeleri, 2026-07-15)

- **uuid5 namespace SABİTİ `7a5b3c1d-9e0f-4a2b-8c6d-1f3e5a7b9c0d`; name deseni `"sipario:deliver-<tag>:<orderId>"`, tag∈{order,debit,payment,coupon}; `ids.dart::deliveryEventId`** — teslimden türeyen 4 olayın (delivered order event + ledger debit + ledger payment + coupon use) hem `id` hem `client_event_id`'si bundan; sunucu uuid5 HESAPLAMAZ, gelen client_event_id ile processed_events dedup eder. Namespace mağazada DEĞİŞMEZ (değişirse iki cihazın id'leri ayrışır, idempotensi kırılır). Teslim-DIŞI defter (elle tahsilat/correction) rastgele uuidv7'de kalır.
- **`collected_by_user_id` kaynağı `sync_meta.userId` (yeni nullable Drift kolon; login Faz 5'te doldurur, yoksa null)** — `deliver` payment kaydına + `LedgerRepository` tahsilatına geçer; sunucu ChangeApplier RLS-kapsamlı `User::exists()` ile doğrular (customer/product deseni). Kuryenin "beklenen nakit"inin dayanağı.
- **Kasa devri beklenen nakit = Σ(−amount_kurus) WHERE `payment_type='nakit' AND collected_by_user_id=fromUserId AND occurred_at>=period_start`** — YALNIZ NAKİT (kart/havale fiziksel kasaya girmez, devre dahil değil). `diff=counted−expected` kanıt olarak saklanır (BRIEF "eksik para görünür kalmalı"); sunucu counted/expected/diff'i verbatim saklar (amount_kurus gibi istemci mutabakat snapshot'ı).
- **`period_start` = fromUserId'nin son devir `occurred_at`'i, yoksa bugünün TR (+03:00) gün başının UTC karşılığı** — her kurye kendi son devrinden beri; DayEndRepository gün-sınırı deseniyle tutarlı (DST yok).
- **KEŞİF: `order_events.event_type` CHECK'i assigned/unassigned içermiyordu → migration 401'de DROP+ADD ile genişletildi** (yoksa INSERT 23514; yerel testte yakalandı).
- **`cash_handovers` append-only DB seviyesinde (REVOKE UPDATE/DELETE, mig 405) + ENABLE/FORCE RLS + güvenli-varsayılan (mig 404); from/to_user_id sert FK'siz, RLS-doğrulamalı; yalnız INSERT.**
- **Çift-dokunma koruması: `deliver`, `order.status=='delivered'` ise erken döner (yerel UI koruması); asıl garanti deterministik uuid5.** `writeLedgerEntry`/`writeCouponMovement` opsiyonel `id`/`clientEventId` aldı (verilmezse uuidv7) — teslim akışı deterministik, elle akışlar rastgele; geriye uyumlu.
- **DÜZELTME (lead, bağımsız doğrulamada yakalandı — atama türetmesi FLAKY'di):** sunucu `deriveAssignedUserId` `(occurred_at, created_at, id)` sıralıyordu; test `occurred_at`'i `now()->toIso8601String()` (SANİYE hassasiyeti) → aynı saniyede occurred_at+created_at eşit → Postgres keyfi sıra → **93/94 flaky** (coder'ın 94/94 koşumu şanslı yeşildi). coder `id` son-tiebreak'i ekledi; lead ayrıca **`created_at`'i DÜŞÜRDÜ**: created_at sunucuya özel (varış anı), istemcide karşılığı YOK — ortada kalsaydı iki cihazın uuid7 id sırasıyla çelişip sunucu/istemci FARKLI kurye türetirdi (kalıcı ıraksama, fazın deterministik-yakınsama tezini bozar). İki taraf artık YALNIZ ortak anahtarı `(occurred_at DESC, id DESC)` kullanır → istemci `_deriveAssignedUserId (occurredAt, id)` ile BİREBİR simetrik. Doğrulama: API pint + phpstan sv6 0 + **phpunit 94/94 (349 assert), 2× koşuldu (flaky değil)**.
- **AÇIK — FAZ 4 KAPANMADI: mobil DOĞRULANMADI** — bu makinede Flutter/Dart YOK (kalıcı hafıza notu); Drift `.g.dart` STALE (şema v4'e değişti, codegen koşulmadı), `flutter analyze`/`test` koşulamadı. Faz 4'ün "test yeşil" sayılması için **partnerin Flutter makinesinde codegen (`<3.0.0` override prosedürü) + analyze + test** şart. API tarafı burada TAM yeşil; mobil kod + testler (courier_test 11, migration_test v4) yazıldı, simetri/imza kontrol edildi, codegen'e hazır.

## Faz 4 — inceleme (reviewer + lead, 2026-07-15)

- **İnceleme sonucu: API YEŞİL — kırmızı çizgi ihlali YOK; Faz 4 API tarafı kapanışa uygun.** Bağımsız doğrulama bu makinede: pint temiz + phpstan sv6 0 + phpunit 94/94 (CourierSyncTest 3 tur, flaky değil). 5 kırmızı çizgi kod üzerinden tek tek: #1 izolasyon (cash_handovers ENABLE+FORCE RLS; 4 yeni yabancı id — assigned/from/to/collected_by — hepsi yazımdan önce RLS-kapsamlı `User::exists()`; TenantIsolationTest +3 dördünü de kapsar), #2 append-only (cash_handovers UPDATE/DELETE REVOKE mig 405, AppendOnlyLedgerTest 42501), #3 offline-first (teslim+kasa devri tek transaction atomik), #4 KVKK (sıfır PII log), para (int kuruş).
- **Faz 4 derin denetim YEŞİL:** teslim idempotensi (deterministik uuid5, iki cihaz aynı client_event_id → processed_events dedup → tek defter seti; mobil+sunucu testi), atama simetrisi (sunucu+istemci birebir `(occurred_at,id)`; created_at düşük; ISO↔timestamptz ve uuid text↔byte sıralamaları bu formatlarda özdeş → ıraksama yok), kasa devri (beklenen yalnız nakit+collected_by+period_start; diff verbatim saklanıyor). Kalite: tüm dosyalar <500 (en büyük ChangeApplier 327).
- **Mobil DOĞRULANMADI (reviewer kod OKUDU, koşamadı — Flutter yok): `app_database.g.dart` STALE doğrulandı** (v4 sembolleri 0; .g.dart 2026-07-14 Faz 3, tables.dart 2026-07-15 Faz 4). Kaynak iç-tutarlı + codegen'e hazır, okumada kırmızı çizgi riski görülmedi. **Faz 4 BÜTÜN olarak kapanmaz** — partner `<3.0.0` override + codegen + analyze + test koşana kadar (PLAN "SENİN SIRAN").
- **Minör bulgu #1 (reviewer) KAPATILDI (lead):** atama eşit-occurred_at + id-tiebreak (flaky'yi doğuran tam senaryo) doğrudan testsizdi → `CourierSyncTest::atama_esit_occurred_at_id_tiebreak_ile_deterministik` eklendi (aynı occurred_at'te assigned+unassigned; unassigned sonra insert → uuid7 id büyük → deterministik null; fix `(occurred_at,id)` geri alınırsa test kırmızı olur). **phpunit 95/95**, CourierSyncTest 2× yeşil. Mobil analog test partnerde eklenebilir (açık).
- **Minör bulgu #2/#3 (reviewer) — BİLİNEN AÇIK, BLOKLAMAZ (savunma-derinliği, ileride architect):** (#2) kasa devri counted/expected/diff sunucuda doğrulanmıyor (verbatim güven) — tek-kiracı/kendi-cihazı olduğundan kırmızı çizgi ihlali YOK; Faz 3 "correction payment_type verbatim" notuyla analog; ileride sunucu-tarafı yeniden-hesap savunma-derinliği eklenebilir. (#3) `period_start` `>=` + üst-sınırsız → teorik ms-çakışmasında çift sayım; pratikte imkânsız, diff zaten kanıt.

## Faz 5 — mimari (architect/lead, 2026-07-15)

- **Faz 5 ALT-FAZLARA bölünür (biri kapanmadan diğeri riske girmesin, her adım burada test edilebilir):** **5a** abonelik kilidi + durum yayını (sunucu enforcement — TAM burada test edilir); **5b** ödeme/üyelik sitesi + iyzico (sandbox burada, üretim anahtarı SENİN SIRAN); **5c** yönetim paneli (Livewire, ayrı guard, `sipario_panel` salt-okunur DB rolü); **5d** hukuk (mesafeli satış/ön bilgilendirme/iptal-iade/KVKK/e-arşiv iskeleti). Sıra: 5a → 5c → 5b → 5d (kilit çekirdeği önce; panel salt-okunur olduğundan erken; iyzico dışsal anahtar beklediğinden sonra).
- **Abonelik durumunun TEK DOĞRU KAYNAĞI sunucudur (BRIEF kırmızı çizgi):** istemci son bilinen durumu önbellekler; kilit kararını kendi başına vermez. Sunucu her push/pull yanıtına `subscription: {status, valid_until, locked_at, server_time}` ekler → istemci önbellekler, ileri-sadece saatle (`elapsedRealtime`) grace hesaplar (DECISIONS "Abonelik ve kilit").
- **5a KİLİT SUNUCUDA `sync/push` (tek yazma yüzeyi) üzerinde uygulanır; okuma/pull ASLA kilitlenmez (veri rehin alınmaz, kırmızı çizgi #5).** Kilit koşulu: `status ∈ {locked, suspended}` VEYA `valid_until < server_now`. Kilitliyken push: `occurred_at <= locked_at` olan olaylar KABUL (offline birikmiş yazımlar sunucuya akar, veri kaybı yok — BRIEF), `occurred_at > locked_at` olanlar `locked` statüsüyle reddedilir (kilit-sonrası yeni yazım). Böylece kilit yazmayı durdurur ama bekleyen kaydı yutmaz.
- **`tenants.locked_at` kolonu eklenir (kilit anı damgası):** panelden suspend/lock edilince o an yazılır; süre dolunca (valid_until geçince) push sırasında LAZY olarak `locked_at = valid_until` set edilir (ilk tespit eden push). `occurred_at <= locked_at` sınırının çıpası budur. Grace SUNUCUDA YOK — sunucuya ulaşılabiliyorsa karar kesindir; 7 gün grace yalnız İSTEMCİDE, sunucuya ULAŞILAMADIĞINDA ödeyen bayiyi yanlış kilitlememek için (DECISIONS "Abonelik ve kilit", değişmez).
- **Trial/abonelik yaşam döngüsü kolonlaştırılır:** kayıt → `trial_ends_at = now+30g`, `valid_until = trial_ends_at`, `status=trial`. Ödeme onayı → `valid_until = now+1yıl`, `status=active` (yenilemede valid_until uzar). `valid_until` tek enforcement çıpası; `trial_ends_at` yalnız "deneme miydi" bilgisi. Login guardّ (AuthController) `valid_until >= now` de kontrol eder (şu an yalnız status'a bakıyor); süre dolmuş active/trial login'de nötr 403 (okuma oturumu ayrı ele alınacaksa 5a'da netleşir — v1: login kapalı, pending sync ayrı token'sız yol DEĞİL, mevcut token'la pull+kilitli push akar).
- **Mobil kilit ekranı NÖTR (mağaza kuralı — pazarlıksız, BRIEF): fiyat YOK, "abone ol" butonu YOK, ödeme/site linki YOK — yalnız "aboneliğiniz sona erdi, destek alın" bilgi metni.** Web'de fiyat/paket/"Abone Ol" gösterilir. Sunucu tek "süresi doldu" durumu döner; EKRAN istemciye göre değişir (mobil nötr, web ticari). Bu ayrım 5a sözleşmesinin parçası (sunucu ayrım yapmaz, istemci yapar).
- **5c yönetim paneli `sipario_panel` DB rolü (Faz 1'de ertelendi) ile iş verisi tablolarına YALNIZ SELECT:** panelin bayinin siparişini/parasını DEĞİŞTİREMEMESİ kod disiplinine değil DB iznine bağlı (DECISIONS "Kiracı izolasyonu"). Panel yapabildikleri (BRIEF): tenant gör/elle-aç, trial uzat, abonelik kaydet, kilitle/aç, opsiyonel modül aç-kapa (boş/emanet), patron şifre sıfırla, cihaz listesi, veri export, kullanım istatistikleri (churn sinyalleri: bayi başına günlük sipariş, sipariş girme saatleri, kurulumdan ilk-tanımaya süre, aktif cihaz). Panel iş verisini YAZAMAZ — yalnız tenant/abonelik yönetimi + salt-okunur istatistik. Livewire, aynı repo ayrı guard (DECISIONS stack).
- **5b iyzico: geliştirme SANDBOX anahtarlarıyla kurulur+test edilir; ÜRETİM anahtarı + hesap SENİN SIRAN (PLAN listesi).** Yıllık peşin tahsilat; ödeme onayı webhook/callback ile `valid_until` uzatır (abonelik durumu sunucuda). Fiyat yapılandırılabilir (config), iş kararı beklemez.
- **Opsiyonel modüller `tenants` üzerinde JSON/kolon bayrağı (`modules`): ör. boş/emanet takibi (BRIEF: istemeyen bayide o alanlar HİÇ görünmez).** Panel açar/kapar; istemci bayrağı pull ile alır, UI'yı ona göre çizer. 5c'de panel tarafı, istemci UI ilgili faz.
- **Mağaza kuralı — mobil uygulamada KAYIT ekranı YOK, yalnız GİRİŞ (BRIEF, pazarlıksız):** üyelik/ödeme/hesap yönetimi YALNIZ web sitesinde. Mobilde satın alma ekranı/fiyat/"abone ol"/ödeme-sitesi linki BULUNAMAZ (Apple 3.1.3(f) + Play). 5b sitesi bu yüzden mobil dışıdır; mobil yalnız login + nötr kilit mesajı.

## Faz 5a — uygulama (coder + lead doğrulama, 2026-07-15)

- **`locked` reddi ≠ `rejected` (ANLAMSAL fark):** `locked` GEÇİCİdir — kilit koşulu (occurred_at>locked_at) `applyOne`'dan ÖNCE kısa devre yapar, `processed_events`'e YAZILMAZ, seq ilerletmez → abonelik yenilenince AYNI `client_event_id` retry'da `applied` olur (testli). `rejected` veri-geçersizliğidir (kalıcı; retry yine rejected). İkisi de idempotency defterine girmez; fark statü ('locked' istemciye "geçici/abonelik" sinyali) + niyet.
- **`subscription` bloğu tek kaynak:** SyncService, RLS-kapsamlı okunan tenant satırından + kilit kararındaki AYNI `$now`'dan üretir; push VE pull ikisinde yayınlanır. `{status, valid_until, locked_at, server_time}` ISO8601 UTC. `AppendServerTime`'ın top-level `server_time`'ı ayrı/additif (istemci ikisini de görür).
- **Lazy `locked_at`:** push'ta kilitliyse ve locked_at NULL ise `locked_at = valid_until ?? now` (süre-dolumu çıpası; panel-lock çıpasızsa now) — `forceFill+save` (RLS WITH CHECK id değişmediğinden geçer; sipario_app tenants'ta UPDATE'e sahip). Pull SALT-OKUMA: lazy yazım YOK (kilit push'a özgü).
- **`null valid_until = KİLİTSİZ`:** kilit koşulu `status∈{locked,suspended} VEYA (valid_until IS NOT NULL AND < now)`; NULL asla tetiklemez → factory default + gelecek-tarihli active tenant kilitsiz, mevcut 95 test bozulmadı (`null_valid_until_kilitsizdir` testiyle çivilendi).
- **Login guard:** `valid_until >= now` kontrolü `Hash::check`'ten SONRA (sabit süre harcandı → zamanlama yan-kanalı açılmaz); süresi dolmuş trial/active → mevcut NÖTR 403. `sipario_login_lookup` zaten valid_until dönüyordu (kolon eklenmedi).
- **Okuma ASLA kilitlenmez (kırmızı çizgi #5):** pull kilit kontrolü yapmaz, yalnız yayınlar; kilitliyken bekleyen (occurred_at<=locked_at) yazım akar → veri rehin alınmaz, kayıp yok.
- **LEAD DOĞRULAMA (bağımsız):** API pint temiz + phpstan sv6 0 + **phpunit 102/102 (381 assert), 2× (flaky değil)**; kilit mantığı (resolveLock/lockedOut/locked/subscriptionPayload + push/pull) satır satır okundu — koşul/lazy-çıpa/#5-okuma-serbest/seq-idempotency korunmuş, tasarım doğru. Ayrı reviewer turu Faz 5 SONU toplu incelemeye ertelendi (5a+5c+5b+5d).
- **MOBİL AÇIK (partner):** Drift `.g.dart` STALE (Faz 4 + 5a kolonları: SyncMeta.lockedAtIso/subscriptionStatus üretilmedi). `SubscriptionState.estimateServerNow` MİNİMAL — gerçek ileri-sadece saat (elapsedRealtime monoton çıpa) platform kanalı ister, partner/sonraki iş bağlar; karar mantığı (`evaluate`) SAF + testli (subscription_state_test 9). Kilit ekranı NÖTR placeholder (fiyat/buton/link YOK). Codegen+analyze+test partnerde.

## Faz 5c-1 — uygulama (coder + lead doğrulama, 2026-07-15)

- **`sipario_panel` rolü BYPASSRLS + grant matrisi (BRIEF panel sınırı DB-zorlamalı):** panel BİZE ait iç destek/istatistik aracı → BYPASSRLS ile cross-tenant OKUR (sipario_app HÂLÂ RLS altında, bayi API'si ETKİLENMEZ). İş verisi 13 tablo (customers…cash_handovers, devices, users) **yalnız SELECT** — INSERT/UPDATE/DELETE YOK → panel bayinin siparişini/parasını FİZİKSEL değiştiremez (append-only REVOKE / FORCE RLS felsefesiyle simetrik). Yazma yalnız: `tenants` SELECT+UPDATE (INSERT yok — elle tenant açma owner/Provisioning ile; DELETE yok), `admin_users` SELECT/INSERT/UPDATE, `panel_audit` SELECT+INSERT (append, ezilmez). Migration 504 grant'lar (owner koşar). `PanelTest` 5 iş tablosu × UPDATE+DELETE = 10 test 42501'i kanıtlar.
- **`admin_users` bayilerin `users`'ından TAMAMEN ayrı** (tenant yok, RLS yok — altyapı tablosu, personal_access_tokens gibi); ayrı `admin` guard + provider + `AdminUser` modeli (pgsql_panel bağlantısı). Bayi kullanıcısı admin guard'ına ASLA giremez (test).
- **Panel eylemleri `tenants` UPDATE ile, 5a `locked_at` mantığıyla TUTARLI:** lock→locked+locked_at=now; unlock→active+locked_at=null+valid_until ileri (geçmişse now+30g, asla geriye); activate→valid_until=now+365g+active+locked_at=null; extendTrial→trial_ends_at&valid_until ileri+trial; suspend→suspended+locked_at=now. Her AÇMA eylemi 5a "writable" koşulunu (locked_at null + valid_until gelecek + status trial/active) sağlar → panel kilitle=API push `locked`, abonelik kaydet=push `applied` (test kanıtı). Panel eylemi (UPDATE) ile API push aynı tenant satırını okur → tutarlı.
- **Livewire 4.3 (kuruldu); `/panel/*` route + `admin` guard; panel guest yönlendirmesi yalnız panel* yollarında** (API JSON 401 sözleşmesi korunur). UI minimal (tasarım sonraki iş). `panel_audit`: her eylem (kim/hangi bayi/ne) append; KVKK nötr (yalnız eylem türü + hedef id, iş verisi DEĞERİ yok).
- **LEAD DOĞRULAMA (bağımsız):** API pint temiz + phpstan sv6 0 + **phpunit 123/123 (421 assert), 2× (flaky değil)**; grant matrisi (mig 504) satır satır okundu — iş verisi SELECT-only, sipariş/para yazımı DB-imkânsız. Ayrı reviewer turu Faz 5 SONU toplu incelemede.
- **ORTAM AÇIĞI (Faz 1 sipario_app deseni):** `sipario_panel` rolü küme düzeyinde (docker init `10-roles.sh` yalnız ilk initdb'de koşar) → mevcut container'da ELLE kuruldu; **CI'da rol SQL'i elle koşulmalı**. `DB_PANEL_USERNAME/PASSWORD` `.env.example`'a eklenmeli (araç-korumalı, manuel — config default'u var, testler yeşil; PLAN "SENİN SIRAN").
- **5c-2'ye DEVİR:** kullanım istatistikleri (churn sinyalleri), veri export, opsiyonel modül (boş/emanet) aç/kapa, patron şifre sıfırlama, cihaz listesi.

## Faz 5b — uygulama (coder + lead doğrulama, 2026-07-15)

- **`PaymentGateway` soyutlaması (SyncApi ikizi):** arayüz + `FakePaymentGateway` (test) + `IyzicoPaymentGateway` (sandbox skeleton); AppServiceProvider bind, testler instance-swap → gerçek ağ olmadan tüm akış test edilir.
- **Üyelik + aktivasyon + subscription_payments INSERT `pgsql_owner` bağlantısıyla** (kiracı-üstü; public callback'te tenant session YOK; RLS-üstü meşru; sipario_app RLS ETKİLENMEZ; public site iş verisine dokunmaz; panel rolü karışmaz).
- **Callback İDEMPOTENT + YARIŞ-KORUMALI:** success zaten varsa no-op; yoksa tek transaction'da success kaydı + `activate`. Sert emniyet **partial unique index `(provider_ref) WHERE status='success'`** → eşzamanlı 2. insert 23505 → rollback → çift aktivasyon İMKÂNSIZ. Bilinmeyen ref → no-op; başarısızlık → durum değişmez + failed kaydı.
- **`subscription_payments` append-only:** initiated/success/failed AYRI satır (durum UPDATE'lenmez); RLS + REVOKE UPDATE/DELETE (mig 506, ledger 211 deseni; 42501 testli). Para int kuruş; **kart verisi ASLA yazılmaz** (iyzico saklar — KVKK); yalnız tutar + provider_ref + onay sürümü + zaman.
- **Aktivasyon SUNUCUda (tek doğru kaynak — BRIEF):** `valid_until = now+period`, `status=active`, `locked_at=null` → 5a kilidini AÇAR (test: kilitli tenant ödeme sonrası push `applied`). İstemci karar vermez.
- **Hukuk onayı:** checkout TÜM onayları (mesafeli satış + ön bilgilendirme + KVKK) ZORUNLU kılar (eksik→`ConsentRequiredException`, ödeme başlamaz); kabul edilen sürümler config'den birleşik string (`distance_sales:v;preinfo:v;kvkk:v`) + `consented_at`, 'initiated' satırına yazılır. Üyelik KVKK onayı zorunlu. Metinler PLACEHOLDER.
- **Mağaza kuralı:** tüm akışlar WEB; mobilde HİÇBİRİ yok.
- **LEAD DOĞRULAMA (bağımsız):** pint temiz + phpstan sv6 0 + **phpunit 132/132 (445 assert), 2× (flaky değil)**; `SubscriptionService` + mig 505 (partial unique idempotensi) satır satır okundu — para/erişim mantığı doğru.
- **AÇIKLAR (SENİN SIRAN / sonraki iş):** (a) **iyzico sandbox + üretim anahtarı = insan**; `IyzicoPaymentGateway` IYZWSv2 HMAC + checkout-form-initialize yapısı kurulu, anahtar yoksa initiate/verify açık `RuntimeException` (sessiz yanlış davranış YOK); sandbox doğrulaması anahtar gelince (webhook imza + buyer/basketItems dahil). (b) **Hukuk metinleri PLACEHOLDER** (tam metin + onay = 5d/insan). (c) **Geri dönen bayi için web login YOK** (akış: üyelik→session→abonelik); küçük, sonraki iş. (d) **MİNÖR:** erken yenilemede `activate` valid_until'ı `now+period`'a ÇEKER (mevcut kalan süreyi stack'lemez) — yıllık peşinde erken yenileme nadir; ileride stack mantığı eklenebilir (bloklamaz).

## Faz 5c-2 — uygulama (coder + lead doğrulama, 2026-07-15)

- **`modules` yayını:** `tenants` += `modules` JSONB (default `{}`); panel PANEL bağlantısıyla UPDATE eder (tenants UPDATE grant, 5c-1 matrisi — yeni yazma yetkisi EKLENMEDİ); istemciye **subscription bloğuyla** yayınlanır (5a status ile AYNI kanal; `SyncService::subscriptionPayload` += modules). Mobil UI'da alan gizleme (boş/emanet istemeyen bayide görünmez) = sonraki iş/partner.
- **Patron şifre sıfırlama OWNER ile** (panel `users`'ta UPDATE'siz — SELECT-only 5c-1 çizgisi): yeni parola üretilir, admin'e BİR KEZ döner, SAKLANMAZ; audit'e `reset_password` + hedef user id (parola DEĞERİ ASLA — KVKK). Test: yeni-parola-login + panel-users-42501 + audit'te-parola-yok.
- **Export cross-tenant güvenliği (kırmızı çizgi #1):** panel BYPASSRLS TÜM satırları görür → `PanelExportService` 12 iş tablosunun HER BİRİNDE `->where('tenant_id', $id)` AÇIK filtresi (RLS panelde yok; sızıntı ancak açık filtreyle önlenir). Test A'nın export'unda B'nin verisi geçmediğini kanıtlar. LEAD okudu — her tabloda filtre var, doğru.
- **İstatistik (churn sinyalleri):** panel BYPASSRLS agregat; TR gün/saat `AT TIME ZONE 'Etc/GMT-3'` (DayEndRepository ile tutarlı, DST yok); günlük sipariş, sipariş GİRME SAATİ dağılımı (akşam yoğunlaşma = gün içi kullanmama sinyali), kurulum→ilk sipariş süresi, aktif cihaz. Salt-okunur, yeni kalıcı durum YOK.
- **Kırmızı çizgi korundu:** panel iş verisini HÂLÂ yazamaz — modül+şifre testlerinde 42501 yeniden kanıtlandı; modül=tenants UPDATE, şifre=owner.
- **LEAD DOĞRULAMA:** bağımsız pint temiz + phpstan sv6 0 + **phpunit 137/137 (466 assert), 2×**; PanelExportService satır satır okundu (cross-tenant sızma yok).
- **AÇIKLAR:** modül bayrağının mobil UI yansıması partner/sonraki iş; export JSON (CSV eklenebilir); şifre sıfırlama tek-patron varsayar (role=patron firstOrFail; çok-patron edge minör).

## Faz 5 — inceleme (reviewer + lead, 2026-07-15)

- **İnceleme sonucu: Faz 5 SUNUCU KODU YEŞİL — kırmızı çizgi ihlali YOK.** Bağımsız API pint temiz + phpstan sv6 0 + **phpunit 137/137 (466 assert)**.
- **#1 İzolasyon (panel BYPASSRLS — bu fazın EN KRİTİK riski) YEŞİL:** `PanelExportService` 12 iş tablosunun HEPSİNDE açık `tenant_id` filtresi (filtresiz sorgu YOK); `PanelStatsService` 5 metodun hepsinde tenant_id filtresi; grant matrisi (mig 504) 13 iş tablosu SELECT-only (PanelTest + PanelExpansionTest 42501×çok); sipario_app HÂLÂ RLS altında; owner-privileged yollar (tenant açma, şifre sıfırlama) panel yazma yetkisinden doğru ayrılmış. **Panel cross-tenant sızma riski YOK.**
- **#2 append-only YEŞİL** (subscription_payments REVOKE UPDATE/DELETE + partial unique success index, 42501 testli). **#4 KVKK YEŞİL** (sıfır PII/kart/parola-değeri log; nötr kilit mesajı; subscription bloğunda fiyat/link YOK). **#5 veri rehin alınmaz YEŞİL** (pull kilitlenmez; occurred_at<=locked_at bekleyen yazım akar; locked reddi processed_events'e yazılmaz → yenilenince retry applied). **Abonelik durumu sunucu YEŞİL; para int kuruş + mağaza kuralı YEŞİL.**
- **Kalite:** tüm Faz 5 dosyaları <500 (en büyük SyncService 338), ölü kod yok, Livewire phpstan sv6 geçiyor.
- **GÜVENLİK BULGUSU (reviewer) — KAPATILIYOR (lead, coder'a verildi):** `IyzicoPaymentGateway::verify()` callback POST GÖVDESİNDEKİ `paymentStatus`'a güveniyordu; callback CSRF-muaf olduğundan ÜRETİM anahtarıyla forged body (`{provider_ref, paymentStatus:SUCCESS}`) POST'layarak BEDAVA abonelik aktive etme vektörü doğardı. Bugün zararsız (Fake kullanımda + anahtarsız `assertConfigured` fail-closed). **Lead kararı: verify() YAPISAL olarak gövde-güven yolundan çıkarılıyor** — iyzico SUNUCU-SUNUCU `retrieve` + IYZWSv2 imza doğrulaması; retrieve yapılamıyorsa fail-closed (asla gövdeden success); forged-body reddi testi. **Gerçek sandbox retrieve+imza doğrulaması anahtar gelince (SENİN SIRAN — GÜVENLİK MADDESİ, yalnız smoke-test DEĞİL: verify MUTLAKA geri-sorgu+imza yapmalı).**
- **Minör (defect değil):** export/grant testleri temsili alt küme tohumluyor (mekanizma tekdüze döngü / tek GRANT olduğundan kanıtlı, kırmızı çizgi riski yok); tam-tablo testi ileride eklenebilir.

## Faz 5b — güvenlik sertleştirme (coder + lead doğrulama, 2026-07-15)

- **`IyzicoPaymentGateway::verify()` FAIL-CLOSED:** callback GÖVDESİNDEKİ `paymentStatus`'a ASLA güvenilmez; gövdeden YALNIZ `token` alınır → iyzico SUNUCU-SUNUCU `retrieve` (`/checkoutform/auth/ecom/detail`, IYZWSv2 imzalı) → sonuç YALNIZ iyzico'nun döndürdüğü `paymentStatus` + `paidPrice`'tan türer. Token yok → `success:false`; retrieve status≠success → RuntimeException; anahtarsız `assertConfigured` patlar → **gövdeden `success:true` üretmek YAPISAL olarak imkânsız**. Forge edilen callback (`{provider_ref, paymentStatus:SUCCESS}`) aboneliği AKTİVE ETMEZ (test kanıtlı).
- **Tutar manipülasyonu koruması:** iyzico'nun döndürdüğü `paidPrice` → kuruş == `config('subscription.price_kurus')`; eşleşmezse (saldırgan 1 kuruş ödedi) `success:false`. Para int kuruş; sınırda dönüşüm (`kurusFromDecimal`).
- **Test (GERÇEK IyzicoPaymentGateway + `Http::fake`, 6 test — `PaymentSecurityTest`):** forged-body aktive-etmez (asıl kanıt), gövde-yoksay + retrieve-esas, iyzico FAILURE'ı ezme yok, tutar-manipülasyon reddi, tokensiz + retrieve-hata fail-closed. Mevcut 137 (Fake yolu) BOZULMADI. KVKK: retrieve yanıtından kart/PII saklanmaz/loglanmaz.
- **LEAD DOĞRULAMA:** bağımsız pint temiz + phpstan sv6 0 + **phpunit 143/143 (476 assert), 2×**; `verify()` + `retrievePaymentStatus()` satır satır okundu — gövde-güven yolu YOK, fail-closed yapısı doğru.
- **AÇIK (SENİN SIRAN — GÜVENLİK):** gerçek sandbox `retrieve` + IYZWSv2 imza doğrulaması ANAHTARLA sınanacak (HTTP+HMAC YAPISI kuruldu, gerçek çağrı bu ortamda doğrulanmadı — sandbox anahtarı yok). Güvenlik property'si (fail-closed, gövde-güven yok) testli; kalan = gerçek endpoint/imza smoke+forge testi.

## Faz 5 — açık kapatma (coder + lead doğrulama, 2026-07-15)

- **Geri-dönen bayi WEB LOGIN `/giris` (Livewire `Site\Login`):** `sipario_login_lookup` (API login deseni, SECURITY DEFINER, cross-tenant deterministik tek satır) + `Hash::check` + **dummy-hash zamanlama koruması** (e-posta enumeration YOK, nötr hata); başarıda Register ile AYNI web session (tenant bağlamı) → `/abonelik`. **FARK (bilinçli): abonelik/valid_until KONTROL EDİLMEZ** — süresi dolmuş bayi tam da ÖDEME için giriş yapar (billing sitesi mantığı, API login'in tersi); yalnız user `active` olmalı. Mağaza kuralı: bu giriş WEB; mobilde siteye giriş/link YOK.
- **Web login guard:** tam auth guard KULLANILMADI; hafif session (`subscription_tenant_id`/`subscription_email`, Register anahtarlarıyla aynı). `/abonelik` zaten session-tenant'a bağlı; tam web guard bu faz için gereksiz yüzey. dummy-hash sabiti `AuthController` ile AYNI (kod tekrarı kabul — reviewer'ın denetlediği AuthController hareket ettirilmedi).
- **Tam-tablo izolasyon testleri (reviewer minör KAPATILDI):** `PanelExpansionTest` export 12 iş tablosunun HEPSİNİ A+B tohumlar → A export'unda 12 tablo dolu + B'nin hiçbir id'si sızmaz; `PanelTest` grant write-denial 5→**13 tablo** (UPDATE+DELETE 42501 tam matris).
- **LEAD DOĞRULAMA:** bağımsız pint temiz + phpstan sv6 0 + **phpunit 163/163 (550 assert), 2×**; `Login.php` okundu — enumeration yok, session doğru, subscription-check-yok billing için bilinçli.
- **FAZ 5 KOD TARAFI TAM.** Kalan tümüyle DIŞSAL (PLAN "SENİN SIRAN"): iyzico anahtar + gerçek retrieve/imza testi, hukuk metin prose'u (5d), mobil codegen+test (partner).

## Faz 5c — CI ortam düzeltmesi (lead, 2026-07-15)

- **CI'da `sipario_panel` rolü eksikti → migration 504 (grant_panel_role) `42704 role does not exist` ile patlıyor, TÜM API CI (push + PR #11) kırılıyordu** (yerelde 163/163 geçse de — rol yerelde elle kurulmuştu). `.github/workflows/api-ci.yml` "Postgres rollerini kur" adımına `CREATE ROLE sipario_panel LOGIN PASSWORD 'sipario_panel_dev' NOSUPERUSER NOCREATEDB NOCREATEROLE BYPASSRLS` + CONNECT/USAGE eklendi (yerel `10-roles.sh` ile birebir; şifre `phpunit.xml` `DB_PANEL_PASSWORD` ile eşleşir). **Push + PR CI YEŞİL** (56-57s, 163 test). Faz 5c "CI'da rol SQL'i elle koşulmalı" açığı KAPANDI (commit 55595ec).
- **Workflow notu (coder önerisi — sonraki tur için):** elle commit'ler kendi turunda PUSH edilmeli (Stop hook'un pushuna bel bağlanmasın); bu, "origin geride → başlamamış mı?" döngüsel karışıklığını bitirir. (coder bu vardiya session-limit'e takıldı — 07:50 Europe/Istanbul reset.)

## Faz 5d — hukuk iskeleti (lead solo, 2026-07-15)

- **Hukuk belgeleri CONFIG-DRIVEN iskelet:** `config('subscription.legal_docs')` (slug→başlık+sürüm_anahtarı) + `/sozlesme/{doc}` route (bilinmeyen slug 404) + `legal/show.blade.php` + 4 partial (`legal/docs/`: mesafeli-satis, on-bilgilendirme, iptal-iade, kvkk-aydinlatma — Yönetmelik/KVKK asgari başlık yapısıyla). Checkout onay kutuları (Subscribe) belgelere LİNKLENDİ. **Metinler PLACEHOLDER (⚠️ "hukuk onayından geçmemiştir" bannerı) — TAM METİN + HUKUK ONAYI İNSAN İŞİ (SENİN SIRAN).** Kabul edilen sürüm zaten `subscription_payments`'a yazılıyor (5b); iskelet o sürüm sistemini görünür belgeye bağlar.
- **LegalDocsTest 2 test** (4 belge 200 + başlık/sürüm/PLACEHOLDER; bilinmeyen 404). **phpunit 165/165, pint, phpstan 0.** Lead SOLO (coder session-limit'te) — düşük risk: şablon + basit route + kozmetik link, iş mantığı/kırmızı çizgi yüzeyi YOK; self-review temiz. (commit 16e7621)

## Faz 6 — hazırlık: demo hesap seeder (lead solo, 2026-07-15)

- **Mağaza inceleme DEMO hesabı seeder'i (`DemoSeeder`) — BRIEF "içi dolu demo hesabı" + arayan-tanıma gereksinimi:** `demo@sipario.com.tr` / `demo1234`, AKTİF tenant + `valid_until = now+10yıl` (inceleme boyunca kilitlenmez); 4 TELEFONLU müşteri (arayan-tanıma demosunu besler), 2 ürün, 3 teslim edilmiş sipariş + defter (2 veresiye borç, 1 nakit peşin), `balance_kurus` defterden türetilir. Provizyon **owner ile** (`Provisioning::asOwner` deseni; superuser RLS'i atlar, tenant_id açıkça yazılır). **İDEMPOTENT** (demo e-postası varsa atlar). SAHTE demo verisi (KVKK açısından temsili; gerçek bayiye verilmez). Çalıştırma: `php artisan db:seed --class=DemoSeeder`.
- **DemoSeederTest 2 test** (dolu+aktif+telefonlu+borç-türetildi+kimlik-geçerli; idempotent ikizlemez). **phpunit 167/167, pint, phpstan sv6 0.** Lead SOLO — test-verisi, kırmızı çizgi yüzeyi yok. (commit 81ad54b). **AÇIK (SENİN SIRAN):** demo hesabının mağaza konsollarına girilmesi + arayan-tanıma tanıtım videosu = insan.

## Faz 5d prose + Faz 6 mağaza paketi — taslaklar (lead + 3 ajan, 2026-07-16)

- **4 hukuk belgesi PLACEHOLDER'dan gerçek TÜRKÇE TASLAK metne yükseltildi** (`legal/docs/*.blade.php`): mesafeli-satis (9 madde: taraflar/konu/nitelik/bedel/ifa/cayma m.15-1-ğ/süre-fesih/uyuşmazlık/yürürlük), on-bilgilendirme (Yönetmelik m.5 kalemleri), iptal-iade (cayma istisnası+iade+iptal+veri-saklama), kvkk-aydinlatma (m.10 aydınlatma + m.11 haklar + veri-sorumlusu/işleyen ayrımı + saklama + açık rıza). **⚠️ TASLAK bannerı KORUNDU, şirket bilgisi `[köşeli]` (uydurma YOK), B2B/tacir belirsizliği için her belgede `<!-- HUKUK NOTU: avukat netleştirsin -->`.** Metin sağlam ilk taslak; **tam onay HÂLÂ İNSAN/AVUKAT (SENİN SIRAN)** — sıfırdan yazmak yerine inceleme.
- **Faz 6 mağaza başvuru metin paketi** (`docs/magaza/`: play-data-safety, play-listing, app-store-listing, inceleme-notlari, README — hepsi ⚠️ TASLAK): Play Data Safety form cevabı (müşteri verisi TR sunucu, üçüncü-taraf paylaşımı YOK, READ_CONTACTS "izin var veri toplanmıyor"), Play+App Store liste metinleri, inceleme notları (demo hesap `demo@sipario.com.tr`/`demo1234`, "kayıt ekranı yok yalnız giriş" Apple 3.1.3-f/Play gerekçesi, USE_FULL_SCREEN_INTENT çekirdek-işlev beyanı, video PLACEHOLDER). **iOS'ta arayan tanıma YOK** açıkça yazıldı (yanlış-vaat reddi önlenir).
- **3 ajanlı otonom akış (fan-out+review): legal-drafter + store-writer paralel yazdı, legal-reviewer 9 maddelik kırmızı-çizgi denetimi yaptı → KRİTİK İHLAL YOK.** Bağımsız çapraz-doğrulama (lead): mağaza izin seti SMS/Call Log grubundan temiz (yalnız CallScreening+READ_CONTACTS+SAW+FULL_SCREEN_INTENT+POST_NOTIF yedek); inceleme notları DemoSeeder ile birebir tutuyor (4 telefonlu müşteri, 2 veresiye+1 nakit+1 hareketsiz, +905321112233 Ahmet, 10yıl valid_until). Reviewer bulgusu (gizlilik URL slug `kvkk`→`kvkk-aydinlatma`) düzeltildi. **Kod/iş mantığı yüzeyi YOK (yalnız Blade metin + md); test etkilenmez.**

## Faz 6 — hesap-silme sayfası + uçtan-uca denetim + YAPILACAKLAR.md (lead + 3 ajan, 2026-07-16)

- **Google Play ZORUNLU hesap/veri silme sayfası KURULDU:** `/hesap-silme` route (`account.deletion`, `Route::view`) + statik `legal/hesap-silme.blade.php`. Play, hesap sistemi olan uygulamalar için genel erişilebilir bir silme URL'i şart koşar; data-safety formu buraya işaret eder. İçerik BRIEF ile tutarlı: uygulamada silme butonu YOK (talep destek kanalından), veri rehin alınmaz ama talep üzerine silinir, mevzuat saklama yükümlülükleri saklı, bayi=veri sorumlusu/Sipario=veri işleyen. İletişim/süre alanları `[köşeli]` (insan/avukat). `AccountDeletionPageTest` 2 test (200 + içerik + KVKK link). Mağaza doküman URL'leri bağlandı (Play silme URL = `/hesap-silme`, gizlilik URL = `/sozlesme/kvkk-aydinlatma`). **phpunit 169/169 (587 assert), pint, phpstan sv6 0.**
- **iyzico'ya DOKUNULMADI (bilinçli):** `initiate()` buyer/basketItems alanları için alıcı adı/telefon/kimlik DTO'da yok → doldurmak Subscribe akışını değiştirmeyi + anahtarsız-doğrulanamayan ödeme kodu yazmayı gerektirir. PLAN bunu "anahtar gelince" park etmiş; doğrulayamayacağım ödeme kodu yazmam — anahtar gelince sandbox'la birlikte yapılır.
- **3 ajanlı uçtan-uca DENETİM (salt-okunur): audit-phases + audit-redlines + audit-external-deps.** Sonuç: PLAN/DECISIONS'ın "kapandı" dediği her şey kodla ÖRTÜŞÜYOR, uydurma/abartı yok; mobil-doğrulanamadı ve iyzico-anahtarsız gibi dışsallar dürüstçe işaretli. **Denetimin yakaladığı iç-tutarsızlıklar düzeltildi:** PLAN panosu %79 vs %80 → tek sayı %79'a harmonize; test sayısı 167 iddiası tam paket koşularak **169/169** ile teyit edildi.
- **Denetimin PLAN'da OLMAYAN yeni dışsal bulguları (YAPILACAKLAR.md'ye işlendi):** (a) **Android release imza anahtarı** — `build.gradle.kts:32-36` release hâlâ DEBUG anahtarıyla imzalı (`// TODO`), debug-imzalı AAB Play'e yüklenemez; (b) **Mac/Xcode** iOS derleme için (ekipte kimde belirsiz); (c) **e-arşiv fatura sağlayıcı** — BRIEF'te var, kodda entegrasyon SIFIR; (d) **VERBİS kaydı** — KVKK veri sorumlusu sicili; (e) transactional mail yok (`MAIL_MAILER=log`).
- **`YAPILACAKLAR.md` OLUŞTURULDU (kullanıcı açıkça istedi):** proje sahibinin tüm insan/dışsal işleri tek dökümanda, öncelikli (🔴kritik-yol/🟡önemli/🟢küçük), her kalemde NE/NEDEN/NASIL/kanıt + kod düzeyinde doğrulanmış kırmızı-çizgi güvence bölümü. Ortak hafızanın 4. dosyası; BRIEF'in "sonradan doküman yaratma" kuralına istisna (talep üzerine).
- **DENETİM DÜZELTMESİ — kırmızı çizgi #6 regresyon bekçisi CI'a bağlandı (audit-redlines bulgusu):** DECISIONS'ın önceki "manifest izin denetimi CI'da merged manifest üzerinde çalışır" ifadesi repo durumuyla ÖRTÜŞMÜYORDU — `check_permissions.sh` mevcut ama hiçbir workflow onu çağırmıyordu (mobil CI yok; merged manifest gradle build ister). **İKİ KATMANLI SAVUNMA kuruldu:** (1) `scripts/check_permissions_source.sh` — KAYNAK manifesti Flutter/gradle'sız denetler (yasaklı SMS/Call Log izni plain beyan + READ_CONTACTS/CallScreeningService düşüşü), pozitif kontrolle yerelde doğrulandı (enjekte edilen `READ_PHONE_STATE`→exit 1); (2) `.github/workflows/manifest-lint.yml` — bu scripti ubuntu'da build'siz koşar (push/PR, manifest/script değişince). **Merged-manifest katmanı (3. parti enjeksiyonu) hâlâ `check_permissions.sh`'in işi ve TAM MOBİL CI ile gelecek** (partnerin Flutter'lı makinesi/codegen işiyle birlikte — YAPILACAKLAR'a eklendi). Böylece #6'nın "otomatik sürekli kanıtla" şartı mobil CI'a kadar en sık regresyonu (kendi manifestimiz) otomatik yakalar.

## 4b Dilim 2 — sipariş ekranları (lead solo, 2026-07-21)

- **Ödeme tipi SİPARİŞ GİRİŞİNDE sorulmaz, TESLİMDE sorulur.** Sipariş telefonda birkaç dokunuşta bitmeli (BRIEF); para mal gidince konuşulur. `OrderRepository.deliver(paymentType:)` zaten defteri o anda yazıyor — ekran bu sözleşmeyi izliyor, sipariş kaydı ödeme tipini boş bırakıyor.
- **veresiye/kupon MÜŞTERİ ZORUNLU (`teslimOdemeTipleri(musteriVar:)`).** Müşterisiz veresiye, kimseye ait olmayan bir borç kaydı üretirdi (defter tutarlılığı, kırmızı çizgi #2 ruhu); müşterisiz kupon zaten `deliver`'da `ArgumentError`. Kural ekrandan ayrı saf fonksiyonda ve testli — nakit/kart/havale müşterisiz serbest (tezgâh satışı).
- **Kupon bakiyesi yetmese de teslim REDDEDİLMEZ** — ekran "N adet düşer · kalan M" der, M<0 kırmızı görünür, kayıt yine yazılır (BRIEF: teslim edilmiş mal gerçektir, düzeltme kaydıyla kapanır). Ekranın gösterdiği adet `kuponAdedi(lines)` = `deliver`'ın couponQty'siz uyguladığı kuralın AYNISI; test ikisini birbirine bağlıyor (ekran ile defter farklı sayı konuşamaz).
- **`parseKurus` (yeni `lib/screens/money.dart`) kullanıcı yazımı ↔ int kuruş sınırının TEK yeri.** TR yazımı esas: "1.234" binliktir (123400 kuruş), "12,50"/"12.50" ondalıktır. **2 haneden uzun kuruş REDDEDİLİR** — yuvarlamayı kullanıcıya sormadan yapmayız (para). Negatif reddedilir. `formatKurus` da buraya taşındı (gidiş-dönüş testli).
- **Ürün yönetimi ekranı (Menü → Ürünler) Dilim 2 kapsamına ALINDI** (planda ayrı madde değildi): taze kurulumda bayinin hiç ürünü yok, ürünler yalnız senkronla gelseydi sipariş ekranı ilk gün boş kalırdı. Silme YOK, **pasifleme** var — geçmiş sipariş satırları ad/fiyatı kendi içinde taşır, bozulmaz.
- **Serbest satır** (katalogda olmayan tek seferlik iş) sipariş satırı üretir ama ürün kaydı OLUŞTURMAZ (`LineInput.productId` null) — katalog çöplenmesin.
- **Sipariş satırı düzenleme ekranda YOK (bilinçli sadelik):** `addLine`/`removeLine`/`setNote` repoda hazır; açık sipariş yanlışsa iptal edilip yeniden girilir. Saha isterse eklenir.
- **Doğrulama:** `dart analyze` 0 · `flutter test` **109/109** · debug APK derlendi · `check_permissions_source.sh` temiz. Sipariş ekranları için mağaza-kuralı regresyon testi eklendi (kayıt/abone/satın al yasak kelimeleri).

## 4b Dilim 3 — defter/tahsilat/gün-sonu ekranları (4 ajanlı hat, 2026-07-21)

- **Hat SIRALI kuruldu (auditor→coder→tester→reviewer), paralel DEĞİL:** hepsi aynı dev ağacında çalışır (worktree yasak — kullanıcı kararı) ve inceleme donmuş ağaçta koşmalı (önceki vardiyaların dersi). Auditor salt-okunur tiple (Explore) koştu — analiz aşamasında yazma yetkisi bilinçli verilmedi.
- **Düzeltme akışı YALNIZ ters kayıt üretir (kırmızı çizgi #2):** hareket satırı menüsünden "Ters kayıtla düzelt" → `LedgerRepository.duzeltme`/`CouponRepository.kuponDuzelt`; UI'da UPDATE/DELETE yolu YOK. Test bunu çift yönlü kanıtlar: düzeltme sonrası satır sayısı +1 VE orijinal satır (drift value-equality) hiçbir alanı değişmeden durur.
- **Defter bölümü ayrı dosyada (`customer_ledger.dart`):** customer_detail 500 satır sınırını aşacaktı; bölüm `CustomerLedgerSection` olarak ayrıldı (417 satır). Gün sonu ekranı (`day_end_screen.dart`) TAMAMEN salt-okunur — tek yazma düğmesi bile yok.
- **Salt-okunurda düzeltme menüsü HİÇ render edilmez** (onDuzelt=null → widget yok); tahsilat/kupon-sat SnackBar ile engellenir (Dilim 1/2 deseni). Fark bilinçli: görünmeyen menü, "basınca uyarı veren düğme"den daha az kafa karıştırır; ana eylemler ise görünür kalıp kilit sebebini söyler.
- **YENİ TEST DERSİ (tester, 6 dk asılı koşumla yaşandı):** widget-testin sahte-zaman diliminde yalnız watch() akışları değil, DÜZ drift Future sorguları da (`getSingle()`, `.first`) `tester.runAsync` sarmalı olmadan await edilirse ASILIR. Normal tam-paket süresi ~6 sn; dakikalara uzayan koşum = asılı test, 10 dk timeout'u beklenmez.
- **İnceleme YEŞİL (reviewer + lead bağımsız):** 8 madde kod kanıtıyla (append-only, para int kuruş, mağaza, KVKK sıfır log, salt-okunur, offline-first, kalite <500, ekran-defter tutarlılığı). Repository katmanına DOKUNULMADI. Doğrulama üç bağımsız koşum: `dart analyze` 0 · `flutter test` 130/130 · APK derlendi · manifest bekçisi temiz.

## 4b Dilim 4 — kurye + kasa devri (4 ajanlı hat + lead, 2026-07-21)

- **Kurye listesi mobile SUNUCUDAN `team` bloğuyla iner (architect):** push/pull yanıtına subscription'ın yanına; `SyncService::teamPayload` YALNIZ id/name/role/status seçer (kolon listesi açık — $hidden'a güvenilmez; parola/telefon/e-posta yapısal olarak payload dışı). Sorgu sipario_app bağlantısında = FORCE RLS → cross-tenant sızma imkânsız (SyncTeamTest kanıtlı). `sync_changes` delta yolu bilinçli REDDEDİLDİ (users hiç push edilmediğinden delta günlüğüne düşmüyor); team her yanıtta tam liste.
- **Mobil `users` aynası TOPTAN tazelenen önbellek (Drift v7 additif):** `team=null` (eski sunucu) → yerel aynaya DOKUNMA (silinirse kurye adımları yanlış gizlenir — testli); `[]` → temizle; liste → delete+insert tek transaction.
- **Rol-yetki tek saf fonksiyonda (`yetkiler()` — K2 matrisi):** kurye = teslim+tahsilat+KENDİ kasa devri (kuryeVar'dan bağımsız — kurye kendisi kanıttır); ürün/gün-sonu/kupon-satışı/düzeltme = patron|operator; **atama ve (yönetici için) kasa devri = yönetici VE aktif kurye VAR** → tek kişilik bayide bu öğeler HİÇ RENDER edilmez (BRIEF pazarlıksız; kontrast çifti regresyon testli). Görünürlük koşulunun kaynağı yerel users aynası (`watchAktifKuryeler`); taze girişte boş liste → güvenli varsayılan (gizle).
- **Kasa devri:** beklenen nakit ekranda ve kayıtta AYNI koddan (`CashHandoverRepository.onizle()` refactor — paralel hesap yasağı); sayılan tutar parseKurus; fark (eksik/fazla) KANIT olarak append-only kayda girer ve geçmişte kırmızı görünür; düzeltme YENİ devirle (UPDATE/DELETE yolu yok — reviewer dosya:satır kanıtladı).
- **Guzzle 7.14.0→7.15.1 (+psr7 2.13.0):** 4 Dependabot uyarısı (orta) kapandı; composer.json değişmedi, majör atlama yok, `composer audit` temiz, 174/174 yeşil.
- **KOORDİNASYON KAZASI ve dersleri (PLAN tuzaklarına işlendi):** ad çakışması yüzünden tasarım mesajı önceki vardiyanın bitmiş 'coder' ajanına gitti ve onu DİRİLTTİ — Dilim 4'ü paralel yazmaya başladı; nazik "dur" tur ortasında etkisiz → `TaskStop` ile sert sonlandırıldı, ağaç donması mtime taramasıyla doğrulandı, işi coder-2 devraldı; coder-2 de doğrulama aşamasında sessiz kalınca kalan koşuları LEAD tamamladı. Eski-coder'ın test dosyasında Dilim 1 dersi (addTearDown(db.close) + eksik pump(5s)) tekrarlanmıştı — lead düzeltti; iki zombi flutter_tester'ın kilitlediği sqlite3.dll üç koşum yaktı (tanı: süreçleri AYRI sorgula; dll'i tutanı Modules üzerinden bul).
- **İnceleme YEŞİL (reviewer-2 + lead bağımsız):** 8 madde kanıtlı; mobil 159/159 · API 174/174 (608) · pint ✓ · phpstan 0 · audit temiz · APK ✓ · izin bekçisi ✓. Minör (bloklamaz): drift çoklu-db test uyarısı (harness), pubspec.lock geçişli kaymalar, Faz 4'ten devreden bilinen açıklar (kasa verbatim/period_start) değişmedi.
- **4b · Saha UI KAPANDI — kodla yapılabilir iş tükendi; kalan tümü dışsal (YAPILACAKLAR.md + PR #11 + gerçek cihaz + pilot).**

## Gerçek cihaz testi — saha hataları ve onarım (lead, 2026-07-22)

- **Tek-yazıcı kuralı (yeni, pazarlıksız):** `sipario.db`'yi `version` parametreli İKİNCİ bir açıcıyla (sqflite/SQLiteOpenHelper) AÇMAK YASAK — user_version damgasını ezip Drift migration'ını raydan çıkarır (iki cihazda sonsuz-loading olarak yaşandı). Tek yazıcı AppDatabase; native taraf yalnız versiyonsuz `OPEN_READONLY`. Faz 0 ekranı bu yüzden AppDatabase'e taşındı, `local_db.dart` silindi.
- **Migration KENDİNİ ONARIR:** v7 işareti (`users` tablosu) varken onUpgrade atlanır (Drift damgayı yeniden yazar); tüm ALTER'lar duplicate-column'a toleranslı. Sebep: damga ezilmesi bir daha yaşanırsa uygulama kilitlenmek yerine kendini tamir etmeli. Açılış hataları artık ekrana basılır (main.dart) — sonsuz spinner yapısal imkânsız.
- **Spike çöpü temizliği:** eski Faz 0 tohum/test kayıtları (id `c1/c2/c3`, `c-<zaman>` — outbox'sız, sunucuya hiç gitmeyen hayaletler) beforeOpen'da silinir; kimlik biçimleri UUIDv7 ile çakışamaz (kanıt migration_test'te).
- **Native adres sorgusu `customer_addresses`'e taşındı** (Faz 2'den beri unutulan devir — taze kurulumda `c.address` yoktu, sorgu patlayıp her aramayı "kayıtsız" gösteriyordu): birincil arşivsiz adres alt-sorguyla; arşivli müşteri/telefon eşleşmez. Kotlin SQL'inin birebir kopyası Dart sözleşme testinde koşar (senkron tutulmalı).
- **Doğrulama:** analyze 0 · flutter test 161/161 (+2 regresyon: damga-ezilme onarımı, native SQL sözleşmesi) · APK cihaza kuruldu. Cihaz teyidi (açılış+kart) devralan kişide.

## Vardiya özeti

- **Vardiya 2026-07-15 (otonom, ajanlarla): Faz 4 (Kurye) + Faz 5 (Para) SUNUCU KODU tamamlandı ve incelemeden geçti (167/167, kırmızı çizgi ihlali yok), 5d hukuk iskeleti + Faz 6 demo hesabı kuruldu, CI `sipario_panel` rolüyle yeşile döndü; kalan iş TÜMÜYLE DIŞSAL (iyzico anahtar + verify retrieve/imza güvenlik testi, hukuk prose, mobil codegen-partner, mağaza hesapları, pilot) — PLAN "SENİN SIRAN".**
- **Vardiya 2026-07-16 (otonom, 6 ajan): 5d hukuk metinleri PLACEHOLDER→gerçek Türkçe TASLAK + Faz 6 mağaza paketi (docs/magaza/) + Google Play zorunlu hesap-silme sayfası (169/169); kırmızı çizgi #6 regresyon bekçisi CI'a bağlandı (iki katmanlı: kaynak-manifest Flutter'sız `manifest-lint.yml` + merged-manifest mobil CI'a devir); 6 ajanlı uçtan-uca denetim kod-belge örtüşmesini + 6 kırmızı çizgiyi doğruladı, yeni dışsal bulgular (release imza anahtarı/Mac-Xcode/e-arşiv/VERBİS) çıkardı; kullanıcı için `YAPILACAKLAR.md` dökümanı oluşturuldu (dışsal işlerin kanonik kaynağı). Kalan iş DIŞSAL (SENİN SIRAN).**
- **Vardiya 2026-07-17: PR #11 güncel dev'e göre yeniden yazıldı (başlık "Faz 3 — Defter"di, dal Faz 3+4+5+6 taşıyordu — merge kararını verecek insan kapsamı yanlış görüyordu); MERGEABLE/CLEAN, CI iki kontrol yeşil, merge düğmesi insanda. Kalite kapıları bu makinede BAĞIMSIZ doğrulandı (169/169 · 587 assert · pint · phpstan sv6 0). ÖNEMLİ KOORDİNASYON NOTU: `main` hâlâ Faz 2'de (PR #10) — GitHub'da main'deki PLAN.md Faz 3–7'yi "bekliyor" gösteriyor ve "kod işi kalmadı" ifadesiyle çelişiyor sanılıyor (kullanıcı bu vardiya yanıldı); çelişki yok, iş dev'de. Durum panosunun tek doğru kaynağı DEV'deki PLAN.md'dir; PR #11 merge edilince görüntü düzelir. Yeni kod işi yapılmadı — dışsal-girdisiz iş gerçekten tükenmiş (iddia sınandı, doğru).**
- **Flutter 3.44.6 geliştirici makinesine kuruldu (2026-07-17, kullanıcı onayıyla; `C:\src\flutter`, JDK=Android Studio JBR 21)** — mobil doğrulama partner bağımlılığından çıktı; codegen+analyze+test+APK döngüsü artık bu makinede dönüyor. Tetikleyici: kullanıcının "APK alıp test edemez miyiz?" sorusu — cevap: mağaza/hukuk/ödeme TEST için gerekmez, debug APK yan yüklenir (Faz 0 deseni).
- **Türkçe-yol tuzağı üç araçta çözüldü (proje yolu `OneDrive\Masaüstü` — ü karakteri):** build_runner AOT → `--force-jit`; `flutter analyze` LSP çökmesi → kalite kapısı dahil her yerde `dart analyze`; AGP ASCII-yol reddi → `gradle.properties`'e `android.overridePathCheck=true` (ASCII-yollu makinede etkisiz). Kapı scripti bilinen Flutter yolunu PATH'e ekleyen emniyet de aldı.
- **Mobil doğrulama SONUCU (2026-07-17): codegen 62 çıktı (`.g.dart` 1.332 satır eksikmiş — Faz 4/5a şeması hiç üretilmemişti), 1 gerçek analiz hatası düzeltildi (`courier_test.dart` drift/flutter_test `isNull` çakışması → `hide isNull`), `flutter test` 72/72, debug APK derlendi** — YAPILACAKLAR madde 2 kapandı; gerçek-cihaz doğrulaması (arayan tanıma, journal_mode) pilot/cihaz işi olarak açık.
- **Pano düzeltmesi (%79→%68): bayinin kullanacağı UI ekranları HİÇBİR fazın ağırlığında değildi — her faz "UI sonraki iş" deyip devretmiş, iş sahipsizdi (kullanıcı yakaladı).** "4b · Saha UI" %15 ağırlıkla panoya eklendi, eski satırlar ×0,85; düşüş gerileme değil ölçek dürüstleşmesi. SONRAKİ BÜYÜK KOD İŞİ = 4b (giriş/müşteri/sipariş/defter/kasa/kurye ekranları; repository+sync+native hazır, tokenProvider bağlanacak).
- **4b Dilim 1 (Saha UI, 2026-07-17): giriş+oturum+kabuk+müşteri ekranları.** Oturum token'ı Drift `sync_meta`'da (v6 additif; app-private sandbox yeterli, yeni native bağımlılık — secure_storage — codegen riski almaz); deviceId İLK girişte üretilir ve KALICIDIR (outbox/LWW device_id ile aynı kaynak); çıkış yalnız token'ı siler, iş verisi/sync imleci kalır (offline-first). Login ekranında kayıt/fiyat/abonelik çağrısı YOK (mağaza kuralı — regresyon testi yasak kelimeleri tarar). SyncService: periyodik 2 dk push+pull, hata uygulamayı durdurmaz. Salt-okunur kipte FAB kapalı (SubscriptionState.writable).
- **Drift widget-test dersleri (test 89/89'a giden yol):** (a) watch() akışları gerçek zamanda emit eder, testWidgets sahte-zamanda — akış-zamanlamalı senaryolar saf async test'e indirilir (sorgu mantığı `watchCustomers()` olarak ekrandan ayrıldı); (b) akış-abonelikli drift db'sini widget-test zonunda `close()` etmek ASILI KALIR (10 dk zaman aşımı yaşandı) — bellek-içi db testte kapatılmaz; (c) SnackBar/animasyon sayaçları için test sonunda ağaç boşaltılıp sahte saat ilerletilir. Testin bulduğu GERÇEK hata: '0532...' yazımı telefon aramasında eşleşmiyordu (baştaki 0 fragmanda kalıyordu) — normalize düzeltildi.
- **Vardiya 2026-07-22 (GERÇEK CİHAZ TESTİ — Samsung S24 FE + arkadaş cihazı): saha 2 GERÇEK hata yakaladı, ikisi de kök nedenden kapatıldı (161/161, +2 regresyon testi, APK cihazda).** (1) Faz 0 ekranının sqflite `version:1` açışı Drift damgasını ezip iki cihazı sonsuz-loading'e kilitliyordu → local_db.dart silindi, Phase0 AppDatabase'e taşındı, migration kendini-onarır yapıldı, açılış hatası ekrana basılır; (2) native sorgu taze kurulumda olmayan `c.address`'i okuyup her aramayı "kayıtsız" gösteriyordu → adres customer_addresses birincilinden. Kurulum/giriş/senkron zinciri gerçek cihazda KANITLANDI (sunucuya gerçek kayıt düştü); açılış+kart teyidi devralana kaldı. Test köprüsü: ayrık php -S + cloudflared tüneli (adres her başlatmada değişir — PLAN rehberi). Yeni pazarlıksız kural: sipario.db'ye version'lı ikinci açıcı YASAK.
- **Vardiya 2026-07-21/3 (4 ajanlı hat + lead devralması): 4b Dilim 4 bitti → 4b · Saha UI KAPANDI; kodla yapılabilir iş TÜKENDİ.** Kurye listesi sunucudan team bloğuyla (RLS altında, PII-asgari, SyncTeamTest kanıtlı); Drift v7 users aynası; yetkiler() K2 matrisi (tek kişilik bayide atama+kasa devri HİÇ render edilmez); kasa devri ekranı (beklenen=onizle(), fark kanıt); guzzle 7.15.1 (4 Dependabot uyarısı kapandı). Doğrulama: mobil 159/159 · API 174/174 · inceleme YEŞİL · APK ✓. Koordinasyon dersleri (ajan adı çakışması→TaskStop, zombi flutter_tester/sqlite3.dll tanısı, Get-Process ayrı sorgulama) PLAN tuzaklarına işlendi. Kalan işlerin tamamı dışsal: YAPILACAKLAR.md + PR #11 merge + gerçek cihaz + pilot.
- **Vardiya 2026-07-21/2 (4 ajanlı hat): 4b Dilim 3 bitti — defter hareket listesi + tahsilat + kupon satışı + ters-kayıt düzeltme + gün-sonu ekranı; 130/130 test, inceleme YEŞİL (kırmızı çizgi ihlali yok), APK derlendi.** Düzeltme yalnız ters kayıtla (UI'da güncelleme/silme yolu yok, testli kanıt); gün sonu tamamen salt-okunur; defter bölümü ayrı dosyada (<500 kuralı). Yeni test dersi: sahte-zamanda DÜZ drift Future sorgusu da asılır (runAsync şart). Kalan tek UI dilimi: Dilim 4 kurye+kasa devri (tek kişilik bayide gizli).
- **Vardiya 2026-07-21: 4b Dilim 2 bitti — sipariş akışı artık uçtan uca ekranda (liste/yeni sipariş/detay + teslim kapatma + ürün yönetimi), 109/109 test, APK derlendi.** Ödeme tipi teslimde sorulur; veresiye/kupon müşteri zorunlu; kupon eksiye düşebilir ama teslim reddedilmez; para sınırı tek yerde (`parseKurus`, 2 haneden uzun kuruş reddedilir). Ürün yönetimi kapsama alındı (taze kurulumda sipariş ekranı onsuz kullanılamıyordu). **Makine farkı notu: bu vardiya ASCII yollu ikinci geliştirici makinesinde koştu — Türkçe-yol tuzakları burada yaşanmadı.** Sıradaki kod işi Dilim 3 (defter/tahsilat/gün sonu).
- **Vardiya 2026-07-17/2 KAPANIŞ: Flutter bu makineye kuruldu ve mobil doğrulama partner beklemeden kapandı (89/89); pano dürüstleştirildi (%79→%68, "4b Saha UI" %15 — UI eforu hiçbir fazda sayılmıyordu, kullanıcı yakaladı); 4b Dilim 1 bitti (giriş/oturum/kabuk/müşteri, Drift v6, mağaza-kuralı regresyon testli) ve Dilim 1'li debug APK kullanıcıya teslim edildi.** Sıradaki kod işi Dilim 2 (sipariş ekranları); telefon gelirse önce gerçek-cihaz doğrulaması (journal_mode/native + arayan tanıma). PR #11 merge insanda. Tuzaklar PLAN "Bilinen tuzaklar"da (Türkçe-yol komut sırası, drift widget-test dersleri, yetim dart süreci sqlite3.dll kilidi, telefondan API erişimi 0.0.0.0).

## Tasarım sistemi (yeniden UI — Claude Design handoff, 2026-07-23)

- **Merkezî tema katmanı `lib/theme/` kuruldu; ekranlarda ham renk/ölçü/font YASAK — her şey token'dan gelir (`DESIGN_SYSTEM.md` özet).** `tokens.dart` (SipColors/SipRadius/SipSpace) + `typography.dart` (SipText + TextTheme) + `app_theme.dart` (SipTheme.dark → M3 ThemeData) + `components/`. Gerekçe: tek yüzey; bir token değişince tüm uygulama takip eder (handoff'un "Tweaks"iyle aynı mantık — vurgu/köşe tek yerde).
- **Font IBM Plex Sans `assets/fonts/`'a GÖMÜLDÜ (google_fonts değil) — 400/600/700, OFL.** Gerekçe: offline-first (kırmızı çizgi #3) runtime font indirmeyi reddeder; saha cihazı hiç iyi bağlanamamışken bile doğru font çizilmeli. Medium/500 gömülmedi (handoff'ta kullanımı az; Flutter en yakınına düşer). TTF'ler makine-yerel OFL kopyası — kanonik sürümle değiştirilebilir.
- **İkonlar Flutter yerleşik `Icons.*` (Material Symbols Rounded değil) — bilinçli sapma.** Değişken font + kod-noktası gömmenin ağırlığı yerine görsel olarak çok yakın yerleşik ikonlar; seçili sekmede dolu varyant. Koyu tema tek tema (handoff koyu-tek).
- **`BalanceBadge` ORTAK bileşen (liste + çağrı popup'ı aynı dil): + borç dolgulu kırmızı, 0 soluk, − sessiz yeşil.** Eski özel `_BalanceBadge` kaldırıldı; tutar hâlâ `formatKurus`'tan (tek para sınırı korunur).
- **Müşteri listesinde ikincil satır artık TELEFON (handoff: "isim büyük, telefon ikincil") — `watchCustomers` KORUNDU (testler ona bağlı), additive salt-okunur `watchCustomerRows` birincil telefonu LEFT JOIN'le ekledi.** Ürün zaten arayan-tanıma; telefon listede olmalı. `formatPhoneTR` görüntü biçimi ("+90…"→"0532 771 08 63"); telefon yoksa not'a düşer. Durum yönetimi/akış deseni bozulmadı; yalnız görsel + additive sorgu.
- **Ekran 1 (Müşteriler) + tema temeli bitti; doğrulama: `dart analyze` 0 · `flutter test` 161/161 · debug APK derlendi.** FloatingActionButton tipi + salt-okunur SnackBar metni korundu (testler yeşil). Codegen GEREKMEDİ (şema değişmedi). Sıradaki: Ekran 2 Siparişler (segment filtresi + sipariş kartı bileşenleşecek).
- **Ekran 2 (Siparişler) bitti: `SipSegmented` (segment filtresi) + `SipEmptyState` ORTAK bileşenleşti (Müşteriler de SipEmptyState'e geçti); sipariş kartı = müşteri + ürün özeti + durum rozeti (Açık/Teslim/İptal) + saat·ödeme + tutar.** Ürün özeti additive `watchOrderItemsSummary` akışından (`watchOrders` sözleşmesine DOKUNULMADI — testli; `initialData:{}` listeyi geciktirmez). Kurye "Benim" sekmesi, FAB tipi ve salt-okunur metni korundu. Doğrulama: analyze 0 · test 161/161 · APK. Sipariş form/detay handoff'ta yok — temayı miras alır, elle çizilmedi. Sıradaki: Ekran 3 Gün sonu.
- **Vardiya 2026-07-23 KAPANIŞ: tema temeli + Ekran 1-2 (Müşteriler/Siparişler) bitti ve doğrulandı (161/161 + APK); Ekran 3 Gün sonu için ultracode tasarım-paneli workflow'u başlatıldı ama vardiya kapanınca DURDURULDU — kod üretilmedi, Gün sonu repo'da eski hâlinde.** Karar: özet bir ekran için 3-önerili panel gereksiz; sonraki kişi Gün sonu'nu DESIGN_SYSTEM.md + Ekran 1/2 kart desenini doğrudan izleyerek uygulasın. Korunacak başlıklar: "Kasa (bugün)"/"Veresiye (açık borç)"/"Kupon" (testli). Kalan ekranlar: Gün sonu → Menü → Ürünler → Gelen Çağrı Popup (bağımsız overlay widget, 2 varyant).

## Tasarım boşluğu — veri katmanı (backend, 2026-07-25/26)

- **Çağrı günlüğü SENKRONLANIR (cihaz-yerel DEĞİL).** Gerekçe: çok cihazlı bayide patron, operatörün/kuryenin aldığı çağrıyı görmek ister; yerel kalsaydı "Son Aramalar" her cihazda farklı olur ve gün-içi kullanım (churn sinyali, BRIEF yönetim paneli) ölçülemezdi. KVKK açısından yeni bir sınır AÇILMIYOR — numara zaten `customer_phones`'ta TR sunucuda; log/crash raporuna asla yazılmaz. `call_logs` append-only DEĞİL (kırmızı çizgi #2 para/hareket tablolarını kapsar): `outcome`/`customer_id` çağrıdan sonra zenginleşir, bu yüzden standart LWW + tombstone. Çağrının `occurred_at`'i LWW damgasından AYRI tutulur — sonuç sonradan yazılınca "Son Aramalar"daki saat kaymaz (testli).
- **Serbest sipariş satırı AÇIK bayrakla ayrılır: `order_lines.is_custom`.** `product_id IS NULL` ayırt edici olarak KIRILGAN — silinmiş ürünün satırı da null olur ve katalog satırı bir gün sessizce "serbest" görünmeye başlardı. Tasarım bu ikisini ayrı gösterdiği için sözleşme açık bayrak olmalı. `unit` da satırda saklanır (`unit_price`/`product_name` deseni: siparişin çekildiği ANDAKİ gerçek, ürünün bugünkü birimi değil).
- **`products.barcode` tenant içinde TEKİL DEĞİL (unique kısıt YOK, yalnız `(tenant_id, barcode)` indeksi).** Çevrimdışı iki cihaz aynı barkodu girerse unique ihlali olayı REDDEDER ve kullanıcının kaydı KAYBOLURDU; offline-first (kırmızı çizgi #3) katı bütünlüğe üstün gelir. Mükerrer barkod yalnız okutmayı belirsizleştirir, veri bozmaz — UI uyarır. Aynı çizgi `exempt_numbers.phone_last10` için de geçerli. Test bu toleransı sabitliyor (iki ürün aynı barkodla `applied`).
- **Kurye telefonu `team` bloğuna EKLENDİ — PII-asgari ilkesiyle çelişmiyor.** O ilkenin amacı MÜŞTERİ verisini ve KİMLİK yüzeyini (e-posta/parola/oturum) payload dışında tutmaktı; kurye telefonu bayinin KENDİ personel iletişim bilgisidir, patron girer ve kuryeyi aramak için gerekir (tasarım: Kuryeler ekranı). Payload TAM OLARAK `{id,name,role,status,phone}` — `SyncTeamTest` katı eşitlikle sabitler, yeni bir kolon sızarsa test kırmızı yanar. `user_profile` olayı yalnız ad/telefon/aktiflik yazar: kullanıcı OLUŞTURULAMAZ, rol/e-posta/parola DEĞİŞTİRİLEMEZ (yetki yükseltme vektörü kapalı, testli).
- **`tenant_settings`in birincil anahtarı `tenant_id` (surrogate uuid YOK).** İki cihaz çevrimdışıyken kendi uuid'siyle profil oluştursaydı `unique(tenant_id)` ihlali bir olayı reddeder ve veri kaybettirirdi; tenant_id'yi anahtar yapmak çakışmayı YAPISAL olarak imkânsız kılar — iki yazım aynı satırda LWW ile birleşir (eski olay `stale`, asla `rejected`). İstemci payload'a id koymaz; "Firma Kodu" burada değil, `tenants.slug`tur (sunucu sahipli, `subscription.tenant_code` ile yayınlanır — `route_credits` de aynı kanaldan).
- **`day_closings` APPEND-ONLY, veritabanı seviyesinde zorlanır (migration 607, `ledger_entries`/`cash_handovers` deseni).** Kapanış bir mutabakat KANITIDIR: sayılan/beklenen/fark ancak ezilemezse anlam taşır; yanlış kapanış YENİ kapanış kaydıyla düzeltilir. Özet tutarlar satırda dondurulur — sonradan gelen geç senkron bugünün toplamını değiştirse de arşiv değişmez, yoksa "kapatıldı" ifadesi anlamını yitirir. `AppendOnlyLedgerTest` veri sağlayıcısına eklendi (app rolü UPDATE/DELETE'te 42501 alır).
- **`orders.sort_index` bir ÖNBELLEKTİR; kaynağı `sort_set` order_event'idir (`assigned_user_id` deseninin ikizi).** Aynı `(occurred_at DESC, id DESC)` anahtarıyla türetilir → iki cihaz aynı olay kümesinden AYNI sırayı üretir, ıraksama olmaz.
- **Drift kendini-onarma işareti `users`'tan `tenant_settings`e taşındı — bu bir sürüm-bakım kuralıdır: işaret HER şema sürümünde EN SON eklenen tabloya güncellenmelidir.** Güncellenmeseydi v7 damgalı saha cihazı "zaten güncel" sanılıp v8 adımını ATLAR ve eksik tabloyla açılırdı. Yeni `v7→v8` testi (gerçek v7 dosyası: v8 tablo/kolonları geri sarılıp `user_version=7` damgalanır) bu regresyonu tutar — v1→v8 yolu `from >= 2` / `from >= 7` dallarını HİÇ koşmadığı için tek başına yetmiyordu.
- **RLS artık migration okumakla değil FİİLEN sınanıyor: `RlsDesignGapTest` dört yeni tabloda iki gerçek tenant bağlamı kurup sorgu atar** (bağlamsız 0 satır · her bayi yalnız kendi satırı · B'nin gerçek id'si A bağlamında bulunamaz · cross-tenant UPDATE/DELETE 0 satır ya da 42501 · yabancı `tenant_id` INSERT'i WITH CHECK ile 42501 · `pg_class`'tan FORCE RLS doğrulaması). Sipario_app NOBYPASSRLS olduğu için test yeşil yanan bir yalan olamaz.
- **Doğrulama (2026-07-26): Postgres migration'ları koştu (601–607, batch 3) ve test paketinin `migrate:fresh`'i sıfırdan sıra bağımlılığını kanıtladı; API 220/220 (760 assert, önceki 174'ten +46) · pint temiz · phpstan 0 · mobil `dart analyze` 0 · `flutter test` migration 4/4 + repository/sync_engine/outbox/db_smoke/ledger_coupon/courier 41/41. UI'ya sunulan yeni repo yüzeyi (imzalar donduruldu): `TenantSettingsRepository.watch()/get()/save({businessName,ownerName,phone,whatsapp,addressText,taxOffice,taxNumber,opensAt,closesAt,receiptNote})` · `ExemptNumberRepository.watchAll()/isExempt(rawPhone)/add({phoneE164,label})/remove(id)` · `CallLogRepository.watchRecent({limit})/log({phoneE164,direction,customerId,outcome,relatedOrderId,occurredAtIso})/setOutcome(id,{outcome,relatedOrderId})` · `DayClosingRepository.watchArchive({limit})/kapaliMi(scope,{userId,localDate})/onizle(...)→ClosingOnizleme/kapat({scope,userId,countedCashKurus,note,toUserId,alsoHandover,localDate})`.**
- **Çağrı kartının native tarafı çağrıyı `call_logs`'a DOĞRUDAN yazmaz: düz metin bir kuyruğa (`sipario_cagri_kuyrugu.txt`, `<iso8601-utc>|<yon>|<haneler>`) ekler, uygulama açılınca `CagriKuyrugu.bosalt()` onu `CallLogRepository.log()` ile normal yoldan tabloya geçirir.** Yazma yolu daima repo → outbox olduğundan, native'in tabloya elle satır atması outbox/LWW/kimlik sözleşmesini ikinci bir yerde kopyalamak ve ilk şema değişiminde sessizce bozulmak demekti; ayrıca tek `appendText` çağrısı, çağrı anındaki 1 sn bütçesine SQLite açmaktan çok daha ucuz. Muaf numara kontrolü (`CallerCard.muafMi`) `CustomerLookup.find`'dan ÖNCE koşar ve `CustomerLookup.openForRead`'i yeniden kullanır (WAL kurtarma düşüşü tek yerde); `exempt_numbers` yoksa `false` döner — kart susmaz, çünkü arayan tanıma ürünün kendisidir, muaf listesi yalnız bir rahatlıktır. Native tema aynası `FlutterSharedPreferences` yerine `tema_deposu.dart`'ın gerçek kaynağından (`getDatabasesPath()/sipario_tema.txt`) okur; eskisi hiç var olmayan bir anahtara baktığı için koyu temada BEYAZ kart çiziyordu.
- **İşletme ekranlarının geçici bellek-içi depoları (`isletme_deposu.dart`: `IsletmeDeposu`/`MuafDeposu`/`KuryeDeposu`/`GunKapanisDeposu`) SİLİNDİ; ekranlar doğrudan repo'lara bağlandı** (`TenantSettingsRepository`, `ExemptNumberRepository`, `CourierRepository`, `DayClosingRepository`). Şema geldiği anda köprüyü bırakmanın nedeni yalnız temizlik değil: muaf liste bellekte tutulduğu sürece çağrı kartı kapısı (native `CallerCard.muafMi`, `exempt_numbers`'a bakar) ile ekranın gösterdiği liste İKİ AYRI gerçek olurdu — kullanıcı numarayı ekler, kart yine açılırdı. Aynı gerekçeyle gün sonu arşivi de `day_closings`e taşındı; ekranın gösterdiği kapanış ile arşivdeki kayıt tek koddan çıkar. **Kuryeler ekranında "yeni kurye ekle" BİLİNÇLİ olarak yok:** `CourierRepository` kullanıcı oluşturamaz (kimlik yüzeyini senkron yolundan açmak yetki yükseltme vektörü), hesap açma panel tarafındadır; işini yapamayacak düğme yerine nedenini söyleyen bilgi bloğu konuldu. Ürün formundaki barkod/görsel köprüleri (`urunBarkodu`/`urunGorseli` null döndüren saplamalar) gerçek kolonlara bağlandı — `update` tüm alanları yazdığı için düzenlemede `imageUrl` geri verilir, yoksa sunucudan inen görsel sessizce silinirdi.
- **Çağrı geçmişi listesi (`lib/screens/cagri/cagri_gunlugu.dart`) TASARIM DIŞI bilinçli bir eklemedir; tasarımdaki tek çağrı geçmişi yüzeyi ana ekrandaki "Son Arama" bento kutusudur (`s-ana.jsx:45`, verisi `sonAramaAkisi(db)`).** `call_logs` zaten geçmiş tuttuğu ve "kim aramıştı" sorusu makul olduğu için tam liste eklendi; erişim ANA EKRANDAN DEĞİL Ayarlar'dan verilir — bento kutusunun tek dokunuşu tasarımda zaten dolu (`s-uygulama.jsx:90`: kayıtlıysa müşteri detayı, kayıtsızsa çağrı kartı) ve üstüne uzun basma bindirmek keşfedilemez bir jest icat etmek olurdu. Liste `s-bugun.jsx`e DEĞİL güncel `.akt-row` kalıbına (Sipario.html 95–102) göre çizilir: **`s-bugun.jsx` tasarımın terk edilmiş bir ara sürümüdür** — `Sipario.html` onu yüklemiyor, kullandığı sınıfların (`ara-row`, `bg-selam`, `hero-v`…) hiçbiri CSS'te yok ve eski jeton adlarına (`--vurgu`, `--borc`) atıf yapıyor; referans alınmaz.
- **Tasarım jetonları `static const SipColors`tan `SipTokens` adlı bir `ThemeExtension`a taşındı; ekranlar `context.sip.surface` diye okur.** SİPARİO 3.0 iki temalı (açık varsayılan + koyu) ve tema ÇALIŞMA ANINDA değişiyor — sabit alanlar bunu taşıyamazdı. Metin stilleri (`SipText`) bilinçli olarak RENKSİZDİR: renk `DefaultTextStyle`tan miras alınır, böylece aynı stil açık temada, koyu temada ve daima-koyu "hero" blokları içinde doğru çalışır; hero üstü katmanlar tema-bağımsız statiklerdir (`SipTokens.onHero*`). Ekranda ham renk/punto YASAK — bir değer jetonda yoksa doğru tepki jetona eklemektir, ekrana gömmek değil.
- **Lucide ikonları için SVG paketi EKLENMEDİ; `svg_path.dart` içinde ~150 satırlık kendi ayrıştırıcımız var.** Tasarım ikonları ham `d` verisi olarak taşıyor; paket eklemek offline-first'e bir bağımlılık ve çalışma-anı ayrıştırma maliyeti bindirirdi, 44 ikonu elle `Path` çağrısına çevirmek ise hataya açıktı. SVG yay komutları Flutter'ın `Path.arcToPoint`'ine BİREBİR eşleştiği için elle bezier dönüşümü gerekmedi; sonuç `Path` önbelleğe alınır, maliyet tek seferliktir.
- **Fontlar DEĞİŞKEN (variable) font olarak gömüldü ve ağırlık `fontVariations: [FontVariation('wght', N)]` ile verilir.** Sora ve Hanken Grotesk Google Fonts'ta yalnız değişken olarak yayınlanıyor. Tek dosyalı bir ailede yalnız `fontWeight` vermek ETKİSİZDİR — Flutter bunu dosya seçimi sayar, tüm ağırlıklar aynı çizilir ve tipografi sessizce düzleşir. Bu bozukluk widget testlerinde görünmez (test ortamı sahte font kullanır), yalnız cihazda fark edilir; `test/font_variable_test.dart` gerçek TTF yükleyip aynı metni farklı ağırlıklarda ÖLÇEREK ekseni kanıtlar (kontrol grubu dahil).
- **Türkçe büyük/küçük harf için `trBuyuk()`/`trKucuk()` zorunlu; `toUpperCase()` yasak.** Dart'ın metotları yerelden bağımsızdır: `'işletme'.toUpperCase()` → `IŞLETME`. Tasarımda form etiketleri, bölüm başlıkları ve rozetler büyük harf olduğu için bu her ekranda görünür bir kusurdur; avatar baş harflerinde ("irfan" → `IY`) ve arama karşılaştırmalarında ise sessizce YANLIŞ SONUÇ üretir.
- **`OrderFormScreen.writable` ZORUNLU yapıldı (varsayılan `= true` kaldırıldı).** Yeni sipariş girişi sipariş listesinden uygulama kabuğuna taşınırken üç çağrı yerinin üçü de bayrağı geçmeyi atladı ve varsayılan bunu örttü: abonelik kilidi açıkken sipariş girilebiliyordu. Salt-okunur bir kırmızı çizgi olduğuna göre, kapının unutulabilir olması kabul edilemez — zorunlu parametre derleyiciyi bekçi yapar.
- **Sözleşme testlerinden STİL KİMLİĞİ iddiaları (`widget.style == SipText.x`) kaldırıldı; yerine davranış iddiaları kaldı.** Tasarım sistemine her dokunuşta ekran testleri kırılıyordu ama hiçbir davranış korunmuyordu. Tipografi/jeton sözleşmesi artık merkezî olarak `test/ui_temel_test.dart`te sınanır; ekran testleri "AppBar yok", "borç kırmızı görünür", "salt-okunurda kayıt oluşmaz" gibi anlamı olan şeyleri sabitler.
- **`SipIcon` ilk parametrede hem sözlük ANAHTARINI hem doğrudan PATH verisini kabul eder — ve bunun testi PİKSEL SAYAR.** Bir sürüm boyunca yalnız `SipIcons.hepsi[ad]` sözlüğüne bakılıyordu; oysa `SipIcons.phone` bir anahtar değil path'in kendisidir, arama `null` dönüyor ve widget sessizce boş kutu çiziyordu → UYGULAMADAKİ HİÇBİR İKON GÖRÜNMÜYORDU. Testler bu hatayı GEÇTİ, çünkü ölçütleri "yol ayrıştı" ve "widget çökmedi"ydi; ikisi de doğruydu. Ders: çizim yapan bir bileşende sözleşme "istisna atmıyor" değil, "ekrana piksel düşüyor" olmalıdır — `test/icon_paint_test.dart` ikonları `RepaintBoundary.toImage()` ile gerçekten boyayıp hücre hücre piksel sayar (hem anahtar hem path biçimiyle) ve düzeltme geri alındığında kırmızıya döndüğü doğrulanmıştır. NOT: `toImage()` gerçek async'tir, `tester.runAsync` içinde çağrılmazsa test assertion'da değil ZAMAN AŞIMINDA düşer ve hata sanki başka bir şeymiş gibi görünür.
- **Kaynak doğrulandı: `design_handoff_v2/` GÜNCEL, `Sipario - Standalone.html` yeni bir tasarım DEĞİL.** MCP (`DesignSync`) ile claude.ai/design projesi ("Sipario APP Reesign") okundu: uzak `s-ana.jsx` yereldekiyle bayt bayt aynı; Standalone ile `Sipario.html`in CSS sınıf kümeleri BİREBİR eşit (390 = 390, sıfır fark) ve jeton değerleri aynı — Standalone yalnızca aynı tasarımın tek dosyaya gömülmüş (inline) sürümü. Dolayısıyla "eski tasarım uygulandı" şüphesi kaynakta değil, ENTEGRASYONDA doğruydu: ekranlar doğruydu ama birbirine bağlanmamıştı (aşağıya bak).
- **Paralel ajanların en pahalı hatası dosya değil BAĞLANTI kaybıdır: çekmece, Ayarlar/Kuryeler/Muaf/İşletme-Profili ekranları yazılmadan ÖNCE yazıldı ve sonradan hiç güncellenmedi.** Sonuç: dört ekran kodda tamdı, testleri geçiyordu, ama uygulamada HİÇBİR YERDEN açılamıyordu (`Ayarlar` bir merkezdi — diğer üçü yalnız ona bağlıydı, dolayısıyla dal komple ölüydü). Ayrıca kabuk `onMenu`yu yalnız ana ekrana geçtiği için çekmece diğer üç sekmede hiç açılamıyordu. `dart analyze` ve 309 testin hiçbiri bunu göremez — "tanımlı ama hiçbir yerden referans verilmemiş ekran" taraması ve CİHAZDA gezme gerekir. Regresyon testleri eklendi (dört sekmeden de çekmece açılır · çekmecede Kuryeler/Muaf/Ayarlar girişleri vardır).
- **Durum çubuğu stili KÖKTE (`main.dart` `MaterialApp.builder`) kurulur, kabukta değil.** Yalnız kabuk ayarladığında, üstüne push edilen ekranlar kabuğun ana ekranın koyu hero'su için beyaza çevirdiği ikonları miras alıyor ve açık temalı ekranlarda saat/pil okunmuyordu. Kök varsayılanı temaya göre verir; koyu hero'su olan ekranlar kendi `AnnotatedRegion`'ıyla geçersiz kılar.
- **Ayarlar'daki tema anahtarı `ValueListenable` alır, düz `bool` DEĞİL.** Ekran `Navigator.push` ile açıldığından kabuk tema değişiminde yeniden çizilse bile bu rota anlık kopyasıyla kalıyordu: tema aydınlığa dönüyor ama anahtar "açık" konumunda takılı kalıyordu (cihazda görüldü). Aynı tuzak, push edilmiş her rotaya değer kopyası geçen yerlerde geçerlidir.
- **Tasarımın YEREL KOPYASI TUTULMAZ — tek kaynak canlı Claude Design projesidir (`a4ab826a-d312-4313-96be-e66519b64fce`, handoff `design_handoff_sipario/`, `DesignSync` MCP ile okunur).** `design_handoff/` ve `design_handoff_v2/` silindi. Gerekçe: aynı hata iki kez yapıldı — kopya depoya konuldu, tasarım güncellendi, kopya bayatladı ve iki vardiya boyunca YANLIŞ kaynak doğru sanıldı. Kopya tutmak "kopyayı güncel tutacağım" sözüdür; bu söz tutulamadı. Kod yorumlarında artık klasör yolu değil dosya adı anılır (`tasarım s-siparisler.jsx`), ölü 31 referans temizlendi.
- **Mobil giriş kimliği E-POSTA DEĞİL, FİRMA KODU + KULLANICI ADIDIR (tasarım `s-giris.jsx`).** `users.username` tenant içinde tekildir (global değil — iki bayide de "patron" meşrudur, ayrımı firma kodu yapar), `tenants.slug` artık ZORUNLUdur; ikisi de DB'de CHECK ile tasarımın regex'ine bağlanmıştır. Gerekçe tasarımın kendi metnidir: bayinin kuryesi/tezgâhtarı çoğu zaman e-posta sahibi değildir, hesabını patron açar. **E-postalı `sipario_login_lookup(text)` KALDIRILMADI:** abonelik web sitesi (Faz 5) onu kullanır — iki AYRI ve meşru giriş yüzeyi vardır, karıştırılmamalıdır. Eski yüzeyin mobile geri sızmadığını `AuthFlowTest::eposta_ile_giris_artik_kabul_edilmez` sabitler.
- **Giriş hız sınırı anahtarı kullanıcı adı değil (firma kodu + kullanıcı adı) ÇİFTİdir.** Tek başına kullanıcı adıyla anahtarlamak, "patron" her bayide bulunduğu için bir bayiye kaba kuvvet uygulayan saldırganın BÜTÜN bayilerin girişini kilitlemesine yol açardı — güvenlik önlemi hizmet reddine dönerdi.
- **"Oto Sırala (rota)" AYRI bir uç noktadır (`POST /orders/auto-route`) ve siparişlere YAZMAZ.** İki gerekçe: (a) kontör (`tenants.route_credits`) sunucu sahiplidir — çevrimdışı bir istemci hak üretememeli; (b) sonuç bir SIRA ÖNERİSİdir, kayıt değil — sıra yine tek yazma yüzeyinden (`sync push` → `sort_set` olayı) geçer, offline-first ve LWW semantiği bozulmaz. Sayaç düşümü satır kilidiyle (FOR UPDATE) aynı transaction'dadır; sıralanacak geçerli sipariş yoksa kontör YANMAZ; hak bittiğinde 409 ve mesaj mağaza kuralı gereği NÖTRdür (satın alma/fiyat çağrısı yok).
- **Rota algoritması en-yakın-komşudur ve LİSTENİN İLK DURAĞINI KORUR; depo (dükkân) konumu uydurulmaz.** Şemada dükkân koordinatı yoktur — başlangıcı tahmin etmek yerine kuryenin mevcut ilk durağı sabit tutulur. Koordinatı olmayan siparişler rotaya giremez ama KAYBOLMAZ: göreli sıraları korunarak sona alınır ve sayıları kullanıcıya SÖYLENİR ("N sipariş konumsuz, sona alındı") — sessizce sona atmak "sıraladım" demeyi yalan yapardı.
- **Kalan hak bilinmiyorsa "Oto Sırala" düğmesi HİÇ ÇİZİLMEZ (uydurma sayı gösterilmez).** Kullanıcıya "34 hakkınız var" deyip tıkladığında 409 aldırmak, sayaca olan güveni bir kerede bitirir. Aynı ilkeyle çekmecedeki kontör kartı, aylık kota sunucudan gelmediyse çizilmez (oran hesaplanamaz).
- **Gün hesabı, kuryelerin BİR KISMI hesabını kapatmışken kapatılamaz (tasarım `gunEngel`).** Hiç kimse kapatmamışken engel YOKtur (kurye hesaplarını kullanmayan bayi gün sonunu tek dokunuşta kapatabilmeli), hepsi kapanmışsa da yoktur; engel yalnız YARIM KALMIŞ devirde çıkar — kapanan gün açık bir kurye kasasını mutabakatsız bırakırdı. Mevcut "açık sipariş" engeliyle birlikte iki bağımsız kapı olarak çalışır.
- **SUNUCU SAHİPLİ sync_meta alanları (abonelik · firma kodu · rota kontörü) TEK ATIŞ OKUNMAZ, `AppDatabase.watchSyncState()` AKIŞIYLA okunur.** Bu alanlar giriş yanıtında GELMEZ; ilk senkronda yazılır. `initState`te bir kez okuyan ekran girişten hemen sonra varsayılanı (0) görür ve orada donar — cihazda "Oto Sırala · 0 hak" yazarken sunucuda 34 vardı, çekmece de oto sıralama kontörü düştükten sonra "34 hak"ta kaldı. İkisi de `dart analyze` ve 318 testten geçmişti: ölçüt "ekran çizildi" değil **"değer değişince tazelendi"** olmalı (icon_paint_test dersinin aynısı; regresyon testi eklendi). Aynı sebeple kabuktaki ikinci tazeleme yolu (`_git()` dönüşünde elle okuma) kaldırıldı — iki yol tutmak ikisinin ayrışması demektir.
- **KUPON ÜRÜNDEN TAMAMEN KALDIRILDI (2026-07-26) — UI + Drift v10 + Postgres + sync applier; `orders.payment_type` artık yalnız nakit/kart/havale/veresiye kabul eder.** Tasarım kararı kaynaktan geldi: `s-*.jsx`lerin hiçbiri kupon çizmiyor, `ODEME_TIPLERI` dört tip, CSS'teki `.gs-kupon`/`.md-kupon` yalnız ARTIK olarak duruyor. Kullanıcı TAM SİLME onayı verdi. Silinenler: mobil `coupon_repository.dart` + `CouponMovements`/`CouponBalances` tabloları + `writeCouponMovement`/`recomputeCouponBalance`/`watchCouponBalance`/`kuponDurumu` + kupon sheet'i/çipi/gün-sonu bölümü + `RolYetkileri.kuponSatisi`; sunucuda `CouponMovement`/`CouponBalance` modelleri, `CouponChangeApplier`, snapshot anahtarları, `entity_type=coupon` ve YALNIZ kupona ait `grant`/`use`/`correction` OP'ları (defterin `correction`ı bir op değil `entry_type` payload alanıdır — o kaldı), panel export tabloları ve demo seeder kupon bloğu. **Drift'te DROP, `if (from < 10)` bloğuna DEĞİL kendini-onarma kapısından ÖNCE koşulsuz konuldu:** kapı `tenant_settings` varsa "şema güncel" sayıp erken döner ve v9 damgalı bir cihazda o tablo ZATEN vardır → koşula alınsa adım sessizce atlanır, tablolar sonsuza dek kalırdı; v10 tablo EKLEMEDİĞİ için kapının "en son eklenen tablo" işareti de güncellenemiyordu (regresyon testi: `migration_test.dart` v9→v10). **Postgres'te CHECK daraltmasından ÖNCE kalan `payment_type='kupon'` satırları NULL'a çekilir** — yoksa migration düşerdi; bu veri kaybı DEĞİLdir: kuponla teslim hiç para hareketi üretmiyordu (tek ledger satırı bile silinmedi, kırmızı çizgi #2 el değmedi) ve "kuponla teslim edildi" olgusu APPEND-ONLY `order_events` payload'ında AYNEN durur — `orders.payment_type` o olay günlüğünden türeyen bir önbellektir. Kupon SAHA GERÇEĞİ olarak `BRIEF.md`de kalır (madde silinmedi, "KALDIRILDI" notuyla işaretlendi): esnafta hâlâ yaygın, geri gelirse gereksinim orada yazılı. Migration `down()` ŞEMAYI (tablolar + RLS/policy + grant/revoke + geniş CHECK) geri kurar ama SATIRLARI geri getiremez — dosyada açıkça yazılıdır.
- **Girdi yüksekliği `InputDecoration.constraints` ile DEĞİL `contentPadding`ten TÜRETİLİR; ölçünün tek kaynağı `SipInputOlcu`, ölçüler `test/ui_olcu_test.dart`te kilitli.** Cihazda görülen kusurun kök sebebi ölçülerek bulundu: `constraints` yalnız dekoratörün ETRAFINA `ConstrainedBox` sarar (`input_decorator.dart:2673-2676`), BOYANAN kutunun yüksekliği ise `contentPadding.vertical + inputHeight`tir (`:1107-1123`) ve kutu üste yapışık çizilir (`:1392-1401`). `isDense: true` + `vertical: 0` ikilisi kutuyu satır yüksekliğine çöktürdüğü için 46 px'lik yuvanın üst ~20 px'i input, altındaki ~26 px ÖLÜ BOŞLUK oluyordu — 37 çağrı yerinin hepsinde. Satır çarpanı `style` VE `hintStyle`a birlikte verilir (yoksa kutu yazmaya başlayınca zıplar); `visualDensity` SABİTLENİR (platform varsayılanı masaüstünde `compact` = −2 px, 46'yı sessizce 44 yapardı).
- **Ölçü sınamasında ölçüt "kutu 46 mı" DEĞİL, "boşluklar simetrik mi" olmalıdır.** Naif `getSize == 46` düzeltme geri alındığında da YEŞİL yanar — çünkü `size = constraints.constrain(...)` dıştaki yuvayı ölçer; `InputDecorator`ın render kutusu bile 46 döner ve boyanan kutu (`_BorderContainer`) private'tır. `ui_olcu_test.dart` bu yüzden BOŞLUK ölçüyor: `metin.top − kutu.top == 14` ve `kutu.bottom − metin.bottom == 14`. `icon_paint_test` dersinin aynısı: testin neyi kanıtladığına bak, "çizildi/ölçüldü" ile "doğru çizildi/ölçüldü" ayrı şeylerdir.
- **Tasarım denetiminde "uygulamada var, tasarımda yok" bir GEREKÇE değil BULGUdur.** 2026-07-26 denetiminde kupon tam bu yüzden gözden kaçmıştı: fark ilk turda görülmüş ama "uygulamanın ekstrası, BRIEF'te var" denip geçilmişti. İşaret nettir: **CSS'te sınıf tanımlı ama hiçbir `s-*.jsx` onu çizmiyorsa özellik kaldırılmıştır** (`.gs-kupon`, `.md-kupon`, `.fabpop*`, `.md-bal*`, `.xiaomi-toggle`, `.siz-not`, `.mrow-av`, `.cagri-av`). Denetim üç başlık verir: EKSİK · FAZLA (=kaldırılmış) · YANLIŞ. Ters yön de kayda değer: JSX kullanıyor ama CSS'te kural YOK (`balrozet*`, `mrow-tag`, `ara-ic`) → ölçü kaynaktan çıkarılamaz, UYDURULMAZ, tasarımın güncellenmesi istenir.
- **DÜZELTME (aynı gün) — "kalan hak bilinmiyorsa Oto Sırala düğmesi HİÇ ÇİZİLMEZ" kararı GEÇERSİZ; düğme HEP çizilir, kullanılamadığında SAYISIZ + PASİF + GEREKÇELİ olur.** Yukarıdaki karar sahte sayı göstermeyi doğru biçimde yasaklıyordu ama yanlış çareyi seçmişti: düğmeyi hiç çizmemek YETENEĞİ GİZLİYOR — ilk senkrondan önce Sıralama sayfasını açan kullanıcı böyle bir özelliğin varlığını hiç öğrenmiyordu. Doğrusu üçüncü yol: etiket sayısız (`Oto Sırala (rota)`), `onTap: null`, altında nötr tek satır gerekçe (`Hak bilgisi bekleniyor — ilk senkrondan sonra kullanılabilir.` / `Salt-okunur kip: sıra kaydedilemez.` / `Oto sıralama hakkı kalmadı.`). Sahte sayı yasağı ve salt-okunur yazma kapısı aynen korunur. Genel ilke: **bir yeteneği gizlemek, o yeteneğin durumunu dürüstçe göstermekten daha kötüdür.**
- **Teslim sheet'inde ödeme tipi NAKİT ön-seçili gelir.** Eski davranış "hiçbiri seçili değil + düğme pasif"ti; gerekçesi append-only deftere yanlış kayıt düşmemesiydi. Ama nakit teslimlerin ezici çoğunluğu olduğu için bu her teslime bir fazla dokunuş bindiriyordu (telefonla sipariş alan bayide günde onlarca). Koruma başka yerde yeterlidir: tahsil edilecek tutar sheet'in tepesinde yazılıdır, kullanıcı "Teslim Et ve Kaydet" ile bilinçli onaylar, defterde düzeltme yolu vardır. Tasarım da (`s-siparisler.jsx:443`) nakit ön-seçili başlatıyor.
- **Ödeme tipinde GÖRÜNÜRLÜK ile SEÇİLEBİLİRLİK ayrı kurallardır ve TEK kaynaktan türer.** Müşterisiz (tezgâh) siparişte veresiye karosu listeden ÇIKARILMAZ — `opacity .45` ile pasif çizilir (tasarım `s-siparisler.jsx:620` `disabled`). Seçeneği gizlemek kullanıcıya "neden yapamıyorum" sorusunu bıraktırıyordu. `odemeTipleri` (çizilen, hep dört) ile `odemeTipiSecilebilir` ayrı ama aynı kuraldan üretilir; biri elle güncellenip diğeri unutulamaz (test bunu kilitler).
- **Tasarımdan BİLİNÇLİ SAPMALAR (2026-07-26 tasarım denetimi) — hepsi gerekçeli, hiçbiri gözden kaçma değildir:** (1) *Lütuf/salt-okunur bantları* — tasarımın `.app` içinde yalnız `.cbant` çevrimdışı bandı var; abonelik kipini kullanıcıya söylemek gerektiği için korundu (nötr metin, mağaza kuralına uygun). (2) *Gün sonunda "açık sipariş" kapatma engeli* — tasarımda tek engel `gunEngel`; kapanmış bir gün açık siparişi gizlerdi, veri bütünlüğü kapısı olarak korundu. (3) *Muaf numara silmede onay diyaloğu* — tasarım doğrudan siliyor; yanlış silme geri alınamadığı için onay korundu. (4) *Giriş ekranında üretim sunucu adresi ipucu* — tasarım `sipario.local` yazıyor; gerçek adres doğru olan, tasarımın regex'i port/şema kabul etmediği için ipucumuzu geçersiz kılardı. (5) *Ürün formunda GERÇEK barkod okuyucu* — tasarım yalnız simülasyon toast'ı basıyor; uygulama lehine sapma. (6) *Faz 0 gecikme ölçüm ekranının girişi yalnız `kDebugMode`da* — tasarımda yok ama arayan-tanımanın 1 sn bütçesini (kırmızı çizgi) ölçen tek araç; üretim menüsünde görünmez, geliştirmede erişilebilir kalır. Erişilemeyen dosya bırakmak da yasak (bkz. çekmece ölü dalı dersi).
- **`SipGovde` bir `ListView`tir, yani TEMBEL çizer — widget testinde katlamanın ALTINDAKİ metin `find.text` ile BULUNAMAZ.** Testin varsayılan yüzeyi 800×600 FİZİKSEL, `devicePixelRatio` 3.0 ile ~266×200 LOGİK pikseldir; `ListView` yalnız görünür alan + `cacheExtent` kadarını build eder. Ekrana bir bölüm eklendiğinde (bu vardiyada gün sonuna koşulsuz kapsam segmenti) altındaki içerik sınırın dışına kayar ve testler "Bad state: No element" ile düşer — ürün kodunda gerileme YOKken. Çözüm: gövde testlerinde yüzeyi büyüt (`tester.view.physicalSize = Size(800, 2400)` + `devicePixelRatio = 1.0`), ki bu desen depoda zaten vardı. Teşhis dersi: "metin ekranda görünüyor ama finder bulamıyor" ilk akla PARA/METİN BİÇİMİ farkını getiriyor; önce GÖRÜNÜR ALAN elenmelidir.
- **İç içe akış dinleyen ekranların widget testinde TEK tur bekleme yetmez.** Sipariş detayı dört akış (sipariş · satırlar · ekip · adresler), gün sonu ise kurye kapanış sorguları eklendikten sonra daha yavaş. Tek tur "başlık çizildi" aşamasına yetişir ama satır/ekip akışları henüz boştur: kalem dökümü `—`, kurye adı `null` görünür. Bu vardiyada ÜÇ ayrı test aynı sebepten düştü ve biri "kodda hata" sanıldı. Ayrıca `findsNothing` bekleyen testler ağaç hazır olmadan VAKUM geçer — yeşil görünür, hiçbir şey kanıtlamaz; onlara da bekleme konur.
- **Çağrı kartının EYLEMLERİ kartın kendi işi DEĞİLDİR; kart yalnız hangi düğmeye basıldığını söyler, gezinmeyi kabuk yapar — ve bu sözleşmenin İKİ ucu da ayrı ayrı test edilir.** Kart `CagriEylemi` enum'u ile pop eder (`cagri_karti.dart`), hedef ekranları TANIMAZ. Bu doğru bir katman ayrımıdır ama bir tuzağı vardır: kartın kendi testi (`ui_cagri_test.dart` — "eylemler doğru geri çağrıyı tetikler") YEŞİL kalırken çağıran taraf dönen değeri atarsa üç düğme de sessizce ölür. Cihazda tam bu oldu (2026-07-26): `home_shell._cagriKartiAc` kartı `await cagriKartiGoster(...)` ile açıp sonucu kullanmıyordu — "Sipariş Oluştur", "Defteri Aç" ve "Müşteri Olarak Kaydet" yalnız kartı kapatıyordu. Genel kural: **bir geri-çağrı sözleşmesinin yalnız ÜRETEN ucunu test etmek, sözleşmeyi test etmek değildir; TÜKETEN uç de kilitlenir** (`ui_cagri_eylem_test.dart`).
- **Native kart eylemi ile Flutter kart eylemi TEK gezinme koduna düşer; native taraf eylemi BEKLETİR, itmez.** Telefon çalarken kartı Kotlin çizer ve o an Flutter motoru yaşamaz — dokunuşu Dart'a itmek boşluğa konuşmaktır. `MainActivity` niyet ekstralarını `bekleyen` alanında tutar, Dart hazır olunca `sipario/cagri` kanalından ÇEKER, çekildiği anda native siler (iki kez uygulanamaz) ve niyetten de silinir (Activity yeniden kurulunca tekrarlanmaz). Uygulama zaten önplandayken yaşam döngüsü olayı doğmadığı için native ayrıca bir "dürtü" gönderir; dürtü VERİ TAŞIMAZ, yalnız "şimdi sor" der — böylece tek transfer yolu vardır. Kanal `sipario/phase0`dan AYRI: Dart tarafında bir kanal adında yalnız SON `setMethodCallHandler` yaşar, ortak kanal ileride sessizce koparılabilirdi.
- **Karttaki bir eylem, kartı da KAPATMAK zorundadır — ikisi tek işlemdir.** Native tarafta eylem düğmesi uygulamayı açıyor ama kartı kapatmıyordu: bayi kendi uygulamasını kartın altında görüyordu (cihazda görüldü). `CallerOverlay.kapat` üç yüzeyi birden kaldırır (overlay penceresi · kilit ekranı Activity'si · bildirim) ve `lastPhone`u temizler — yoksa çağrı yanıtlandığında `CallSessionWatcher` kartı yeniden gösterip bayinin az önce açtığı ekranın üstüne binerdi. Bayi eylemini seçtiyse o çağrı için kartın işi bitmiştir.
- **Platform kanalına dokunan kod, kanalın OLMADIĞI yerde çağrılmamalıdır — `flutter_test`in mesajcısı sahtelenmemiş kanala ne yanıt ne hata döndürür.** Ölçüldü: kanalı sahtelenmemiş tek bir `invokeMethod` testi 10 dakika zaman aşımına düşürdü, ve kabuk bu köprüyü AÇILIŞTA yokladığı için kabuğu kuran her widget testi arkasında askıda bir gelecek bırakacaktı (o testler yine yeşil geçer — sinsi olan bu). Çare `try/catch` değil KAPIDIR: `cagriKopruAcik = Platform.isAndroid`, köprüyü sınayan testler açar. `.timeout(...)` ile çözülemez, o da widget testinde "A Timer is still pending" üretir.
- **Barkod ikonu KAMERAYI açar; okunan kod doğrudan alana yazılır — arada elle doldurulacak ikinci bir yüzey YOKTUR** (kullanıcı kararı, 2026-07-26). Eski hâl iki yerde de "kesik çerçeveli kutu + elle giriş + kayıtlı barkod listesi" sunan bir ara sheet'ti (`barkod_okut_sheet.dart`, `barcode_sheet.dart`); ikisi de silindi, yerlerine tek yüzey geçti (`screens/barkod/barkod_kamera.dart`). Ürün formunda okunan kod barkod alanına, POS kataloğunda ARAMA alanına yazılır — POS'ta "input" o alandır ve süzgeç (`katalogSuz`) artık adın YANINDA barkodu da tarar, yoksa okuma başarılıyken katalog `"…" için sonuç yok` derdi. **DÜZELTME:** 2026-07-26 tasarım denetiminin "bilinçli sapmalar" listesindeki *"(5) Ürün formunda GERÇEK barkod okuyucu"* maddesi O GÜN YANLIŞTI — ortada kamera yoktu, elle giriş vardı; madde ancak bu vardiyadan sonra doğrudur.
- **Okunan barkodun KABUL KAPISI vardır ve harfli kodun rakamları AYIKLANMAZ** (`barkodKabulEt`). Kabul kuralı formunkiyle aynıdır: yalnız rakam, en az 8 hane. `"AB12345678"` → `"12345678"` üretmek okuyucuyu "daha yardımsever" yapmaz; yanlış ürüne bağlanmış bir barkod sonra sessizce YANLIŞ FİYAT keser. Okunamayan kod alana yazılmaz, kullanıcıya söylenir ve kamera okumaya devam eder. Okuyucu ayrıca yalnız perakende biçimlerini dinler (EAN/UPC/Code128/Code39) — kargo etiketi ya da QR yanlışlıkla alana düşmesin.
- **Kamera bir YETENEKTİR, bir ZORUNLULUK değil: izin verilmezse ya da cihazda kamera yoksa okuyucunun İÇİNDE elle giriş çıkar.** Mutlu yola adım eklemez (kamera çalışıyorsa hiç çizilmez) ama eski yeteneği de kaybettirmez — izin reddi barkod girmeyi imkânsız kılsaydı bu bir gerileme olurdu. Manifest'te `uses-feature ... required="false"`: kamerasız cihazlar mağazada uygulamayı görmeye devam eder. ML Kit modeli PAKETE GÖMÜLÜDÜR, çalışma anında indirme yapmaz — depoda internet olmayabilir, offline-first çizgisi korunur.
- **Uygulama ANA EKRAN'la açılır** (kullanıcı kararı, 2026-07-26) — tasarımın `s-uygulama.jsx` `useState('siparis')` başlangıcından bilinçli sapma. Gerekçe: Ana ekran yalnız bir karşılama değil, günün ÖZETİ; bento kutuları son aramayı, günün sayılarını ve kısayolları bir arada verir. Sipariş listesine oradan tek dokunuşla gidilir, tersi ise özeti hiç görmeden çalışmak demekti. Sapma `ui_kabuk_ana_test.dart` ile KİLİTLİDİR: tasarımı kaynak alan bir denetim bunu gerileme sanıp geri çevirebilir, test o dönüşü kırar.
- **Vardiya devri PLAN.md'nin başında, tek yerde ve ADIM ADIM yaşar; `YAPILACAKLAR.md` arşive düştü.** İki geliştirici nöbetleşe çalışıyor ve sohbet geçmişi paylaşılmıyor, yani devir notunun bulunabilirliği içeriği kadar önemlidir: `PLAN.md`'nin en başına "vardiyaya başlıyorsan doğruca şuraya git" işareti, `## Güncel durum` altına da "🔻 VARDİYA DEVİR NOTU + SIRADAKİ İŞLER" bölümü kondu. Her iş **NEDEN / KİMDE / ADIMLAR / BİTTİ SAYILIR / KANIT** beşlisiyle yazılır — "iyzico anahtarı al" gibi bir başlık, sırayı devralan kişiye ne yapacağını söylemez. `YAPILACAKLAR.md` 2026-07-16'dan beri güncellenmediği ve kapanmış maddelerle kaldırılmış kupon özelliğinden söz ettiği için başına BAYAT uyarısı düşüldü; iki dosya çeliştiğinde PLAN.md doğrudur. Proje `CLAUDE.md`'sine de "vardiyaya başlıyorum denince devir notunu özetle" satırı eklendi — hafızanın var olması yetmez, okunacağının GARANTİ olması gerekir.
- **Çağrı YÖNÜ tek bir tiple (`CagriYonu`) taşınır; kart, bildirim, ölçüm defteri ve çağrı günlüğü aynı kaynaktan okur.** Saha hatası "giden çağrılar gelen görünüyor"du ve kök neden tespitin YANLIŞ olması DEĞİLdi: `CallScreeningService` `callDirection`'ı doğru okuyordu ama hiçbir yüzeye ulaşmıyordu — `CallerCardViews.kt` üst şeride `"GELEN ÇAĞRI"` metnini SABİT yazıyor, `CallerOverlay.reshow()` ve `EXTRA_DIRECTION` taşımayan Activity niyeti yönü `"in"`e çiviliyordu (MIUI'de asıl görülen kart hep "gelen" derdi). Ders: **doğru hesaplanan bir değerin taşınmadığı yer de bir hatadır**; değeri üreten uç yeşilken tüketen uç sabit yazabilir (çağrı kartı eylem sözleşmesi dersinin aynısı). Tespit hâlâ yalnız `CallScreeningService` verisidir — `CallLog`/`PhoneStateListener` yasak izinler ister (kırmızı çizgi #6). Geçmiş listesinden kart açılırken yön YENİDEN ÇÖZÜLMEZ, satırdaki değer geçirilir: kayıt durumu (arayan sonradan müşteri olmuş olabilir) dokunma anında tazelenir ama yön çağrı ANINDAKİ gerçektir.
- **`DIRECTION_UNKNOWN` kartı ATLAMA sebebi değildir; cevapsız çağrı günlükte YENİ satır değil, aynı çağrının güncellenmiş hâlidir.** Yönü bilememek arayanı hiç göstermemeyi gerektirmez — kart her koşulda çizilir, yön izinsiz `AudioManager.mode` ile tahmin edilir. Cevapsız için native anahtar + uuid5 ile deterministik `call_logs.id` üretilir ve `insertOnConflictUpdate` üzerine yazar: bir çağrı günlükte iki satır olamaz. Kayıt ZİL anında atılır (süreç konuşma ortasında ölse de kayıt durur), zaman damgası çağrının BAŞLANGICIDIR (cevapsıza dönüşmek satırı ileri kaydırmaz).
- **Arayan kartı son siparişin DURUMUNU gösterir, kalem dökümünü değil.** `CustomerLookup` sipariş bilgisini hiç çekmiyordu; tek satırlık sorgu eklendi (`occurred_at DESC, id DESC` — Dart çözümleyicisiyle AYNI anahtar, iki yüzey aynı siparişi gösterir). Kalemler bilerek çekilmedi: `order_lines.order_id` indeksi yok ve arayan-tanıma 1 sn bütçesi (kırmızı çizgi) kalem sorgusunu kaldırmaz. Sözlük kartta uzun form (`Hazırlanıyor · Yolda · Teslim edildi · İptal edildi`), liste rozetinde kısa form (`Açık/Teslim/İptal`) kalır — dar alan ile geniş alanın kasıtlı ayrımıdır.
- **`newId()` (UUIDv7) aynı milisaniye içinde MONOTON olmak zorundadır — "UUIDv7 zaman-sıralıdır" ifadesi çıplak hâliyle YANLIŞTI.** `uuid` paketinin `v7.dart`'ı zaman damgasından sonraki 74 bitin tamamını rastgele dolduruyor (`buf.setRange(6, 16, randomBytes)`), aynı ms içinde monotonluk sayacı yok: ölçüldü, aynı ms'e düşen 20.000 çiftin **%50,5'i ters** sıralanıyor. `OrderRepository.create` kalem id'lerini döngüde ürettiği için `ORDER BY id ASC` yazı-turaydı; aynı siparişin kalem dökümü iki cihazda FARKLI sırada çizilebiliyordu — yani `sort_index` için verilen "iki cihaz aynı olay kümesinden aynı sırayı üretir" kararının `order_lines` ayağı boştaydı. Bunu bir test yıllardır ŞANSLA yeşil geçerek gizledi; üç ajan aynı anda koşup makine yavaşlayınca kumar tuttu ve açığa çıktı. Düzeltme RFC 9562 "monotonic random" desenidir (şema yok, migration yok, mevcut kayıtlar etkilenmez). Monotonluk TEK SÜREÇ içindir ve yeterlidir: bir siparişin kalemleri tek cihazda üretilip senkronla taşınır, kaynakta sıra doğruysa her cihazda aynı görünür. Ders: **kırılan bir testi "kodu teste uydur" diye kapatmadan önce testin dayandığı VARSAYIM sınanır** — bu turda ajanın itiraz edip ölçüm getirmesi gerçek bir ürün hatasını testin altına süpürülmekten kurtardı.
- **Sipariş kartındaki Ara · WhatsApp · Konum düğmeleri gerçek uygulamayı AÇAR; numara TR kuralıyla `+90`'a çevrilir.** Düğmeler bozuk değildi, HİÇ BAĞLANMAMIŞTI — üçü de yalnız "… aranıyor" bildirimi basıyordu ("harici uygulama açma Faz 6'ya bırakıldı" yorumuyla). `wa.me/0532…` sohbet açmaz, sessizce boş sayfa verir; E.164 normalizasyonu (0532/532/+90/0090/90 → `+905324152290`, belirsiz girdide null) bu yüzden zorunludur. `Ara` çeviriciyi AÇAR, aramayı BAŞLATMAZ — `CALL_PHONE` izni kırmızı çizgi #6'ya yaklaşırdı. Telefonu/konumu olmayan müşteride düğme GİZLENMEZ, pasif çizilir ve dokununca sebebini söyler (Oto Sırala dersinin aynısı: yeteneği gizlemek, durumunu dürüstçe göstermekten kötüdür). `url_launcher` bu iş için eklendi; alternatif native kanal iOS'ta çalışmazdı.
- **Sürükle-bırak tutamağı VARSAYILAN SAĞDADIR, tercihle sola alınır ve tercih CİHAZ-YERELDİR (senkrona girmez).** Kullanıcının çerçevesi aynen korundu: "çoğunluk telefonu sağ eliyle kullanıyor, ama sol elle kullananları da göz ardı edemeyiz" — varsayılan çoğunluğa, anahtar azınlığa. Anahtar Ayarlar'a değil elle sıralama bandının içine kondu: tutamaç yalnız o kipte görünür, kullanıcı tutamacı GÖRDÜĞÜ anda tercihi arar. Kalıcılık şemaya alan eklemekle değil tema tercihi deseniyle (cihaz-yerel dosya) sağlandı — tek bayrak için migration riski alınmaz. Cihaz-yerel olması bilinçli: masa telefonu sağ, kurye telefonu sol isteyebilir.
- **"Borçlu" sekmesi = TESLİM EDİLMİŞ + siparişe yazılan tahsilat tutarın altında + müşteri bakiyesi borçta; ödeme TİPİNE bakılmaz.** Eski sorgunun tek şartı `customers.balance_kurus > 0` idi ve sipariş durumuna hiç bakmıyordu: borçlu bir müşterinin HENÜZ TESLİM EDİLMEMİŞ siparişi de borç listesine giriyordu (kullanıcının bildirdiği mantık hatası). Borç deftere teslim anında yazılır; teslim edilmemiş sipariş borç değildir. Ölçüt `payment_type='veresiye'` DEĞİL DEFTERDİR: kısmi ödeme ("50 ver, kalanı yaz") kaydı `nakit` tipiyle taşır ama borç bırakır — tipe bakan sorgu onu kaçırırdı.
- **Kurye süzgeci yalnız PATRONA görünür; patron listede ADA göre, ayrıcalıksız bir satır olarak durur.** Süzgeç iki katmanlı bir eksikti: `watchOrders`'ın `assignedTo` parametresi gövdede HİÇ KULLANILMIYORDU ("Benim" sekmesi kaldırılınca dal silinmiş, parametre ölü kalmış) ve ekran o ölü parametreye kullanıcı kimliğini geçip süzdüğünü SANIYORDU. Kullanıcının "patronun kendisi de aslında bir kurye olarak görünmeli" cümlesi rolü BAŞA ALMAK değil, sıradanlaştırmak demektir — iki patronlu bayide "rol önce" keyfî bir ikinci sıralama kuralı gerektirir ve aynı ekip kurye seçme sheet'inde başka sırada görünürdü. Rol `sync_meta`dan AKIŞLA okunur (tek atış okuma girişten hemen sonra "rol yok" görüp süzgeci sonsuza dek gizlerdi — kontör dersi).
- **Teslimde tahsil edilen tutar DÜZENLENEBİLİR; kalan fark AYRI BİR KAYIT DEĞİL, ödenmemiş `debit`in kendisidir.** Teslim tutarı otomatik doluyor ve tamamı tahsil edilmiş sayılıyordu; gerçekte müşteri bir kısmını öder. Çift-satır modelinin genellemesi yeterli oldu: `debit(+tam tutar)` her teslimde, `payment(−alınan)` yalnız alınan > 0 ise. Bakiye hem yerelde hem sunucuda filtresiz `SUM(amount_kurus)` olduğu için kalan borç kendiliğinden doğrudur — yeni `entry_type`/olay/migration GEREKMEDİ ve append-only (kırmızı çizgi #2) el değmedi. Veresiye = tahsil 0, peşin = tahsil tam; ikisi aynı kuralın uç noktalarıdır. uuid5 teslim idempotensi korunur (id tutardan değil SİPARİŞ KİMLİĞİNDEN türer). Tahsil 0 ise kayıt daima `veresiye`ye normalize edilir: `payment_type='nakit'` derken defterde tek kuruş `payment` olmaması gün sonu kasa özetini sipariş listesiyle çeliştirirdi.
- **Sipariş tutarını AŞAN tahsilat kabul edilir; gerekirse müşteri alacaklıya geçer.** Kasa toplamı `payment` satırlarından çıkar — fazlayı reddetmek bayiyi `correction`a iter ve `correction` kasaya girmez, yani kasa devri tutmaz. Ayrıca reddetmek "önce teslim et, sonra ayrı tahsilat gir" iki adımına zorlar; saha ikinciyi atlar, para cebe girer kayıt kaçar. Vakaların çoğunda zaten kimse alacaklı OLMAZ: eski borcu olan müşteri teslimde fazlasını verir, bakiye sıfırlanır. Müşterisiz (tezgâh) satışta tutar TAM olmalıdır — eksiği yazacak, fazlasını alacak yazacak cari yoktur.
- **Tahsilat alanı KURUŞ kabul eder, çipler TAM LİRA yuvarlar; "Tamamı" çipi istisnadır (kuruşuyla doldurur).** Tasarımın tam-lira kısıtı `payment_type`ın üç değerinden yalnız birine (nakit) dayanıyordu; kartla veya havaleyle 85,50 ₺ tahsil etmek olağandır ve borcu TAM kapatır, tam lira orada borcu kapatılamaz kılıyordu. "Yarısı" gerçek bir kısayoldur, yuvarlaması doğrudur; "Tamamı" ise bir KESİNLİK iddiasıdır — yuvarlasa 85,50 ₺ borçta 85 yazıp borcu kapatmaz, üstelik çip `girilen == borç` ile boyandığı için KENDİ dokunuşundan sonra seçili görünmezdi. Bakiye düzeltme sheet'i de aynı sebeple kuruşa açıldı: kısmi ödeme kuruşlu bakiye üretiyorsa, kuruşluk sapmayı kapatamayan bir düzeltme işlevini kaybeder.
- **"Tahsilat Al" TESLİM EDİLMİŞ siparişte de, borcu OLMAYAN müşteride de görünür; yalnız müşterisiz tezgâh siparişinde hiç çizilmez.** Asıl kullanım anı teslim SONRASIDIR (veresiye verilir, müşteri sonradan öder) — düğmeyi "yalnız açık sipariş" kapısına bağlamak onu tam gerektiği anda kapatırdı. Bakiye CANLI bir değerdir; düğmeyi ekranın elindeki anlık kopyaya göre gizlemek bayat karar vermektir — sheet bakiyeyi DOKUNMA ANINDA defterden okur ve borç yoksa sebebini söyleyip hiçbir kayıt yazmaz. Tezgâh siparişi tek istisnadır: orada pasif düğme bile yanlış olur, çünkü tahsilatın muhatabı olan cari HİÇ YOKTUR (veresiye karosundan farkı budur — orada kavram vardır, o bağlamda kullanılamaz).
- **Saha geri bildirimi (2026-07-27, 7 madde) üç paralel ajanla kapatıldı; dosya sahipliği FONKSİYON düzeyinde bölündü ve kırılan her test sahibine geri gönderildi.** Maddelerin dördü (`Ara/WhatsApp/Konum`, tutamaç tarafı, borçlu sekmesi, kurye süzgeci) aynı iki dosyada kesişiyordu — tek sahip kuralı olmasaydı ajanlar birbirinin düzenlemesini ezerdi; `order_repository.deliver` ise sahibi başka olan bir dosyanın içinde ayrı bir ajana verildi. Test koşumu SIRAYA alındı (eşzamanlı `flutter test` sqlite3.dll üzerinde çakışıp aracı çökertiyor). Ajan raporları doğrulanmadan kabul edilmedi: `uuid` paketinin kaynağı, `git status` ile dosya sahipliği ve test sonuçları lead tarafından bağımsız koşuldu.
- **Senkron isteklerinde ZAMAN AŞIMI zorunludur ve meşguliyet bir `bool` ile DEĞİL, çalışan turun kendisiyle tutulur.** Bayi "alakasız yerde çevrim dışı diyor, kapatıp açınca senkronize ediyor" dedi; tek bir kök neden zinciri belirtinin tamamını üretiyordu: (1) `HttpSyncApi` push/pull çağrılarında zaman aşımı yoktu ve `package:http` varsayılanı SONSUZ bekler — mobilde yarı-açık TCP olağandır (wifi↔mobil veri geçişi, captive portal, operatör NAT'ının soketi sessizce düşürmesi): istek ne biter ne hata verir; (2) asılı `await` yüzünden `finally` HİÇ çalışmaz, `bool _running` sonsuza dek `true` kalır; (3) 2 dakikalık `Timer.periodic`in her turu `if (_running) return SyncOutcome(ok: true)` ile sessizce yutulur — üstelik dönen değer YALANDIR, tur koşmamışken "başarılı" der; (4) o erken dönüş `_status`a hiçbir şey yayınlamaz, gösterge kilitten hemen önceki "başarısız" değerinde DONAR → cihaz çevrimiçiyken ekranda "Çevrimdışı"; (5) `_running` de `_sonSenkron` da BELLEKTE olduğu için yalnız uygulamayı öldürmek kurtarır. Düzeltme: 25 sn zaman aşımı + `Future<SyncOutcome>? _tur` ile birleştirme (`_tur ??= _turAt().whenComplete(() => _tur = null)`) — eşzamanlı çağrılar aynı turun GERÇEK sonucunu alır, yalan üretilmez. Ders: **bir "meşgul" bayrağı, onu temizleyecek kodun çalışacağını varsayar; asılı bir `await`te bu varsayım çöker — bayrak yerine işin kendisini tut.**
- **Senkron `catch`i `on Exception` DEĞİL, `catch (e)` olmalıdır — Dart'ta `TypeError` bir `Error`dur, `Exception` değil.** `SyncEngine`deki `as String` / `as num` dönüşümleri beklenmedik payload'da TypeError atar; `on Exception` bunu yakalamadığı için `_status.add` atlanıyor (gösterge donuyor) ve hata yutulmuş async hataya dönüşüyordu. Bu, yukarıdakinden BAĞIMSIZ ikinci bir kilit yoludur ve yeniden başlatmayla DÜZELMEZ: delta transaction'ı geri sarıldığı için `lastPulledSeq` ilerlemez, her tur aynı kayda çarpar. Sahada henüz tetiklenmemişti — ama sunucuya nullable bir kolon eklendiği gün kalıcı kilit üretirdi. **Yakalanan tip daraltıldığında, daraltmanın dışında kalan hata sınıfı sessiz kilit üretir.**
- **Senkron turunu ÜÇ tetikleyici açar: zamanlayıcı · uygulamanın öne gelmesi · ağın geri gelmesi; ve tetikleyicinin susması senkronu DÜŞÜRMEZ.** Önceden tek tetik vardı (açılış + 2 dk zamanlayıcı); yaşam döngüsü ve ağ tetiği YOKTU — `connectivity_plus` pubspec'te kayıtlıydı ve `sync_engine.dart`ın yorumunda "ağ tetiği bunları çağırır" yazıyordu ama `lib/` içinde HİÇ kullanılmamıştı, yani niyet hiç gerçekleşmemişti. Kurye kapsama alanına girdiği ANDA sipariş gitmelidir; iki dakika ya da uygulamayı öne getirme beklemek saha gerçeğine aykırıdır. Tetikleyici bir Stream olarak dışarıdan bağlanır (servis `connectivity_plus`ı tanımaz) ve abonelik `onError` ile korunur: tetik bir İYİLEŞTİRMEDİR, senkronun şartı değil — kaynağı susarsa zamanlayıcı ve öne-gelme turu işi sürdürür. Bu koruma aynı zamanda uygulama kökünü kuran widget testlerini de platform kanalı hatasından korur.
- **Çevrimdışı bandı TEK metin değildir: ağ · oturum · veri hatası ayrı ayrı ve DOĞRU söylenir.** Oturum süresi dolduğunda (401/403) bant "Çevrimdışı · bağlanınca gönderilecek" diyordu — düpedüz yanlış bilgi: bayi bekler, oysa hiçbir şey gönderilmeyecektir, oturumu yenilemesi gerekir. Bu, düzelttiğimiz "gösterge yalan söylüyor" arızasının aynı ailesidir. Üç varyant: ağ ("değişiklikler kaydedilip bağlanınca gönderilecek"), oturum ("çıkış yapıp yeniden girin"), veri ("kayıtlarınız cihazda güvende, destekle görüşün"). Veri varyantı ayrı tutulur çünkü beklenmedik payload ne ağ ne oturum sorunudur; onu "çevrimdışı"ya eşlemek düzelttiğimiz yalanı yeniden üretirdi. Sınıflandırma saf bir fonksiyonda, eşleme ekranda: tema katmanı `sync` paketini TANIMAZ, kendi enum'unu taşır.
- **Sesli giriş ad · adres · bölge · not alanlarındadır; TELEFON alanında YOKTUR.** Türkçe tanıma rakamları tutarsız döndürür ("beş yüz otuz iki" / "532" / "5 32"); tek hane kayması yanlış numara kaydeder ve o numara ARAYAN TANIMAYI kör eder — yani ürünün varlık sebebini bozar. Risk simetrik değildir: on haneyi klavyeden yazmak birkaç saniye, yanlış numara sessizce kaybedilen bir müşteridir. Tanıma dili DAİMA `tr`dir — cihaz diline bırakmak "Ayşe Kaya"yı "I shall car" yapar; cihazda Türkçe yoksa özellik pasif görünür ve sebebini söyler (gizlenmez — Oto Sırala dersi). Tanınan metin alanın SONUNA eklenir, üzerine YAZILMAZ: tanıma yanılabilir, elle yazılanı sessizce silmek geri alınamaz, fazlalığı silmek tek dokunuştur — bu, "kayıt ezilmez, düzeltme eklenerek yapılır" disiplininin arayüz karşılığıdır. `RECORD_AUDIO` Play'in KISITLI grubunda değildir (kırmızı çizgi #6 korunur), ama `<queries><intent action="android.speech.RecognitionService">` beyanı ZORUNLUDUR: Android 11+ paket görünürlüğü yüzünden bu satır olmadan tanıma servisi hiç görünmez ve özellik sessizce kapanır.
- **Sesli tanıma cihazın OS servisine gider ve bu, kırmızı çizgi #4'ün ihlali DEĞİLDİR — ama KVKK aydınlatma metnine yazılmalıdır.** Android `SpeechRecognizer` tanımayı bulutta yapabilir, yani dikte edilen müşteri adı/adresi üçüncü tarafın sunucusundan geçebilir. Kırmızı çizgi #4 verinin NEREDE SAKLANDIĞIYLA ilgilidir; cihazın tanıma servisi bir saklama değil, kullanıcının kendi klavyesiyle aynı kategoridedir (Gboard'un sesle yazması zaten her bayide açıktır ve aynı yolu kullanır). `onDevice: true` ile zorlamak DENENMEDİ çünkü Türkçe çevrimdışı modeli yüklü olmayan telefonlarda özellik hiç çalışmaz — "var ama çalışmıyor" bir düğme, olmayan özellikten kötüdür. Aydınlatma metni ve Play "Veri güvenliği" formu proje sahibinin borcudur (PLAN.md).
- **Arayan kartının yan boşluğunu KART değil PENCERE verir.** Kart kendi kenar payını `LinearLayout.LayoutParams` ile veriyordu ve İKİ host da onu sessizce çöpe atıyordu: `WindowManager.addView` görünümün layoutParams'ını kendi params'ıyla değiştirir (üstelik `WindowManager.LayoutParams` bir `MarginLayoutParams` değildir, kenar payı alanı yoktur) ve `Activity.setContentView(View)` içeriğe `MATCH_PARENT`lık düz bir params dayatır. Niyet en baştan doğruydu — kod yorumunda "yanlardan 16 boşluk" yazıyordu — uygulama iki yerde birden düşüyordu. Boşluk pencere genişliğinden verilir (ekran − 2×16dp, ölçü tasarımdaki `.cagri-overlay { padding: 0 16px }`ten); şeffaf sarmalayıcı görünüm KULLANILMAZ çünkü yan şeritler dokunuşu yutar ve kartın yanına basınca çağrı ekranına geçilmesi gereken davranış bozulurdu. Kartın DARALMASI üst şeritte gizli bir taşma açığa çıkardı (yön etiketi + daralma birlikte sığmıyordu) — tek başına sığan iki değişiklik birlikte taşabilir, bu yüzden dar ekran taşma testi kalıcıdır.
- **Saha geri bildirimi ikinci tur (2026-07-27): gösterge yalanı, sesli giriş ve kart boşluğu.** Üç maddenin ikisi kozmetik göründü ama biri (senkron) veri güvenilirliği sorunuydu: kurye "çevrim dışı" görüp siparişin gitmediğini sanırsa işi tekrar yapar ya da bekler. Ders tekrar doğrulandı: **bayinin "alakasız yerde" dediği yer rastgele değildi — belirtinin tarifi (özellikle "kapatıp açınca düzeliyor") kök nedeni doğrudan işaret ediyordu**; bellekte tutulan bir durumun yalnız yeniden başlatmayla düzelmesi, o durumun temizlenme yolunun hiç çalışmadığının kanıtıdır.
- **Bildirim mimarisi ÜÇ KATMANDIR: saf kural · üretici · servis — ve bu ayrım pazarlıksızdır.** Kural fonksiyonları veri alıp `BildirimTaslagi?` döner: veritabanı yok, saat yok, yan etki yok ("şimdi" bile parametre). Üreticiler defteri okuyup kurala verir (saf DEĞİL, Drift'e dokunur). Servis gösterir/zamanlar (platforma dokunur). Ayrımın karşılığı bu turda defalarca alındı: "bu müşteri gecikmiş sayılır mı", "bu ödeme eşiği geçirdi mi", "30 günü aşan borç ne kadar" sorularının tamamı sahte veritabanı ve sahte saat kurmadan, düz `test()` ile sınandı. Kurallar `goster`in sessizce atlayabileceğini (kategori kapalı · izin yok · bütçe dolu · sessiz saat) BİLMEZ ve buna dallanmaz — yalnız doğru taslağı üretir; politika servistedir.
- **Sessiz saatte bildirim ATILMAZ, ERTELENİR (22:00–08:00 varsayılan).** Gece bildirimi esnafın uykusunu böler ve bildirimlerin tamamen kapatılmasıyla sonuçlanır; ama atmak da bilgiyi kaybettirir. `goster` sessiz saatteyse otomatik olarak sabah 08:00'e zamanlanır. Ayarlarda bilgilendirme satırı vardır — bayi gece bildirim gelmediğini bir yerde okuyabilmeli, yoksa "bildirimler çalışmıyor" diye döner.
- **Günlük bütçe (toplam 6 · kategori başına 2) vardır ve bütçe yüzünden atlanan bildirim İZ BIRAKIR.** Bildirim yorgunluğu gerçek bir risktir: günde 30 sipariş giren bir bayi bildirim yağmuru görürse hepsini kapatır ve sonra gerçekten önemli olanı da kaçırır. Kategori başına sınır, tek bir kategorinin bütçeyi tek başına tüketmesini engeller. Sayaç GÖSTERİM değil FARKLI KİMLİK sayar — aynı bildirimin gün içinde tazelenmesi bütçe yemez; gün anahtarıyla saklandığı için gece yarısı kendiliğinden sıfırlanır. **Sessizce düşürme yasaktır:** bütçeye takılan bildirim `atlananlar` listesine (kimlik, an) yazılır — sahadan "bildirim gelmiyor" şikâyeti geldiğinde bakılacak ilk yer orasıdır; süreç ömrüyle sınırlı, diske yazılmaz (tanı verisi, iş verisi değil).
- **Bütçe sınırı, bildirimi MÜŞTERİ BAŞINA değil GÜNLÜK ÖZET olarak üretmeyi zorunlu kılar.** İki kural ajanı da bağımsız olarak aynı sonuca vardı: kategori başına 2 sınırıyla müşteri başına bildirim üretilirse üçüncü müşteri SESSİZCE DÜŞER — üstelik düşen, en son ve muhtemelen en kritik olandır. Bunun yerine gün başına tek özet üretilir ("3 müşterinin borcu 2.000,00 ₺ eşiğini aştı · toplam 7.550,00 ₺"), gün içinde dördüncü müşteri aşarsa AYNI kimlik üzerine yazılır: bütçeden ikinci kez düşmez, bayi tek ve güncel satır görür. **Bir sınır koymak, o sınıra takılan şeyin ne olacağını da tasarlamayı gerektirir.**
- **Bildirimler kilit ekranında TAMAMEN gizlenir (`VISIBILITY_PRIVATE`); gün sonu ciro özeti de buna dâhildir.** "Nötr başlık göster, ayrıntıyı gizle" ikilisi denendi ve KURULAMADI: `flutter_local_notifications` `publicVersion` alanını açmıyor, `private` seçilince Android başlık dâhil her şeyi gizliyor. Kapsam: müşteri adı ya da para taşıyan HER kategori. Gün sonu tutarının da gizlenmesi bilinçlidir — bayinin günlük cirosu tezgâhta duran telefonda yalnız mahremiyet değil, **hedefleme bilgisidir**. Yalnız `sistem` kategorisi `public` (kişisel veri de para da taşımaz; "senkron yapılamıyor" uyarısının kilitliyken görünmesi işe yarar). Arayan kartı bildirimi de bilerek `public` kalır: kart çağrı ekranının altında kalırsa bilgiyi taşıyan tek yüzey odur ve telefon o an bayinin elindedir. **Metinlerde müşteri adı yine de BAŞLIKTA DEĞİL GÖVDEDE tutulur** — görünürlük kararı ileride değişirse ya da native köprüyle `publicVersion` eklenirse sızacak alan tam olarak başlıktır; bugün gereksiz görünen koruma yarın tek koruma olabilir.
- **Vadesi geçen borç FIFO ALACAK YAŞLANDIRMASIYLA hesaplanır: ödemeler en eski borcu kapatır, kalan parçalardan 30 günü aşanlar gecikmiştir.** İki basit alternatif de denendi ve ikisi de YANLIŞ müşteriyi işaretliyor: "ilk ödenmemiş `debit`in tarihi" ölçütü, bakiyesi hiç sıfırlanmayan ama her ay düzenli ödeyen müşteriyi sonsuza dek alarma sokar — yani bayinin EN İYİ müşterileri uyarı listesine düşer; "son tahsilat tarihi" ölçütü ise 1.000 ₺ borcuna dün 50 ₺ ödeyeni temiz gösterir (borcun yaşını değil temas tarihini ölçer). FIFO muhasebedeki alacak yaşlandırmasının kendisidir ve esnafın kafasındaki modelle örtüşür. Tarama PAZARTESİ 10:00'dur: borç kovalamak planlanacak bir iştir, cuma akşamı atılan bildirim eyleme dönüşmez.
- **Müşteri sipariş ritmi ORTANCA ile ölçülür, ortalama ile değil; düzenlilik ölçüsü MAD'dir, standart sapma değil.** Ölçüldü: aralıkları [4, 15, 15, 90] gün olan bir müşteride (15 günlük düzenli müşteri, bir kez 90 gün tatil) ortalama 31, ortanca 15 der — ortalamayla o müşteri bir daha ASLA "gecikmiş" görünmez, yani kural tam yakalaması gereken yerde kör kalır. Alt sınır 4 TESLİMATTIR (3 aralık): 2 aralıkta ortanca ikisinin ortalamasıdır ve tek aykırı değer yine sonucu kaydırır; 3 aralıkta ortanca gerçekten ortadaki değerdir. Gecikme eşiği sabit gün ya da düz kat DEĞİL, müşterinin KENDİ değişkenliğine göredir (`ortanca + min(max(2·MAD, 0.4·ortanca), 30 gün)`) — tavan 30 gündür çünkü 90 günlük müşteride oransal pay bildirimi dört ay sonraya atardı ve müşteri çoktan kaybedilmiş olurdu. Sıralama ORANA göredir, ham güne göre değil: 7 günde bir alanın 14 günlük sessizliği, 60 günde bir alanın 100 günlük sessizliğinden daha alarm vericidir. **Emin olunmayan durumda SUSULUR** (ritim bandı dışı · MAD ortancanın yarısını aşan düzensiz müşteri · açık siparişi olan müşteri): yanlış bir "Ahmet Bey'i aramalısın" bildirimi, bayinin o bildirime bir daha bakmamasına yol açar.
- **Borç eşiği bildirimi VARSAYILAN KAPALI başlar; bayi kendi eşiğini girince açılır.** Cirosu 2.000 ₺ olan bayiyle 200.000 ₺ olan aynı sınırı kullanamaz — uydurulmuş bir varsayılan ya bildirimi susturur ya gürültüye çevirir. Üçüncü yol seçildi: ayarlarda eşik alanı boşken kategori pasif görünür ve "bir eşik belirleyin" der. Kapı TEK YERDEDİR (`kategoriAcik` eşik yoksa `false` döner), böylece servis · ayar ekranı · tetikleyici ayrı ayrı kontrol etmez ve eşiksiz bir borç bildirimi hiçbir yoldan çıkamaz. Üretici tarafında ikinci bir erken dönüş vardır — mükerrer değil faydalı: biri bildirimi göstermeden, diğeri defteri HİÇ OKUMADAN durdurur. Eşik sheet'i LİRA sorar (esnaf eşiği "iki bin lira" diye düşünür; kuruş alanı 100 kat hataya açıktır).
- **Bildirim kuralları HER AÇILIŞTA anlık koşar; zamanlama tek dayanak değildir.** Xiaomi/Redmi'nin agresif pil yönetimi zamanlanmış bildirimi öldürebilir ve bu GARANTİ ALTINA ALINAMAZ (`inexactAllowWhileIdle` + boot alıcısı + her açılışta kanal kurulumu alınan önlemlerdir, yeterli değildir). Asıl güvence mimaridir: kimlikler gün damgalı olduğu için tekrar koşmak güvenlidir — aynı bildirim tazelenir, yenisi doğmaz, bütçeden ikinci kez düşmez. Tetikleme YALNIZ oturum açıkken koşar (oturumsuz cihazda defter boştur, beş kural da `null` dönerdi) ve açılışı BLOKE ETMEZ; ama altyapı kurulumu tetiklemeden ÖNCE beklenir — kanallar ve saat dilimi hazır olmadan zamanlama yapılırsa bildirim yanlış saate düşer.
- **Ekrana gömülü bir widget platform eklentisine uzanıyorsa, eklenti yokken ÇÖKMEDEN pasifleşmek zorundadır.** Ayarlardaki bildirim bölümü kurulurken izin durumunu okuyor, o da platform eklentisini çözmeye çalışıyordu; test ortamında eklenti kaydı olmadığı için `LateInitializationError` atıyor ve EKRANIN TAMAMINI çökertiyordu. Bu yalnız bir test sorunu değildi: iOS'ta Android eklentisi zaten null döner ve ayarlar ekranı orada da açılmazdı. Çözüm `tutamac_deposu`/`tema_deposu` disiplininin aynısıdır — platform yoksa varsayılana düş. **Ders: `flutter analyze` temiz olması kodun ÇALIŞTIĞI anlamına gelmez** — statik analiz çalışma zamanı davranışını göremez; bu turda analiz temizken bir dosyanın tamamı istisnayla düştü.
- **Bildirim `yol` sözlüğünün çözümü SÖZLEŞMEDEDİR, kabukta değil.** Taslağı üreten kural ile onu tüketen kabuk aynı tanıma bakmalıdır; iki ayrı `split('/')` iki ayrı gerçek demektir. Saf olduğu için ayrıca testlenebilir (kabuktaki özel bir metot testlenemezdi). Tanınmayan yol `null` döner, istisna ATMAZ: sözlük Faz 2'de büyüyecek ve zamanlanmış eski bir bildirim yeni ya da eski bir yol taşıyabilir — bilinmeyen hedef bir hata değildir, kullanıcı bulunduğu yerde kalır. Kabuk tarafında dinleyici kurmak YETMEZ, mevcut değer bir kez YOKLANIR: uygulama kapalıyken bildirime dokunulmuşsa değer biz dinlemeye başlamadan düşmüş olur ve o dokunuş sessizce kaybolurdu (çağrı kartı köprüsündeki `bekleyen` deseninin aynısı); değer tüketilince SIFIRLANIR, yoksa aynı yol her dinleyici kurulumunda yeniden açılır.
- **Faz 1 bildirimleri (2026-07-27) yerel üretilir; push/Firebase YOKTUR ve gerekmedi.** Beş bildirim de cihazdaki veriden hesaplanır: gün sonu özeti · borç eşiği · vadesi geçen borçlar · gecikmiş müşteri · rutin teslim günü. Bu, offline-first çizgisiyle kendiliğinden uyumludur ve KVKK açısından sessizdir (bildirim cihazdan ÇIKMAZ, üçüncü tarafa payload gitmez). Cihazlar arası bildirimler (kuryeye sipariş atandı, teslim edildi, kasa devri) FCM gerektirir ve Faz 2'dedir — tek kişilik bayide değeri sıfıra yakın olduğu için önceliklendirilmedi. Abonelik bildirimi bilinçli olarak KAPSAM DIŞIDIR: mağaza kuralı (Apple 3.1.3f / Play) satın almaya yönlendirmeyi yasaklar ve bu riski bir hatırlatma için almaya değmez.
- **Arayan kartı ile bildirimi AYNI ANDA çıkar; bildirim kartın YEDEĞİ değil, ikinci yüzeyidir (bayi isteği, 2026-07-27).** Eski davranış `if (!shown) showNotification(...)` idi: kart gösterilebildiyse bildirim atlanıyordu. Bayi ikisini birden istedi ve haklı — kart çağrı bitince kapanır, bildirim gölgede kalır ve müşteriyi/borcu sonradan da gösterir. Bunun bir tuzağı var: kanal `IMPORTANCE_HIGH` olduğu için bildirim **heads-up olarak kartın ÜSTÜNE biner** ve tam istenenin tersini yapardı. Kanal önemi oluşturulduktan sonra değiştirilemediği için (Android 8+, sonrası kullanıcının ayarı) çözüm İKİ KANALDIR: `sipario_caller` (HIGH — kart HİÇ gösterilemediğinde, bildirim tek yüzeydir ve kilit ekranındaki tam ekran niyeti ancak yüksek önemli bildirimle açılır, sessiz kanal o yolu tamamen öldürürdü) ve `sipario_caller_sessiz` (LOW + ses/titreşim kapalı — kart ekrandayken, bildirim yalnız gölgede birikir). Telefon zaten çalarken bildirimin ayrıca ses çıkarmaması da bu kanalın işidir; `Notification.Builder.setSilent` ÇERÇEVEDE YOKTUR (o `NotificationCompat` API'sidir) ve Android 8+'da bildirim düzeyinde `setSound(null)` yok sayılır — sessizlik yalnız kanaldan gelir. **Ölçüm tuzağı:** bildirim artık her çağrıda çıktığı için `LatencyLog` iki kayıt yazacak ve go/no-go sayacı şişecekti; ölçüm yalnız bildirim TEK yüzeyken yazılır (overlay'in ölçüm yazmama kuralının aynısı).
- **Kart eylemleri ile bildirim eylemleri TEK LİSTEDEN ve TEK NİYET ÜRETİCİDEN gelir.** `CallerCard.eylemListesi(customer)` eylemlerin tek kaynağıdır (kartın kendi listesi silindi), `CallerCard.eylemNiyeti(...)` köprünün tek yeridir. İki yüzey ayrı listeler tutsaydı bir eylem eklendiğinde sessizce ayrışırlardı. Her iki düğme de aynı ekstralarla `MainActivity`ye gider, oradan `bekleyen` + `sipario/cagri` kanalıyla Dart'a — ikinci bir transfer yolu AÇILMAZ. **`PendingIntent` tuzağı:** eşitlik ekstraları saymaz (`filterEquals`), yani iki eylem aynı `requestCode` ile kurulsaydı tek `PendingIntent`e çökerler ve **iki düğme de aynı işi yapardı**; sıra numarası `requestCode`a katılır. Kapatma sözleşmesi ("bir eylem, kartı da kapatmak zorundadır") artık İKİ yüzeyi birden kapsar ve tek yerde (`MainActivity.eylemiAl`) uygulanır: bildirimden basılırsa kart, karttan basılırsa bildirim de kapanır.
- **Sürüm ADI elle (`pubspec.yaml` → `0.9.0`), YAPI NUMARASI git commit sayısından OTOMATİK türer; ayarlar ekranı ikisini de APK'nın kendisinden okur.** Kullanıcı kararı (2026-07-27): *"artık versiyon ile uğraşmak istemiyorum."* Öncesinde versiyonlama HİÇ yapılmıyordu ve iki ayrı sayı birbirinden habersizdi: `pubspec.yaml` Flutter varsayılanı `1.0.0+1`de duruyordu, ayarlar ekranı ise elle yazılmış `'Sipario 3.2'` sabitini gösteriyordu — aralarında hiçbir bağ yoktu. Bedeli aynı gün görüldü: **dört ayrı APK derlendi, dördü de aynı sürümü gösteriyordu** ve sahadaki bayi hangisini test ettiğini söyleyemiyordu; "bildirim gelmiyor" raporunun hangi APK'ya ait olduğu bilinemezdi. Elle yönetilen bir sürüm er geç unutulur, iki yerde yazılan bir sayı ise biri güncellenip diğeri unutulduğunda **sessizce yalan söyler**. Çözüm: (1) `versionCode = git rev-list --count HEAD` — monoton, atlamayan, kimsenin yazmadığı bir sayaç; depoda zaten her yanıt sonunda otomatik commit atan bir kapı olduğu için kendiliğinden ilerler; (2) `versionName` yalnız `pubspec.yaml`da, nadiren ve bilinçli değişir (mağaza çıkışında `1.0.0`); (3) ayarlar ekranı `package_info_plus` ile `PackageManager`dan okur — gradle ne ürettiyse bayi onu görür, ikinci bir kaynak yoktur. **Yapı numarası ekranda GÖSTERİLMEK ZORUNDA** (`Sipario 0.9.0 (123)`): sürüm adı iki derleme arasında aynı kalabilir, yapı numarası kalamaz — saha testinde "hangi APK?" sorusunu tek başına o cevaplar.
- **Sürüm türeten kod derlemeyi ASLA kırmamalıdır; git yoksa yedek değere düşülür.** `gitCommitSayisi()` her hata türünde `null` döner (git kurulu değil · depo dışında derleniyor · sığ klon · komut hata verdi) ve çağıran `flutter.versionCode`a düşer. Gerekçe: sürüm numarası uğruna APK üretilememesi, elle yönetilen sürümden daha kötüdür. Aynı disiplin ekran tarafında da geçerli — `PackageInfo` çözülemezse nötr `'Sipario'` metnine düşülür ve ayarlar ekranı bir sürüm satırı yüzünden açılamaz hâle GELMEZ (bu ders `LateInitializationError` ile bir kez ödendi).
- **`aapt2 dump badging` ile APK'dan geri okumadan sürüm doğrulanmış sayılmaz.** Gradle'ın yazdığına, testin geçtiğine ya da analizin temiz olmasına güvenilmez: doğrulama derlenmiş APK'dan okundu — `versionCode='123'` (git sayacıyla birebir) · `versionName='0.9.0'`. Aynı araç aynı gün izin denetiminde de kullanıldı ve kırmızı çizgi #6'yı üründe kanıtladı. Bu, "üç ayrı doğrulama kapısı" dersinin dördüncü kapısıdır: **analiz · testler · derleme · ÜRÜNÜN KENDİSİ.**
- **Pilot dağıtımı GITHUB RELEASES üzerinden TAM OTOMATİK; uygulama kendini `saha` kanalından günceller (kullanıcı kararı 2026-07-28: "sürekli APK paylaşmak istemiyorum, tam otomatik bir sistem istiyorum — geçici, sonrası Play Store").** Akış: dev'e mobil dokunuşlu her push → CI (`.github/workflows/saha-apk.yml`) APK'ları derler, sürümü APK'nın KENDİSİNDEN doğrular ve `saha` adlı YUVARLANAN release'in varlıklarını değiştirir (tek release; `saha-arm64.apk` · `saha-evrensel.apk` · `surum.json`, adlar SABİT — flutter'ın çıktı adı değişse bile uygulama tarafı etkilenmez). Uygulama açılışta `releases/download/saha/surum.json`u çeker (5 sn zaman aşımı, HER hata sessiz — çevrimdışında sıfır gecikme/uyarı, offline-first bozulmaz), yenisini görünce müdahalesiz bir bant gösterir (diyalog DEĞİL: bayi sabah sipariş girmek için açar, ortasına diyalog çıkarmak işi bölmek olur), dokununca indirir + kurulum ekranını açar. Bu yol TÜNELDEN ve API'DEN TAMAMEN BAĞIMSIZDIR — geliştirici makinesi kapalıyken de çalışır; kalıcı sunucu adresi kurulmasını beklemez. Cloudflare/DNS alternatifi bu yüzden elendi; depo zaten GitHub'da ve public'ti.
- **İki kanal (flavor): `saha` ve `magaza` — AYNI applicationId, farklı yetki.** `saha`: `REQUEST_INSTALL_PACKAGES` + FileProvider YALNIZ kendi manifest katmanında, güncelleme kodu açık. `magaza`: ikisi de YOK, güncelleme kodu derleme sabitiyle ölü (`kKanal != 'saha'` → ağa hiç çıkılmaz, bant ağaçta yer tutmaz). applicationId ikisinde de aynı ve bu PAZARLIKSIZ: farklı olsaydı güncelleme kendi üstüne kurulamazdı. "Geçici özellik Play'e sızmasın" garantisi koda gömüldü: `check_permissions.sh` kanal farkındadır — magaza APK'sında kurulum izni görülürse KIRMIZI, saha APK'sında izin DÜŞERSE DE kırmIZI (pozitif kontrol: izin sessizce düşerse güncelleme sessizce ölürdü). **Bundan sonra derleme komutları HER ZAMAN `--flavor` verir:** flavor'sız `flutter build apk` kırılmıyor ama adında kanal olmayan, içeriği belirsiz bir APK üretiyor — biri yanlışlıkla yanlış kanalı dağıtabilir.
- **Güncelleme karşılaştırması `PackageInfo.buildNumber`a DAYANDIRILAMAZ; yapı numarası uygulamaya `--dart-define=SIPARIO_YAPIM=<git sayısı>` ile geçilir.** Ölçüldü: `--split-per-abi` Flutter gradle eklentisinde versionCode'a ABI sapması ekler (v7a +1000, arm64 +2000, x86_64 +4000) — git sayacı 128 iken arm64 APK'sı kendini 2128 görür, `surum.json`daki 128'le karşılaştırıp "zaten güncelim" der ve BİR DAHA ASLA GÜNCELLENMEZ (hiçbir şey çökmez, sadece hiçbir şey olmaz — teşhisi en zor arıza türü). Sapmayı gradle'dan geri almak DENENDİ ve OLMUYOR: Flutter eklentisi `applicationVariants.all { versionCodeOverride }`ı `afterEvaluate`te uygulayıp geç yazımı eziyor. `SIPARIO_YAPIM` tanımsızsa güncelleme kontrolü HİÇ koşmaz — yerel/geliştirme derlemeleri bayiyi dürtmez. Ders: **sözleşme, iki tarafın da OKUYABİLDİĞİ bir değere dayanmalı; platformun sessizce değiştirdiği bir alan sözleşme taşıyamaz.**
- **Saha imza anahtarı depo DIŞINDA yaşar (depo public), CI'ya secrets ile gider; İLK kurulum da CI APK'sıyla yapılmak zorundadır.** Anahtar `~/.sipario-anahtar/saha.jks`te + GitHub secrets'ta (`SAHA_KEYSTORE_B64/SIFRE/ANAHTAR_SIFRE`, alias `saha`); gradle yalnız `SAHA_KEYSTORE_YOLU` tanımlıysa kullanır, yerel derleme hiçbir sır istemez ve debug imzayla sürer. Uygulama içi güncelleme yeni APK'nın eskisiyle AYNI anahtarla imzalanmasını şart koşar — elle kurulmuş debug-imzalı uygulamanın üstüne CI APK'sı KURULAMAZ ("imza uyuşmuyor"); sahadaki cihazda tek seferlik sil + CI APK'sıyla kur geçişi gerekir (veri sunucudan geri gelir). `surum.json`daki `boyut_*` alanları indirme bütünlüğü içindir: boyut tutmuyorsa APK KURULMAZ, silinir, sonraki açılışta yeniden denenir; alan yoksa yalnız bütünlük kontrolü atlanır (eski bir surum.json güncellemeyi tamamen durduramaz). Sürüm DÜŞÜRME koruması: uzak yapı KÜÇÜKSE güncelleme önerilmez — cihazdaki daha yeni elle kurulmuş derlemeyi geri sürüme çekmek veri kaybı riskidir.
- **Coğrafi kodlama SAĞLAYICI SOYUT kuruldu; ilk sağlayıcı Yandex Geocoder, anahtar YALNIZ SUNUCUDA (kullanıcı kararı 2026-07-28: "Yandex Geocoder seçtim, Google'ı da alacağım — altyapısını kur").** `Geocoder` arayüzü + `YandexGeocoder`/`GoogleGeocoder`/`NullGeocoder`; sürücü `GEOCODING_DRIVER` ile seçilir, anahtarı olmayan sürücü Null'a DÜŞER (yanlış yapılandırma sessiz 500 değil, dürüst "bu kurulumda tanımlı değil"). İstemci sağlayıcıya HİÇ gitmez, `POST /api/v1/geocode`e gider: (1) APK'ya gömülen anahtar ilk kurulumda çıkarılır ve kota yakılır, (2) aynı adres ikinci kez sorulmamalı — önbellek 30 gün ve GLOBAL'dir (dönen veri kamuya açık coğrafi veridir, kiracı verisi değil; bir bayinin sorduğu mahalle diğerine bedavaya gelir), (3) Yandex→Google geçişi bir env satırı olmalı, uygulama güncellemesi değil. Kota kiracı başınadır (kullanıcı başına DEĞİL — cihaz ekleyerek kota çoğaltılamaz): dk 20 + gün 300, aşımda 429. Arıza ÖNBELLEKLENMEZ, boş sonuç önbelleklenir. **`Point.pos` "BOYLAM ENLEM" sırasındadır (coğrafyada alışılanın TERSİ) — ters okunsa Antalya'daki adres Mısır açıklarına düşer ve rota sessizce saçmalardı; ayrıştırma tek yerdedir ve testle kilitlidir.** Türkiye kutusu dışına düşen aday listeye HİÇ girmez ("Bahçelievler" Kosova'da da var). Google hatayı HTTP koduyla değil gövdedeki `status` ile bildirir; 200+`REQUEST_DENIED` "başarılı" sayılsaydı kullanıcıya sessizce boş liste gösterirdik. Sağlayıcının ham gerekçesi (anahtar/kota) istemciye ASLA geçmez, log'da durur. KVKK (kırmızı çizgi #4): uç nokta yalnız `query`+`region` KABUL EDER — ad/telefon/müşteri kimliği sınırı geçemez ve bu bir testle kilitlendi; aydınlatma metnine "adres yurt dışı sağlayıcıya aktarılır" satırı borç yazıldı.
- **Konum İKİ ayrı yolla alınır ve ikisi karıştırılmaz: adresten kodlama TAHMİN, cihaz GPS'i ÖLÇÜM'dür.** "Konum Al" (konum yokken) sunucudan aday listesi getirir ve doğrusunu KULLANICI seçer — otomatik atama YOK, yanlış algılanan adres yüzünden kurye yanlış kapıya gitmesin. "Konum Güncelle" (konum varken çipe dokunuş) cihazın bulunduğu noktayı yazar; adres metni, bölge ve etiket DEĞİŞMEZ. Gerekçe: coğrafi kodlama sokağı bulur, kapıyı bulmaz — doğru pin ancak kapının önünde durup alınır ve kurye ilk teslimatta tahmini ölçümle değiştirir. Bu yüzden aday satırında kesinlik kademesi (bina/sokak/semt) GÖSTERİLİR: "sokak" kesinliğindeki bir pini sessizce "konum kayıtlı" saymak, kuryenin güvendiği bir yanlış üretir. Aynı disiplin GPS tarafında: ±100 m üstü ölçüm kaydedilir ama doğruluğu SÖYLENİR. `geolocator` yalnız ÖN PLAN konumu ister; `ACCESS_BACKGROUND_LOCATION` İSTENMEZ (Play'in kısıtlı izin beyan formunu tetikler, kırmızı çizgi #6 ile aynı red riski; canlı kurye takibi bu sürümde yok). `ACCESS_COARSE_LOCATION` da beyan edilir: Android 12+ diyaloğunda "Yaklaşık" seçen kullanıcıda özellik aksi hâlde SESSİZCE hiç çalışmazdı. Eklenti çağrılarının tamamı try içindedir ve ekranlar `cihazKonumuOku`/`adresAdaylariGetir` dikişlerinden geçer — widget testleri platform kanalına ve ağa hiç uzanmaz.
- **Kill-switch'i derleme sabitinden OKUYAN widget test edilemez; koşul widget'ın İÇİNE alınır ve widget KOŞULSUZ mount edilir (2026-07-28 saha bulgusu).** `GuncellemeBanti` aylarca hiçbir ekrana bağlanmamıştı: `sessizKontrol()` açılışta koşuyor, `surum.json`u çekiyor, yeni yapımı buluyor ve `ValueNotifier`ları dolduruyordu — ama onları dinleyen hiçbir widget ağaçta yoktu. Hiçbir şey çökmedi, hiçbir şey de olmadı; boru hattının son halkası sessizce kopuktu ve mevcut testlerin tamamı saf fonksiyon testi olduğu için hiçbiri bunu göremezdi. Kök neden bağlantının unutulması DEĞİL, **bağlantının test edilemez olmasıdır**: bant `guncellemeKapaliMi` sabitini kendi içinde okuyup erken dönüyordu ve varsayılan `flutter test` altında (kanal `magaza`, yapım 0) o sabit HEP true — yani bandı mount eden bir test yazılsaydı bile hiçbir şey kanıtlamazdı. Düzeltme iki parçalı: (1) kill-switch `kapali` parametresiyle ENJEKTE EDİLEBİLİR, (2) kabuk bandı `if` ile SARMAZ, koşulsuz mount eder — mağaza garantisi aynen korunur (kapalıyken yükseklik sıfır, servis zaten ağa çıkmamıştır) ama artık `find.byType(GuncellemeBanti)` ile "ağaçta mı" sorusu varsayılan test koşusunda sorulabilir. **Genel kural: bir özelliğin çalıştığını kanıtlayan test, o özelliği kapatan bayrağın altında yazılamıyorsa bayrak yanlış yerdedir.** Ayrıca `SafeArea` yalnız ÇİZİLEN bandın etrafındadır: boş widget'ı sarmak her ekranın tepesinde hayalet bir boşluk bırakır, üst üste iki bant ise durum çubuğu boşluğunu iki kez ekler (`ustBosluk` bunu çözer).
- **Sürüm ADI (0.9.0) pilot boyunca SABİTTİR; derlemeleri ayıran şey yapı numarasıdır ve bu bir arıza değil karardır.** `pubspec.yaml` sürüm adını elle yönetir (mağaza çıkışında 1.0.0 olacak); her commit'te değişen `+N` git sayacından türer. Ayarlar ekranı bu yüzden `Sipario 0.9.0 (N)` biçiminde YAPI NUMARASINI DA gösterir — saha testinde "hangi APK?" sorusunu tek başına o cevaplar. DİKKAT: `--split-per-abi` ile derlenen arm64 APK'da `PackageInfo.buildNumber` ABI sapmasını taşır (2000+N), evrensel APK'da taşımaz (N); yani ayarlarda görünen sayı ile `surum.json`daki `yapim` aynı olmak ZORUNDA DEĞİLDİR. Karşılaştırma zaten `PackageInfo`ya değil `--dart-define=SIPARIO_YAPIM` sabitine dayanır (bkz. yukarıdaki karar); ayarlardaki sayı yalnız TEŞHİS içindir.
- **Bir saha hatası düzeltilirken AYNI kuralın diğer kullanım yerleri de aranır; "düzelttim" demek yalnız o ekran için geçerlidir (2026-07-28).** 2026-07-27'de sipariş satırındaki Ara·WhatsApp·Konum düğmelerinin yalnız toast bastığı bulunup `musteri_eylemleri.dart` yazıldı — ama MÜŞTERİ DETAYI ekranı aynı üç düğmeyle o gün bağlanmadan kaldı ve `url_launcher henüz bağımlılıkta yok` yorumu paket eklendikten sonra bile dosyada durdu. Kod incelemesi bunu görmedi çünkü ekranda bozuk bir şey yok: düğme var, dokunuş var, hatta mesaj bile çıkıyor — yalnız asıl iş yapılmıyor. Ders TEST TARAFINDA da geçerli: eylemin ÇAĞRILDIĞINI sınamak yetmez, HARİCİ UYGULAMANIN AÇILDIĞI (`uriAcici` dikişinde hangi URI denendiği) sınanmalıdır; sipariş tarafının testleri yeşilken müşteri detayı aylarca ölüydü. Bu, aynı vardiyada bulunan "güncelleme bandı hiç mount edilmemiş" arızasıyla AYNI aileden: kopmuş son halka, hata üretmez — yalnız sessizlik üretir.
- **"Uygulamayı kapatıp açtım" SÜREÇ ÖLDÜ demek DEĞİLDİR; periyodik/açılışa bağlı her kontrol ÖNE GELMEYE de bağlanır (2026-07-28 saha bulgusu).** Güncelleme kontrolü yalnız `initState`te koşuyordu; bayi son kullanılanlardan kaydırınca çoğu Android (özellikle Xiaomi/Samsung) uygulamayı bellekte bırakıyor, Dart isolate yaşamaya devam ediyor ve `initState` bir daha koşmuyor — bant ancak "zorla durdur" sonrası çıkıyordu ve hiçbir bayi bunu yapmaz. Senkron motoru bu dersi bir kez ödemişti (üç tetikleyici: zamanlayıcı · öne gelme · ağın dönüşü) ama güncelleme kontrolü aynı `didChangeAppLifecycleState`e bağlanmadan kalmıştı. Öne gelme SIK olduğu için tetikleyiciye 15 dakikalık aralık eklendi; aralık son DENEME zamanını sayar (başarısızı da) — çevrimdışıyken her öne gelmede yeniden denemek pil yakar, hiçbir şey kazandırmaz. Karar `kontrolGerekli()` saf fonksiyonunda ve `sessizKontrol(kapali:)` test yoluyla sınanabilir; ayrıca `istekSayisi` sayacı "tetikleyici bağlı mı" sorusunu ağ olmadan cevaplar (asıl arıza ağ yanıtı değil, HİÇ ÇAĞRILMAMASIYDI).
- **Durum çubuğu ikon rengi, üstte BANT olup olmadığına bakmak ZORUNDADIR.** Ana ekranın tepesi koyu hero'dur ve kabuk ikonları orada beyaza zorluyordu; güncelleme bandı araya girince durum çubuğunun altındaki artık hero değil AÇIK RENKLİ BANT oluyor ve saat/pil okunmaz hâle geliyordu (cihazda görüldü). Aynı kusur çevrimdışı ve grace bantlarında da vardı, yalnız onlar nadir göründüğü için fark edilmemişti. Karar `heroDurumCubugu(anaSekme:, kilit:, bantVar:)` saf fonksiyonuna çekildi — **yeni bir bant eklendiğinde `bantVar`a katılmalıdır**. Bandın görünürlüğü ise `guncellemeBandiGorunurMu()` ile TEK yerde tanımlı: hem bandın kendisi hem kabuk onu okur; iki kopya koşul ayrıştığı gün ikonlar yine okunmaz olurdu. Kabuk ayrıca `durum` bildirimini DİNLER — bant kendi builder'ıyla tazelense de onu saran `AnnotatedRegion` kabuğun `build`indedir.
- **"Bölge" alanı üründen KALDIRILDI; `customer_addresses.region` kolonu ŞEMADA UYUYOR (kullanıcı kararı 2026-07-28: "gerek yok, tamamen kaldır").** Semt/ilçe zaten adres metninin içine yazılıyordu; iki ayrı alan aynı bilgiyi iki kez soruyor ve esnafın en yavaş adımını (yazmak) uzatıyordu. Kaldırılan: form alanı + mikrofonu, liste satırı ve detaydaki gösterim (`adresGosterimi` artık tek parametreli), çağrı kartındaki "adres — bölge" birleşimi (`CagriKisi.bolge` silindi) ve coğrafi kodlama sorgusundaki bölge ipucu. **Kolon DÜŞÜRÜLMEDİ:** senkronlanan bir kolonu kaldırmak şema + senkron sözleşmesi değişikliğidir ve eski kayıtların verisini siler; kolon nullable kalır, yeni kayıtlarda HİÇ doldurulmaz, düzenlemede eski değer KORUNUR (`region: eskiAdres?.region`) — alan ekrandan kalktı diye kayıttaki veriyi ilgisiz bir düzenlemede silmek sessiz veri kaybı olurdu. Coğrafi kodlama sonucu: sağlayıcıya artık yalnız adres metni gidiyor; ölçüldü — "Sarampol Cad. No:10, Muratpasa" tek ve doğru adayı buluyor, ilçe hiç yazılmazsa Yandex aynı adlı sokakları başka illerde de önerebilir (aday satırında il GÖRÜNÜR, seçim kullanıcıda).
- **Tam sayfa PUSH edilen her ekran `Scaffold` + `SafeArea` ister; `ColoredBox`+`Column` yetmez.** Çağrı geçmişi (`CagriGunluguEkrani`) ikisi olmadan yazılmıştı: başlık durum çubuğunun ALTINA giriyor, liste altta gezinme çubuğunun altından taşıyordu (cihazda görüldü). Widget testleri bunu göremez çünkü test ağacı ekranı kendi `Scaffold`una sarıyor ve sahte cihazda sistem çentiği yok — kusur YALNIZ gerçek cihazda görünür. Liste alt boşluğuna `MediaQuery.viewPaddingOf(context).bottom` eklenir: sabit bir boşluk, jest çubuğu olan ve olmayan cihazlarda tutmaz.
- **Yandex Türkiye'de KAPI NUMARASINI katı eşleştirir; numara veritabanında yoksa sokağa DÜŞMEZ, sıfır döner — bu yüzden numarayı atıp bir kez daha soruyoruz (2026-07-28, gerçek yanıtla ölçüldü).** Saha bulgusu: "Şirinyalı Mah. 1497. Sk. No: 9 Muratpaşa/Antalya" → `found=0`; aynı adres numarasız → `found=1` (sokak sistemde VAR, yalnız 9 numaralı bina kaydı yok). Kullanıcıya "bu adres bulunamadı" demek YANLIŞ: adres doğru, eksik olan sağlayıcının bina verisi. `YandexGeocoder::ara()` artık iki basamaklı: tam sorgu boş dönerse `kapiNumarasiniAt()` ile kapı/daire atılıp tekrar sorulur ve dönen adayın kesinliği ZORLA `sokak`a çekilir — Yandex "exact" dese bile kapı numarası artık sorgunun parçası değildir, bunu "bina" diye sunmak kuryenin güvendiği bir yalan olurdu (aday satırındaki "sokak yaklaşık" uyarısı tam bu iş için vardı). **Sokak adındaki sayı KORUNUR** — Türkiye'de sokaklar numaralıdır ("1497. Sok."), "bütün sayıları at" demek sokağın kendisini silmek olurdu; yalnız açıkça kapı/daire olduğu yazan biçimler atılır (`No: 9`, `no12`, `9/A`, `Daire 5`, `Kat 2`, `D:3`). İki regex tuzağı ödendi: numaradan sonraki harf eki YALNIZ `/` veya `-` ile bağlıysa yutulur (düz boşluk sonrası gelen kelime bir sonraki adres parçasıdır — "No: 9 Muratpaşa"da ilçe siliniyordu), ve `(?:daire|blok|kat)` alternatifleri UZUNDAN KISAYA sıralanır (`d|daire` sırasında regex yalnız "d"yi yiyip geriye "aire 5" bırakıyordu). Atılacak bir şey yoksa ikinci sorgu HİÇ koşmaz (boşuna kota); karşılaştırma normalize edilmiş hâller arasında yapılır, yoksa sarkan nokta kırpması bile "değişti" sayılırdı.
- **Sipariş satırı TUTARI yazar, borcu yazmazdı; kalan borç artık kendi pilinde ve kendi kutusunda görünür (saha bulgusu 2026-07-29).** Bayi: *"borçlu sekmesinde sipariş teslim edildikten sonra borç rakamı gözükmüyor, tahsil et diyinceye kadar da borcu göremiyoruz."* Haklıydı ve kusur veresiye teslimde GÖRÜNMÜYORDU: orada sipariş tutarı ile borç aynı sayıdır, satırın sağındaki rakam tesadüfen doğru okunuyordu. Kısmi ödemede ikisi ayrışıyor (200 ₺ sipariş, 80 ₺ borç) ve ekran hangisine baktığını söylemiyordu. Kalan borç TEK saf kuraldan gelir (`siparisKalanBorcu`): teslim edilmemiş sipariş borç DEĞİLDİR (deftere borç teslim anında yazılır — "Borçlu" sekmesinin 2026-07-27 düzeltmesiyle aynı kural) ve fazla tahsilat borcu eksiye çevirmez, 0 döner (fazlası müşterinin ÖNCEKİ borcunu kapatır ve MÜŞTERİ bakiyesinde görünür; siparişe "−50 ₺ borç" yazmak yanlış yerde doğru sayı göstermektir). Tahsilat verisi `watchOrders`a KOLON OLARAK EKLENMEDİ: korelasyonlu alt sorgu her sekmede her satır için koşardı; ayrı bir akış defteri tek taramada gruplayıp Dart tarafında eşler (`satirlar`/`adresler`/`telefonlar` akışlarının deseni) ve testlerin doğrudan çağırdığı `watchOrders` sözleşmesi hiç değişmez. Sipariş detayında ayrıca `.sd-odendi`nin borç eşi bir kutu vardır ve "Tahsilat Al" düğmesi TUTARI DA YAZAR — borcu öğrenmek için bir para YAZMA akışına girmek zorunda kalmak, şikâyetin asıl sebebiydi. Kutuda sipariş borcu ile müşterinin toplam borcu AYRI satırlardır ve yalnız farklıyken ikisi birden yazılır: tahsilat toplam bakiyeden yapılır, tek sayı göstermek hangisinin tahsil edileceği konusunda yanıltırdı.
- **Ana ekrandaki "Açık Veresiye" kutusu "Borçlular" oldu ve kendi ekranını açar (kullanıcı kararı 2026-07-29).** Rakam aynı (tahsil edilmemiş toplam) ama kutunun VAADİ değişti: eskiden dokunuş müşteriler sekmesine gidiyor, bayiyi borcu olmayan yüzlerce kaydın arasına bırakıyordu — oysa o kutuya dokunmanın tek sebebi "kim borçlu, ne kadar, hangi siparişten" sorusudur. Yeni ekran yalnız `balance_kurus > 0` müşterileri listeler (müşteri listesi ve bento sayacı ile AYNI ölçüt — üç yüzey aynı kümeyi konuşur), her kartın altında o müşterinin ÖDENMEMİŞ teslim edilmiş siparişleri durur ve sipariş sorgusu borçlu kimlikleriyle DARALTILIR (defter yıllara yayılınca da liste kısa kalır). İki sayı toplanmaz ve karıştırılmaz: DEFTER bakiyesi (tahsilatın yapıldığı tek doğru kaynak) ile siparişlerden kalanların toplamı eşit olmak zorunda değildir — elle düzeltme, sipariş dışı borç ve avans ikisini ayırır; fark varsa ekran "bu siparişler dışında X ₺ borç (defter kaydı)" diye SÖYLER, sessizce bir tarafı seçmek bayiye "rakamlar tutmuyor" dedirtirdi. Borcu tamamen sipariş dışı olan müşteri listeden DÜŞMEZ (kartı düşürmek borcu görünmez yapardı), kartında sebebi yazar. Ekranı KABUK açar, bento değil: yazma yetkisi (abonelik kilidi) ve rol orada bilinir.
- **Alt navigasyondaki + tuşu artık iki seçenek sunar: "Müşteri Ekle" · "Sipariş Ekle" (kullanıcı kararı 2026-07-29).** Öncesinde FAB tek dokunuşla doğrudan sipariş formunu açıyordu ve bu bilinçli bir karardı (tasarımın `.fabpop*` CSS ailesi ölü kural sayılıp kaldırılmıştı); karar tersine çevrildi çünkü müşteri ekleme YALNIZ Müşteriler ekranının "Yeni"sinde yaşıyordu ve yeni müşteri kaydı esnafın en sık ikinci işidir — onu bir sekme değişikliğinin arkasına koymak kurulum sürtünmesini artırıyordu. Menüyü KABUK açar, `AltNav` DEĞİL: navigasyon hangi ekranların var olduğunu bilmez ve yazma kapısı (`_yazilabilir`) kabukta durur; bu yüzden geri çağrımın adı `onEkle`dir, "yeni sipariş" değil. Menü İKİ SATIRDIR ve üçüncü bir şey eklenmez — FAB'ın değeri bir dokunuşla en sık iki işi başlatmasıdır, dolan bir menü onu bir alt menüye çevirir. Kapı tek yerdedir (FAB `_yazilabilir` değilken pasif çizilir); sheet içinde ikinci bir kontrol yapılmaz, iki yerde ayrı koşul ayrışabilen iki kural demektir.
- **Saha sunucusu script'i araçlarını KENDİ bulur; PATH bir sözleşme değildir (2026-07-29 saha hatası).** `SUNUCU-BASLAT.bat` ham bir PowerShell istisnasıyla düşüyordu: *"Start-Process : ... Sistem belirtilen dosyayı bulamıyor."* Sebep `Start-Process -FilePath "php"` idi — bu makinede php PATH'te yok (iki ayrı yerde kurulu: XAMPP 8.2 ve Laragon 8.3), cloudflared ise hiç kurulu değildi. Mesaj NE eksik olduğunu söylemiyordu ve iki geliştiricinin nöbetleşe ayrı makinelerde çalıştığı bir projede "benim makinemde PATH'te var" bir varsayımdır. Script artık php'yi sırayla PATH → Laragon (en yeni sürüm) → XAMPP'ta arar ve **sürümü doğrular** (8.2+; yanlış php ile açılan sunucu anlaşılmaz hatalarla düşer, "php yok" demek "yanlış php" demekten iyidir), cloudflared'i bulamazsa resmî sürümden bir kez indirir (`scripts/.araclar/`, .gitignore'da — depo public, 50 MB'lık ikili commit edilmez) ve indirmeyi boyut + `--version` ile doğrular (yarım inen exe, teşhisi zor bir tünel arızası üretirdi — APK indirmesindeki boyut kontrolünün aynı disiplini). Araç kontrolü Docker'dan ÖNCE koşar: eksik bir araç için iki dakika Docker beklemek kullanıcının zamanını boşa harcar.
- **`php -d extension=...` ve `PHP_INI_SCAN_DIR`, `artisan serve` ile ÇALIŞMAZ — eklenti php.ini'de açık olmak zorundadır.** İkisi de denendi ve ölçüldü (2026-07-29). Kök sebep: `artisan serve` isteklere kendisi bakmaz, `php -S ... server.php` ile AYRI bir süreç doğurur; komut satırı bayrakları o çocuğa geçmez ve Laravel'in `ServeCommand`'i çocuğa yalnız beyaz listedeki ortam değişkenlerini aktarır (`$passthroughVariables` — listedeki `HERD_PHP_8x_INI_SCAN_DIR` girdileri tam da bu yüzden vardır). Ana süreçte `extension_loaded('pdo_pgsql')` **true** dönerken sunucu hâlâ "could not find driver" diyordu; teşhisi zorlaştıran şey buydu. Script bu yüzden geçici çözüm denemez: eklenti kapalıysa **hangi php.ini dosyasında hangi satırın açılacağını yazıp durur**. php.ini'yi script'in kendisi düzenlemez — o dosya makinenin tamamı içindir (aynı PHP'yi kullanan diğer projeler de onu okur) ve başkasının ortamını sessizce değiştirmek bu script'in işi değildir.
- **"Konteyner healthy" ile "veritabanı kullanılabilir" iki ayrı sorudur; script artık şemayı da kurar.** Bu makinede `sipario_db` sağlıklıydı ama şema BOMBOŞTU (tek tablo yok) — giriş isteği daha oturum açmadan `cache` tablosunu okuyup 500 veriyordu ve script'in "API hazır mı" kontrolü bunu 422 beklediği için yakalıyor ama SEBEBİNİ söyleyemiyordu. Sunucu açılmadan önce `migrate --database=pgsql_owner --force` (owner ŞART: uygulama rolü `sipario_app`'in DDL yetkisi yok — Faz 1 izolasyonu) ve idempotent `DemoSeeder` koşulur. **`migrate:fresh` bu script'te ASLA çağrılmaz** (kırmızı çizgi #2: para kayıtları silinmez); `migrate` yalnız ek yapar, bekleyen göç yoksa bir saniyede çıkar.
- **`artisan serve` bir SARMALAYICIDIR; süreç temizliği PID'e değil KOMUT SATIRI kalıbına bakmalıdır.** Script yalnız `*artisan serve*` kalıbını öldürüyordu ama istekleri karşılayan asıl süreç onun çocuğu olan `php -S 127.0.0.1:8000 ...server.php`tir; ölçüldü: makinede **5 yetim süreç** birikmişti. Sonucu sinsidir — ölü sunucu 8000 portunu tutmaya devam eder, yeni sunucu porta bağlanamayıp sessizce ölür ve "API hazır mı" kontrolü ESKİ sürece cevap verdirip YEŞİL yanar: ekranda her şey yolunda görünürken bayi bir önceki çalıştırmanın koduna bağlanır. Hem açılıştaki temizlik hem kapanış artık iki kalıbı birden (`artisan serve` + `-S 127.0.0.1:8000`) hedefler; tünel adresi alınamadığında da php ile cloudflared BİRLİKTE kapatılır (eskiden cloudflared sahipsiz kalıyordu).
- **Güncelleme adresleri ÖNBELLEKSİZ istenir; CDN, yayından saatler sonra bile bayat cevap veriyordu (2026-07-29 saha hatası, ölçüldü).** Bayi "güncelleme gelmiyor" dedi ve haklıydı: CI derlemeyi yayınlamıştı (`yapim: 151`) ama `surum.json`u DÜZ adresinden çeken üç ardışık istek de eskisini (`yapim: 149`) döndürdü — aynı adres tek bir sorgu parametresiyle istendiğinde yenisi geldi. Yanıt başlıkları sebebi yazıyordu: `X-Cache: HIT, HIT` · `Age: 930` · `Last-Modified` yayından dokuz saat öncesi. Dikkat: kaynak zaten `Cache-Control: no-cache` gönderiyordu ve İŞE YARAMADI — araya giren katman onu dinlemedi. Bu yüzden çözüm başlık değil ADRES: benzersiz bir `?t=<saniye>` parametresi ÖNBELLEK ANAHTARINI değiştirir ve ara katmanın seçimine bırakılmaz (başlıklar yine gönderilir, uyumlu katmanlarda bedava kazanç). Aynı disiplin APK indirmesine de uygulanır: bayat bir paket ya boyut kontrolüne takılıp sonsuza dek "bozuk indirme" üretir ya da — boyutlar tesadüfen tutarsa, ki bu turda arm64 paketinin boyutu iki derlemede birebir aynıydı — ESKİ derlemeyi "güncelleme" diye kurdurur; ikincisi teşhis edilemez. **Arızanın ailesi tanıdık:** hiçbir şey çökmez, hiçbir günlük satırı düşmez, yalnız hiçbir şey olmaz (güncelleme bandının hiç mount edilmemesi ve öne gelmede kontrol edilmemesiyle aynı aile). Ölçülen değerler ve gerekçe `onbelleksizAdres` başlığında; kural dört testle çivilendi.
- **Bir testin dayanağı ÇERÇEVENİN O ANKİ DAVRANIŞI ise, çerçeve güncellendiğinde ürün sağlamken kalite kapısı kilitlenir (2026-07-29).** `font_variable_test.dart`ta bir "kontrol grubu" testi vardı: `fontVariations` verilmezse değişken fontta 400 ile 800 AYNI genişlikte çıkmalı — çıkmazsa üstteki iki testin bir şey kanıtlamadığı varsayılıyordu. Flutter 3.44.6'da bu artık doğru değil; ölçüldü (Sora, 40 punto): eksenli 400 = 367,64 · eksenli 800 = 379,72 · **eksensiz 400 = 367,64 · eksensiz 800 = 379,72** — dördü birebir aynı, yani motor `fontWeight`i wght eksenine kendi eşliyor. Bedeli doğrudan görünür oldu: `flutter test` kırmızı → Stop hook'undaki kalite kapısı mobil commit'e izin vermiyor → bir vardiyanın tamamı (borç görünürlüğü, Borçlular ekranı, FAB menüsü) commit edilmeden bekliyordu ve bayi "güncelleme neden gelmedi" diye soruyordu. **Kırmızı bir kapı yalnız bir test değil, TÜM DAĞITIM HATTIDIR.** Test davranışa değil SÖZLEŞMEYE bağlandı: `SipText` stilleri `fontVariations` taşımak ve eksen değeri `fontWeight` ile uyuşmak zorunda (ekseni silmek eski motorlarda ve sahadaki eski cihazlarda tipografiyi düzleştirir — korunması gereken budur, çerçevenin bugünkü nezaketi değil).
- **Müşteri ve sipariş kodları SUNUCUDA atanır; istemci kendi numarasını üretmez (kullanıcı isteği 2026-07-29: "her müşterinin bir kodu olmalı, m100 değil 100 101 102 gibi; her siparişin bir kodu olmalı #xxx gibi").** Öncesinde mobildeki `musteriKod()` UUID'nin son üç RAKAMINDAN "M-007" üretiyordu: bu bir kimlik değil SÜSTÜ — iki müşteri aynı üçlüyü alabiliyor, sayı hiçbir sırayı anlatmıyor ve bayi "102 numaralı abone" diyemiyordu. Kod artık `customers.code` / `orders.code`, kiracı içinde sıralı ve 100'den başlıyor. **Neden istemci üretmiyor:** sıra numarası TEK bir dağıtıcı ister; iki cihaz çevrimdışıyken kendi numarasını üretseydi ikisi de aynı sayıyı alır ve senkronda biri DEĞİŞTİRİLMEK zorunda kalırdı — bayinin kâğıda yazdığı "#248" ertesi gün başka bir siparişe ait olurdu. Geç gelen bir numara, değişen bir numaradan iyidir; kodsuz kayıtta arayüz rozeti HİÇ çizmez (uydurma numara basmaz). Desen zaten depoda vardı: `customers.balance_kurus` da sunucu türetimlidir ve istemciden yazılabilir kolonlar listesinde yoktur; `SyncPayload::change` modeli `refresh()`leyip serialize ettiği için yeni kolon senkrona kendiliğinden akar. **Atama MODEL DÜZEYİNDEDİR** (`creating` olayı), senkron uygulayıcısında değil: müşteri üç ayrı yoldan doğuyor (senkron push · demo seeder · konsol komutları) ve kodu yalnız senkron yoluna koysaydık diğerlerinden doğanlar kodsuz kalır, bu da ancak SAHADA fark edilirdi. Eşzamanlılık `pg_advisory_xact_lock` ile çözülür: iki cihaz aynı anda push ederse ikisi de aynı MAX'ı okur, ikincisi kısmi unique indekse çarpar ve olay 'rejected' olurdu — yani kayıt kaybolmuş GİBİ görünürdü (kırmızı çizgi #3). Kilit kiracı başınadır, transaction sonunda kendiliğinden düşer ve farklı bayiler birbirini beklemez. Kiracı başına Postgres sequence'i ELENDİ: şema yönetimini kiracı ömrüne bağlar ve rollback'te sayı yakıp "101'den 103'e neden atladı" sorusu üretir.
- **Sipariş satırında hangi kodun görüneceği bayi ayarıdır; sipariş DETAYINDA ikisi de görünür.** Kullanıcı: "isteyen firma siparişte müşteri kodu, isteyen firma sipariş kodu görsün, bunu ayarlardan kendi ayarlayabilsin". Ayar `tenant_settings.order_code_display` ('musteri' varsayılan | 'siparis') — **KİRACI düzeyinde, cihaz-yerel DEĞİL**: iki telefonlu bir bayide aynı liste iki farklı numara gösterirdi ve "sende 102 yazıyor bende #248" trafiği doğardı. (Karşılaştırma: sürükleme tutamacının sağ/sol tercihi cihaz-yereldir — o EL ALIŞKANLIĞIDIR, bu ise bayinin İŞ SÖZLÜĞÜ.) Varsayılan 'musteri': liste bugün de müşteri kimliğini gösteriyor ve telefonla sipariş alan bir bayide satırda aranan şey müşteridir. Ayar yalnız LİSTEDEKİ dar alanı paylaştırır; detayda yer var ve orada ikisi birden çizilir — birini ayara feda etmek, tercihini değiştirmeden ulaşılamayan bir bilgi yaratırdı. Sunucu tarafında değer BEYAZ LİSTEYE tabidir (tanınmayan değer varsayılana düşer): istemci sürümleri ayrışabilir ve serbest metin, kararı okuyan her yüzeyi bilinmeyen bir dala sokar. **Yazma tuzağı:** `tenant_settings` LWW upsert'tir, yani sunucu satırı gelen payload'la DEĞİŞTİRİR; tercih işletme profili formuna ait olmadığı için payload'da eksik kalsaydı bayi adresini düzeltince tercihi sessizce sıfırlanırdı. Bu yüzden `save()` açıkça verilmeyen tercihi MEVCUT değerden taşır ve ayrı bir `siparisKoduTercihiKaydet` metodu profilin geri kalanını olduğu gibi geçirir.
- **Açık siparişte durum pili yerine BEKLEME SÜRESİ yazar ("12 dk").** Kullanıcı isteği: "açık siparişin kartta durumu açık yazmak yerine siparişin oluşturulalı kaç dakika olduğu yazmalı, kutu içinde". Gerekçe sahadan doğru: "açık" bilgisi zaten sekmenin adında (Açık sekmesi) — satırda merak edilen BEKLEME SÜRESİDİR, geciken teslimat böyle fark edilir. Biçim kademeli: 1 dk altı "yeni" (0 dk yazmak siparişin girilmediğini düşündürür) · 60 dk'ya kadar dakika · sonra saat+dakika (**ilk gün dakika ÖNEMLİDİR**: 1 sa 5 dk ile 1 sa 55 dk arasındaki fark bayinin müşteriyi arayıp aramayacağını belirler, saate yuvarlamak o kararı görünmez yapar) · 24 saatten sonra gün. İLERİ tarihli damga "yeni" sayılır — cihaz saati kaymışsa "−3 dk" yazmak veriye güveni sarsar. Teslim/iptal edilmiş siparişte durum pili KALIR: kapanmış bir siparişte geçen süre bir şey anlatmaz. **Süre kendiliğinden ilerlemek zorundadır** ve bu ücretsiz değildir: Drift akışı veri değişmedikçe tik yayınlamaz, yani widget yeniden çizilmezse "3 dk" saatlerce öyle kalır ve YANLIŞ bilgi verir. Çözüm UYGULAMADA TEK bir `Timer` (60 sn) + paylaşılan `ValueNotifier`: 40 açık siparişte 40 zamanlayıcı dönmez, dinleyici yokken zamanlayıcı HİÇ kurulmaz ve son dinleyici gidince durur (arka planda pil yakan sessiz bir sayaç bırakmaz).
- **Veriyi değiştiren her migration, o veriyi SENKRONA da düşürmek zorundadır (2026-07-29 saha hatası, aynı gün bulundu).** Kod atama migration'ı mevcut müşteri/siparişlere düz SQL ile kod yazdı ama `sync_changes` kaydı üretmedi. İstemci ilk snapshot'tan sonra YALNIZ delta çeker, dolayısıyla kodlar sunucuda doğru dururken telefona HİÇ ulaşmadı. Ölçüldü: 11 kodlu müşteri, kod taşıyan senkron kaydı **sıfır**. Bayi "kodları göremiyorum" dedi; sunucuya bakan geliştirici kodların orada olduğunu görüp sorunu istemcide arardı. Arıza ailesi tanıdık ve bu depoda dördüncü kez ödendi: hiçbir şey çökmez, hiçbir günlük satırı düşmez — yalnız hiçbir şey olmaz (mount edilmeyen güncelleme bandı · öne gelmede koşmayan kontrol · bayat CDN cevabı). Düzeltme ayrı bir migration'la delta üretti (kiracı başına seq ayırıp `tenant_sync_state`i ilerleterek — push yolunun aynısı). **Testin dersi ayrıca kaydedildi:** `kod_senkron_yaniyla_istemciye_doner` yalnız SNAPSHOT'ı sınıyordu ve yeşildi; kurulu bir cihazın gördüğü şey DELTA'dır. Snapshot'ı sınayan bir test, delta yolunu kanıtlamaz.
- **Kalite kapısının API bölümü php PATH'te olmadığı için SESSİZCE devre dışıydı (2026-07-29).** `$phpOk = Get-Command php` başarısız olunca script bölümü "kurulum eksik" diye atlıyor ve kapı yine "yeşil" diyordu: pint · phpstan · `php artisan test` bu makinede aylardır HİÇ koşmamış, API değişiklikleri doğrulanmadan commit edilmişti. Kırmızıyı uzaktaki CI yakalıyordu (6 dosyada stil hatası) — yani yerel bekçinin işini hat yapıyordu ve geri bildirim dakikalar sonra, başka bir ekranda geliyordu. **Bir bekçinin sessizce devre dışı kalması, bekçinin hiç olmamasından kötüdür: "yeşil" raporu güven üretir.** Kapı artık php'yi kendisi buluyor (PATH → Laragon → XAMPP, saha sunucusu script'iyle aynı sıra) ve araçları `.bat` sarmalayıcılarıyla değil bulunan php ile koşturuyor — sarmalayıcılar içeride düz `php` çağırdığı için çözülen sorunu geri getiriyordu.
- **Gün sonunun ekseni KAPSAM değil GÜN oldu (kullanıcı isteği 2026-07-29).** Eski "Arşiv" bölümü kapanış kayıtlarını düz liste hâlinde gösteriyordu ("Gün hesabı · Emre · Hakan" alt alta) ve satırlar tarihi değil KAPSAMI yazıyordu; bayi "28 Temmuz ne oldu" diye bakamıyordu. Yeni yapı: tepede BUGÜN (kapatma eylemi ve bugünün kapanışları orada kalır — mutabakatın kanıtı kapatıldığı anda görünmek zorunda), altında GEÇMİŞ günler listesi (`28.07 Salı · 2.100 ₺ · 18 teslimat`), bir güne dokununca gün detayı: kasa · kurye kırılımı · satılan ürünler · kapanış kayıtları. **Liste HAREKET OLAN her günden doğar, kapanış kayıtlarından değil** (kullanıcı kararı): bayi bir günü kapatmayı unutursa o günün cirosu uygulamadan okunamaz hâle gelirdi — en çok ihtiyaç duyulan anda veriyi gizlemek. Kapatılmamış gün listede kalır ve uyarı rengiyle işaretlenir; kilit ikonu ona yanlış bir güven verirdi. Ürün dökümü YALNIZ teslim edilenleri sayar (kullanıcı kararı): açık sipariş henüz satılmış değildir ve kasa özetiyle aynı kümeye bakmak zorundayız — iki rakam tutmazsa bayi hangisine güveneceğini bilemez. Gruplama ürün KİMLİĞİNE değil ADINA göredir: serbest satırların ve silinmiş ürünlerin productId'si yoktur, ada göre gruplamak ikisini de dökümde tutar (aynı adı taşıyan iki ürünün birleşmesi, satılan bir kalemi dökümden düşürmekten iyidir). Gün listesi TEK GEÇİŞTE gruplanır — her gün için ayrı sorgu, yıllara yayılmış bir defterde ekranı dakikalara çıkarırdı. O gün işi olmayan kuryenin kartı çizilmez ve sayım yapılmamış kasaya "fark 0" YAZILMAZ (mutabık göstermek olurdu).
- **Aşağı çekerek yenile uygulama genelinde açıldı; ana ekranda senkron + GÜNCELLEME kontrolü koşar (kullanıcı isteği 2026-07-29).** Veri yerelden okunuyor ve senkron 2 dakikada bir kendiliğinden dönüyor — yani ekran "eski" görünse bile yakında tazelenecek, ama bayi bunu BİLMİYOR ve bekleyecek bir gerekçesi yok. Aşağı çekmek telefonda "şimdi bak" demenin evrensel jestidir; olmadığında kullanıcı uygulamayı kapatıp açıyordu (sahadaki "ancak kapatıp açınca senkronize ediyor" şikâyeti kısmen bu boşluktan besleniyordu). Jest beş ekranda açıldı (ana · sipariş · müşteri · borçlular · gün sonu + ürün/kurye/muaf/çağrı geçmişi/gün detayı); FORMLARDA AÇILMADI (sipariş formu, işletme profili) — yarım doldurulmuş bir formda yenileme, kullanıcının yazdığını riske atan bir jesttir. **ELLE SIRALAMA kipinde de kapalıdır**: aşağı çekme ile sürükleme aynı jesttir, ikisi birlikte açıkken kullanıcı sırayı bozmadan listeyi kaydıramaz. Servis her ekranın imzasına eklenmedi, modül düzeyinde bir kez bağlanıyor (`yenilemeyiBagla`) — beş ekran farklı yollardan (sekme · `Navigator.push`) kuruluyor ve callback'i taşımak ekranların yarısını yalnız aktarma amacıyla değiştirmek olurdu; `guncellemeServisi` tekilinin aynı deseni. `syncNow()` zaten eşzamanlı çağrıları birleştirdiği için zamanlayıcının turu sürerken çekilirse ikinci tur açılmaz. **FutureBuilder tabanlı ekranlarda (gün sonu, gün detayı) yenileme senkronu VE ekranın kendi future'ını tazeler** — yalnız senkron koşsaydı gösterge döner, hiçbir rakam değişmezdi; sıra da önemlidir (önce senkron, sonra hesap).
- **"Sürüm güncel" çipi YALNIZ sunucuya gerçekten ulaşılmış bir kontrolden sonra çizilir.** Kullanıcı isteği: "güncelleme geldi mi kontrol etsin, gelmediyse senkron güncelin yanında sürüm güncel yazabilir." Üç durumda susar ve üçü de bilinçli: (1) henüz kontrol yapılmadıysa — hiç sorulmamış bir soruya "güncel" diye cevap vermek olurdu; (2) çevrimdışı denemede — ulaşılamayan sunucu hakkında "güncelsiniz" demek yanlış bilgidir; (3) güncelleme BULUNDUYSA — o durumu güncelleme bandı anlatır ve iki yüzey çelişemez. Dayanağı yeni `sonBasariliKontrol` bildirimidir: 200 alınıp gövde çözüldüğünde yazılır (güncelleme çıksın çıkmasın), yani "sunucuya ulaşıldı" ile "güncelleme yok" ayrı ayrı biliniyor. Mağaza derlemesinde kontrol hiç koşmadığı için çip de hiç görünmez.
- **Selam saate göre DÖRT kuşak; kuşaklar su bayisinin gününe göre biçildi (kullanıcı isteği 2026-07-29).** 06–11 "Günaydın" (dükkân açılır, siparişler girilir) · 12–17 "Kolay gelsin" (teslimatın en yoğun bandı; "iyi günler" burada fazla resmî kalıyordu, esnaf birbirine kolay gelsin der) · 18–21 "İyi akşamlar" (son teslimatlar, gün sonu kapanışı) · 22–05 "İyi geceler" (dükkân kapalı; uygulama o saatte açıksa ya hesap yapılıyordur ya gece nöbeti vardır, ikisinde de "günaydın" yanlış olur). Test gün dönümünün İKİ yakasını birden sınar (22:00 ve 05:00): tek yakayı sınayan bir test, "saat < 6" dalını unutan bir düzeltmeyi yakalamaz ve gece yarısı sessizce günaydına döner.
- **Bento kutularının iç düzeni ORTALANDI (kullanıcı isteği: "yazılar ve rakamlar çok dağınık").** Sola dayalı hâlde dört kutunun etiketi, rakamı ve alt satırı farklı uzunlukta olduğu için ızgara dört ayrı sol kenar üretiyordu; göz her seferinde hangi rakamın hangi etikete ait olduğunu yeniden kuruyordu. Ortalama dört kutuyu tek dikey eksende topluyor. Yalnız `textAlign` YETMEZ — kısa bir metin kendi içinde ortalanmış görünse de sütunun içinde sola yapışık durur; `crossAxisAlignment` ve `FittedBox` hizası da çevrildi ve test ikisini birden kilitliyor.
- **Müşteri DÜZENLEME ekranında da "Konum Al" var; tasarım dosyası ürün kararının önüne geçmez (kullanıcı isteği 2026-07-29).** Önceki davranış tasarımı izliyordu: `MusteriDuzenle` düz bir "Adres" etiketi gösteriyor (s-musteriler.jsx:353-354), konumun tek yolu müşteri DETAYINDAKİ koyu karttı ve bu bir testle kilitlenmişti. Sahada tutmadı — adres yanlışsa bayi zaten düzenleme ekranını açıyor, adresi düzeltiyor ve konumu TAM ORADA almak istiyor; kaydedip detaya dönüp ikinci bir ekrandan almak üç fazladan dokunuş ve akış kesintisi. **Tasarım dosyası ölçü/renk ÇATIŞMALARINDA bağlayıcıdır, ürün kararlarında değil** (kupon kaldırma ve bölge alanının silinmesiyle aynı çizgi). Düğmeyle birlikte ADAY LİSTESİ de açıldı: yalnız düğmeyi açsaydık "Konum Al" sunucuya gidip adayları alır ve hiçbir şey göstermezdi — bu depoda dört kez ödenen "kopmuş son halka" arızası (hata yok, sonuç da yok). Eski davranışı kilitleyen test tersine çevrildi, silinmedi: kararın değiştiği görünür kalsın.
- **Sipariş detayındaki teslimat adresine "Konum Güncelle" eklendi — CİHAZ konumu, adresten kodlama DEĞİL (kullanıcı isteği 2026-07-29).** İki iş bilinçle ayrı tutuldu (2026-07-28 kararının devamı): kodlama sokağı bulur ve bir ADAY listesi döndürür, doğrusunu kullanıcı seçer — teslimatın ortasındaki kuryeyi bir karar ekranına sokmak yanlış olurdu; cihaz konumu ise ÖLÇÜMDÜR ve tam o anda en doğrudur. Sahadaki değeri şudur: kurye kapıyı İLK KEZ bulduğunda oradadır ve o an tek dokunuşla kaydedilen pin, bir daha hiçbir kuryenin aynı kapıyı aramamasını sağlar. Ayrıntılar: düğme konum VARKEN de çizilir (kodlamadan gelen "sokak" kesinliğindeki pin yanlış kapıya işaret edebilir, kurye onu düzeltebilmeli) ama metin değişir — olmayan bir şeyi "güncellemek" yanlış okunurdu. Kapı `writable`dır, `_duzenlenebilir` DEĞİL: konum güncelleme KAPALI siparişte de anlamlıdır (kurye teslim ettikten sonra da kapının önündedir). Salt-okunur kipte düğme gizlenmez, sebebini söyler (Dilim 3 kararı). Adres metni/etiket DEĞİŞMEZ — akış yalnız koordinat ekler (`konumKaydet` sözleşmesi) ve zayıf ölçüm sessizce kaydedilmez: kurye "konum kayıtlı" yazısına güveniyor. Testler düğmenin çizildiğini değil VERİTABANINA YAZDIĞINI sınar.
- **Sihirbazın "pil" adımı ADI NE DİYORSA ONU AÇIYOR; otomatik başlatma AYRI bir düğme oldu (saha hatası 2026-07-29).** Bayi: *"kurulumdaki pil optimizasyonu izni arka planda otomatik çalıştırma iznini açıyor."* Haklıydı: adım "Pil optimizasyonu muafiyeti" yazıyor, `openBestSettingsScreen` ise Xiaomi/Oppo/Vivo/Huawei'de OTOMATİK BAŞLATMA bileşenini (`AutoStartManagementActivity` vb.) açıyordu; yalnız hiçbir OEM eşleşmediğinde gerçek pil ekranına düşülüyordu. İki zarar birden: (1) kullanıcı adı verilen ayarı açılan ekranda bulamıyor ve "uygulama bozuk" diyor, (2) **pil kısıtlaması HİÇ kaldırılmıyor** — oysa BRIEF'in en büyük korkusu tam olarak agresif pil yönetimidir ve MIUI arayan tanımayı İKİ ayrı mekanizmayla öldürebilir: otomatik başlatma kapalıysa servis hiç uyanmaz, pil kısıtlaması ise uyanmış süreci keser. Düzeltme ikisini ayırdı: `openBatterySettings` artık MIUI'nin uygulama-başına pil ekranını (`HiddenAppsConfigActivity`, paket adı ekstra ile → kullanıcı listede uygulama ARAMAZ), yoksa standart `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS`, o da yoksa uygulama ayrıntılarını açar; `openAutostartSettings` OEM listesini açar. **Sabit altı adım kararı korundu**: o karar ADIM SAYISIYLA ilgilidir (cihaza göre değişen liste "Adım 3/6" numaralarını iki telefonda farklı izne bağlıyordu), bir adımın içindeki düğme sayısıyla değil — adım artık iki düğme taşıyor ve her düğme ne açtığını YAZIYOR ("Pil Ayarını Aç" · "Otomatik Başlatmayı Aç"). İkinci düğme YALNIZ cihazda böyle bir ekran varsa çizilir (`hasAutostartSettings`): Pixel'de otomatik başlatma kavramı yoktur ve hiçbir yere gitmeyen bir düğme kullanıcıya "bir şeyi eksik yaptım" hissi bırakır. İkincil düğmeye dokunmak adımı "verildi" SAYMAZ — iki ayardan yalnız birini yapan kullanıcının durumunu gizlerdi. `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` izni yine İSTENMEZ (Play kısıtlı izni, kırmızı çizgi #6 ile aynı red riski); yalnız izin gerektirmeyen ayar ekranları açılır.
- **Vardiya kapanışı 2026-07-29 — dört SESSİZ arıza tek turda çıktı ve hepsi aynı aileden: hiçbir şey çökmez, günlüğe hiçbir şey düşmez, yalnız HİÇBİR ŞEY OLMAZ.** Bayat CDN cevabı (güncelleme bandı hiç düşmüyordu) · migration'ın senkron deltasına düşmemesi (kodlar sunucuda doğru, telefonda yok) · kalite kapısının php bulunamayınca API bölümünü sessizce atlaması ("yeşil" raporu, aylardır koşmayan pint/phpstan/test) · sihirbazın "pil" adımının otomatik başlatma ekranını açması (pil kısıtlaması hiç kaldırılmıyordu). Bu ailenin daha önceki üyeleri de kayıtlı: mount edilmemiş güncelleme bandı, öne gelmede koşmayan kontrol, yalnız toast basan eylem düğmeleri, `assignedTo` parametresinin gövdede hiç kullanılmaması. **Ortak ders: bir özelliğin ÇALIŞTIĞINI kanıtlayan tek şey, sonucunun GÖRÜLDÜĞÜ bir ölçümdür** — "kod yazıldı", "test yeşil" ve hatta "kapı yeşil" bunu kanıtlamaz. Bu yüzden bu vardiyada her düzeltme, davranışın kendisini değil SONUCUNU sınayan bir testle kilitlendi (kanala giden metot · veritabanına yazılan satır · deltada taşınan alan · önbellek anahtarını değiştiren adres) ve üç bulgu doğrudan ÜRÜNÜN KENDİSİNDEN ölçüldü (HTTP başlıkları, `sync_changes` satır sayısı, `pint --test` çıkış kodu).
